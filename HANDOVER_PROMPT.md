# Petrin svet Handover Prompt

Location: `E:\GitHub\Games for kids`

Purpose

This file summarizes the current workspace, conventions, and project state so the next session can continue without friction. Read this before making changes. It is refreshed at the end of every session.

## First thing next session — Racing game decisions (ALL 10 CONFIRMED 2026-09-22)

The Мала тркачица (Little Racer) game is fully built (Stages 1–5, tasks 95–99) and committed. The 10 gameplay/design decisions below were made autonomously during the build and have now been **explicitly confirmed by the user as-is (2026-09-22)**. Do NOT re-open or re-litigate them. Only #8 is called out because it visibly changes the game on the user's own machine:

1. **Unlock progression = wins-based thresholds `[0, 2, 4, 7]`** — 3rd car of each driver unlocks after 4 finished races, 4th after 7. No coins/currency; kids earn cars by completing races. (Also drives the ARIA/🎉 "НОВО" unlock toast.)
2. **One picker** — a single 8-card combo grid (2 drivers × 4 cars): driver sprite + car emoji, selected = gold ring, locked = greyed 🔒 + "N победе" hint, no pick-through on locked.
3. **Car stats stay subtle** — only `accel` (0.95–1.15 speed-growth multiplier) added to the existing maxSpeed/steer; NO stats UI — differences are "feel" only, shown as name + emoji.
4. **Countdown words use existing MP3s** (`три`/`два`/`један`); `Крени!` uses TTS. No new audio assets.
5. **Engine hum is synthesized** Web Audio (twin sawtooth, pitch/gain follows speed); music restarts on replay (also fixed a pre-existing bug: music never resumed after a race).
6. **Pickup collect → 5 sparkle particles** in the world's finish color (gold default).
7. **Controls = tablet-first thumb zones** — two big round buttons (⬅/➡) inside full screen-half zones (left/right 50% width, top 26vmin → bottom); portrait tablets ≥520px get an "Окрени уређај водоравно 🌍" overlay.
8. **Reduced-motion caveat (watch this one):** the user's OS reports `prefers-reduced-motion: reduce` = ON (same reason task 83 was reverted). Consequence on the user's machine (`racing.js` `&& !REDUCED_MOTION`): the obstacle screen-shake AND the 2026-09-11 FOV/scale pulse + speed-line streaks are suppressed; `@media (prefers-reduced-motion: reduce)` also removes button-press and progress-fill transitions. Banking lean, hills, fog, shadows, skids/dust and the vignette still render (kept so the user can SEE the upgrade; discrete/gentle effects stay on). Slowdown + sound feedback always work. If the user wants any of the gated effects, it's a one-line change.
9. **Wins count only on a real finish** — quitting mid-race never counts (guarded by `finishRecorded`, reset in `restart()`).
10. **Locked cars announce via ARIA + 🎉 "НОВО: <car>" toast** when a threshold is crossed.

Verified state to give the user: `racing_smoke.js` **39/39 PASS**, `hub_smoke.js` ALL PASS, docs mirrored (offline pack regenerated). **Open work: user commits the 2D racer visual-style adjustment (this session) + push, then play-tests.**

## New game now in review — Мала тркачица 3Д (task 100) — FULL 8-WORLD GAME (2026-09-22)

`pages/racing3d.html` + `games/racing3d.mjs` (ES module) + `games/racing3d-config.js` + vendored `game/assets/lib/three.module.min.js` (r169). Full three.js 3D racer. The 6-item scope the user confirmed (2026-09-22) is all built and headless-validated: **8 themed worlds** (reads the shared 2D `RACING_CONFIG.worlds` — Ливада/Плажа/Снег/Слаткиш/Џунгла/Свемир/Ноћ/Фарма; single source of truth, no palette duplication), **world picker** (start modal: 8 world cards + kart colors, countdown only starts on «Крени!»; 🌍 HUD button + «Изабери свет 🌍» in the win modal reopen anytime = pause → fresh reload on confirm), **per-world pickups** (`buildPickupMesh`: 🌸/🐚/❄️/🍭/💎/⭐/🌙/🥕), **per-world music** (mirrors the 2D racer's lookahead step-scheduler, theme from shared `RACING_CONFIG.music[world.music]`, 🔊 toggle persists `racing3dMusic`, starts on «Крени!»), **win-flow progression** («Следећи свет ➡️» saves next world + reloads; `racing3dSave` = `{wins, world, kart}`), **kart color picker** (4 chassis colors, cosmetic). New **rock obstacle** mesh; obstacle hitText from def. Default world = meadow, so the smoke kept its original thresholds. The 2D racer's hub button is now `hidden` (DOM wiring intact — 10 buttons still counted).

Debug hook: `window.__r3d` (mode/speed/lateral/progress/lap/score/flowerTotal/worldIdx/worldCount/kartIdx/pickupKind/musicOn/confirmStart, `tris()` = last frame triangles drawn). `confirmStart()` dismisses the picker and starts the countdown (used by the smoke).

Validation: `racing3d_smoke.js` **17/17 PASS** (picker presence + card/swatch counts + card-click selection + music-on, then the full original steering/particles/map/hills battery), `hub_smoke.js` ALL PASS, `node --check` clean; throwaway headless probe confirmed night (Ноћ/🌙/kart-2) and beach (Плажа/🐚) world boots. `docs/` re-synced. **NOT committed — user play-tests the full 8-world game then commits/pushes.**

**Next:** user play-tests the ambience-polished game on tablet (decor props, stars/clouds/moon, per-world ambient sounds, Снег snowfall / Свемир star-rain, pickup float, obstacle beep) → polish rounds per feedback (build-then-polish rhythm). Follow-ups NOT in scope unless asked (previous round): kart colors 6–8, AI/rival karts, unlockables.

## Next implementation task — Racing3d polish round 2 (task 102, external AI review plan)

The racer analysis was reviewed by ChatGPT/Claude/Grok/DeepSeek (reviews + analysis in `resources/3dracer analysis/`; combined plan in `resources/3dracer analysis/combined_action_plan.md`). Task 102 turns the deduped recommendations into **11 micro-batches**: (1) frame-timing normalization + `__r3d.step(dt)` 60↔120 Hz test hook — **DONE, 18/18**; (2) touch/palm hardening — **DONE, 19/19**; (3) visibility pause + audio suspend — **DONE, 20/20**; (4) reactive decor billboards — **DONE, 21/21**; (5) contact/blob shadows *(user-requested)* — **DONE, 22/22**; (6) boost visual identity — **DONE, 23/23**; (7) pickup feedback (kart emissive flash on collect + rising chime pitch) — **DONE, 24/24**; (8) celebration choreography (world-colored petal bursts on lap-complete + finish) — **DONE, 25/25**; (9) audio buses + ducking — **DONE, 26/26**; (10) minimal spoken Serbian hint — **DONE, 27/27**; (7) pickup feedback (emissive flash + rising pitch); (8) lap/finish celebrations; (9) audio buses + ducking + progressive obstacle tone; (10) minimal spoken Serbian hint («Пази!»/«Браво!», beep stays authoritative); (11) manual/dev QA (WebGL context-loss, iOS audio unlock audit, offline matrix, `?debug=1`). **User-locked decisions:** simultaneous L+R steer = **most-recent-wins**; resume after background = **clean auto-resume** (no overlay); **shadows included**. Ground truth: the feared per-frame `x *= 0.85` decay does NOT exist — easings at racing3d.mjs:1416/1419/1425/1563 use `Math.min(1, k*dt)`; Batch 1 is normalization, not a bug fix. Boost-FOV kick already present. Order: batch 1 → 2 → 3 → … → 11, each approved by the user before starting. Batch status: **1–9 done (26/26); Batch 10 DONE (2026-09-23, spoken Serbian hint, 27/27); next = batch 11 (manual/dev QA: WebGL context-loss, iOS audio unlock audit, offline matrix, `?debug=1`) — to be driven with Playwright MCP (session tool)**.

## Session log — 2026-09-23

- 2026-09-23 — **Task 102 Batch 6 DONE — boost visual identity (23/23).** `racing3d.mjs`: **(c) warm amber boost dust** — `dustPuff(pos,col,tag)` gained optional color/tag; while `now < boostUntil` a warm dust kicker (`boostDustAcc`, 90 ms) puffs `0xffaa33`/`0xffdd55` off the kart rear (`tag:'boostDust'`). **(d) soft rear-wheel road trail** — new `rearTrail(posL,posR)` drops flat, semi-transparent (`op:0.45`) warm trail puffs at the two rear-wheel positions off `tan`/`right` (`trailAcc`, 50 ms, `tag:'boostTrail'`); `spawnP` gained an optional `op` field. **(a) flames tagged** `boostFlame`. New `__r3d`: `tagged(tag)`, `triggerBoost(dur?)` (ms override for the harness, defaults `BOOST_TIME` for the real pads). `racing3d_smoke.js` **23rd check**: halted-loop + `triggerBoost(250)` → flames/dust/trail all spawn while boosting, then after expiry + 75 virtual steps `boosting()` is false and every boost tag clears to 0. Validation: `node --check` clean; `racing3d_smoke.js` **23/23 PASS**; `hub_smoke.js` ALL PASS; docs synced (201) + offline rebuilt (200). NOT committed (user commits/pushes).
- 2026-09-23 — **Task 102 Batch 7 DONE — pickup feedback (24/24).** `racing3d.mjs`: collect path refactored into **`collectPickup(f)`** (shared by the collision loop and the new `forceCollect(i)` hook, so the smoke exercises real code). **(1) hull emissive flash** — `kartFlash` 1 on collect, decays `dt*3`, `kartMat.emissive` lerps `0x120000`→`world.pickupColor`→base (0.33 s pop; a material tint, not reduced-motion-gated). **(2) rising chime** — `pickupCombo++` per collect; `lastPickupFreq = pentaFreq(combo)` on the C5 major pentatonic `[0,2,4,7,9,12,14,16,19,21]` (587→659 Hz first pair), double-tone `tone(fq,0.07)` + `tone(fq*1.5,0.09,0.06)` replaces the fixed 1046/1568. **(3) combo reset on obstacle** — hit path refactored into **`hitObstacle(o, now)`** (`pickupCombo = 0` inside), called by the loop and the new `testObstacleHit()` hook. New `__r3d`: `combo()`, `resetCombo()`, `kartFlash()`, `lastPickupFreq()`, `forceCollect(i)`, `testObstacleHit()`. `racing3d_smoke.js` **24th check**: reset combo → 2 forceCollects (combo 1→2, strictly rising freq), flash 1→0.25→0, obstacle hit resets combo to 0. Validation: `node --check` clean; `racing3d_smoke.js` **24/24 PASS**; `hub_smoke.js` ALL PASS; docs synced + offline rebuilt. NOT committed (user commits/pushes).
- 2026-09-23 — **Task 102 Batch 8 DONE — celebration choreography (25/25).** `racing3d.mjs`: new **`celebrateBurst(pos)`** — world-colored petal ring (`world.finishColor` + `world.pickupColor` + white, `geo:'small'`, `tag:'celebrate'`) reusing the shared `spawnP` emitter; particle count capped to remaining `MAX_PARTICLES` headroom (`Math.min(24, MAX_PARTICLES - particles.length)`), the 420 cap can never overflow. Fires on every non-final lap-line cross (the `"Круг n/3"` round path) and is **layered over the finish confetti** in `finish()` (110 confetti + ≤24 world petals). `racing3d_smoke.js` **25th check**: `seekToProgress(0.985)` (just under the next lap line in drive mode) + 120 virtual steps → `lap()` increments (1→2) and `tagged('celebrate') > 0` with `particles() <= 420`. Validation: `node --check` clean; `racing3d_smoke.js` **25/25 PASS**; `hub_smoke.js` ALL PASS; docs synced (201) + offline rebuilt (200). NOT committed (user commits/pushes).
- 2026-09-23 — **Task 102 Batch 9 DONE — audio buses + ducking (26/26).** `racing3d.mjs`: all SFX/announcements now flow through **one per-frame audio queue** instead of inline oscillator calls — `queueAudio({k})` (`qTone`/`qSweep`/`qSpeak`/`{k:'chime'}`), 40 ms wall gate, `drainAudio(now)` heads `update()` → **at most one generator/speech call per frame**, same-frame events sequenced (16-item burst guard). **Obstacle announce** (`announceObstacle(o, now)`): speaks `hitText` (Serbian) + falling **minor-third G4→E4 "uh-oh"** duo, arms `announceDuckUntil`; while active, duckable SFX play at 40% (`item.duck !== false`). Ducking needed a volume knob → `game/shared/audio.js` `window.tone()` got optional **`vol` (default 0.25)** (backward compatible, other games untouched). **Exponential backoff** on re-announce 600→1200→2400 ms (`o.msgGap`/`msgUntil`/`lastMsgAt`, reset after 3 s calm); physical effects stay on the 600 ms cooldown. Countdown/collect/lap/boost/warn/finish-chime all re-routed through the queue. New `__r3d`: `audioQueueLen()`, `audioDrained()`, `ducking()`, `setWarnUntil(null)`, `resetAudio()`. `racing3d_smoke.js` **26th check**: forceCollect queues 2 → back-to-back steps drain only 1 (one-per-frame), 55 ms sleep drains the 2nd; testObstacleHit queues exactly 4 + arms duck; 4 gated steps drain all; immediate repeat hit enqueues only the thud (backoff gate); G4/E4 = 3.00 semitones. Validation: `node --check` clean (racing3d.mjs + audio.js); `racing3d_smoke.js` **26/26 PASS**; `hub_smoke.js` ALL PASS; docs synced (201) + offline rebuilt (200). NOT committed (user commits/pushes).
- 2026-09-23 — **Task 102 Batch 10 DONE — spoken Serbian hint (27/27).** `racing3d.mjs`: obstacle announce speaks the **minimal `Пази!`** (full `hitText` stays on the visual banner) and the boost hint speaks **`Буст!`** — also added to the `triggerBoost` smoke hook so hooks mirror the real pad path; **beeps stay authoritative**: the announce batch is 3 tones vs 1 speech (`audioKinds()` = `tone,speak,tone,tone`), so a lagging/silent TTS never mutes the call. New `__r3d`: `audioKinds()`, `lastSpoken()`, `resetObstacleMsgs()` (clears per-obstacle announce backoff for tests). `racing3d_smoke.js` **27th check**: resetAudio+resetObstacleMsgs → obstacle hit queues `tone,speak,tone,tone` with nothing voiced until drained (4 gated drains → `lastSpoken()==='Пази!'`); boost queues `sweep,speak` and drains to `Буст!'`. Validation: `node --check` clean; `racing3d_smoke.js` **27/27 PASS** (full run confirmed); `hub_smoke.js` + docs sync + offline rebuild RERUN PENDING (machine Chrome load wedged headless boots mid-session — environment, not code). Headless harness teardown hardened: `tools/headless.js` `killChromeByTag` now runs with `{ timeout: 8000, stdio: 'ignore' }` so `h.close()` can never hang the smoke exit. NOT committed (user commits/pushes).
- 2026-09-23 — **Test-infra decision (user, per internet research): Hybrid.** Keep `tools/headless.js` as the fast deterministic functional gate; add **Playwright MCP (`npx @playwright/mcp`) as a SESSION-ONLY play-test tool** for batch 11 QA + WebKit/tablet checks — configured in `opencode.json` `mcp.playwright`, enabled, never committed to `game/`. `racing3d_smoke.js` at **27 checks** is fully green; recent slowness/wedges were NOT test failures but (a) my own over-aggressive `Stop-Process` sweep that destabilized the machine Chrome and (b) `killChromeByTag`'s blocking pwsh scan (fixed with the timeout above). MCP is NOT active until opencode restarts — batch 11 starts next session.

## Session log — 2026-09-22

- 2026-09-22 — **Task 102 Batch 4 DONE — reactive decor billboards + start-gate wave.** 16 emoji `THREE.Sprite` billboards now pulse `1→1.15→1` (0.3 s sine, dt-accumulated), tilt `rotation.z`, bob `y` as the kart passes within ~10 track units; 24 pennants + start-gate banner wave 0.8 s after countdown + each lap pass. Zero new draw calls (transforms on existing meshes). `decorAmp` master = `REDUCED_MOTION?0:1`. Scoping bug fixed en route (hoisted `gateWave/gateBanner/gateFlags/decorAmp/decorBils` out of the `{}` landmark block to main scope). New `__r3d` hooks `seekToProgress/decorNear/decorScale/decorAt/decorAmp/setDecorAmp` (setDecorAmp = test-only a11y-gate override). `racing3d_smoke.js` **21/21 PASS** (new check: gated-off stays 1.0, enabled pops to 1.13, returns to 1.0). `hub_smoke.js` ALL PASS; docs synced (201) + offline rebuilt (200). NOT committed (user commits/pushes). **Watch item:** headless AND the user's OS are `prefers-reduced-motion: reduce` → the billboard pop + gate wave are OFF for the user by default; decide whether to relax the decorative-motion gate (screen-shake stays gated regardless).
- 2026-09-22 — **Task 102 Batch 3 DONE — visibility pause + audio suspend (locked decision).** `racing3d.mjs` gained `onHide()` (stopMusic + suspend AudioContext when running — silences hum/ambient/music/beeps away) and `onShow()` (resume ctx if suspended/interrupted + restart music when past the menu + **discard one stale `clock.getDelta()`** so no kart jump on return). Bound to `visibilitychange`; `blur`/`focus` guarded by `document.hidden` so devtools focus doesn't kill music. No overlay (locked clean auto-resume). New `__r3d` hooks: `musicPlaying()`, `audioState()`, `testVisibility(hidden)`. `racing3d_smoke.js` **20/20 PASS** (new check: hide stops music interval, show restarts it, sim still steps). `hub_smoke.js` ALL PASS; docs synced (201) + offline rebuilt (200). NOT committed (user commits/pushes).
- 2026-09-22 — **Task 102 Batch 2 DONE — touch/palm-press hardening.** `racing3d.mjs` input rewritten: per-zone pointerId ownership + `setPointerCapture`, release via pointerup/pointercancel/lostpointercapture (kills the stuck-zone-on-drag-across bug), palm filter (touch `radiusX > 48` ignored), **most-recent-wins** when both zones held (locked decision; keyboard also stamps `recentPress`). `racing3d.html`: zones got `touch-action: none`. `racing3d_smoke.js` **19/19 PASS** (new check: synthetic touch events prove most-recent-wins + palm ignore). `hub_smoke.js` ALL PASS; docs synced (201) + offline rebuilt (200). NOT committed (user commits/pushes).
- 2026-09-22 — **Task 102 Batch 1 DONE — frame-timing normalization + test hook.** 8 easing sites converted `Math.min(1, k*dt)` → `1 - Math.exp(-k*dt)` (racing3d.mjs:1383/1411/1415/1416/1418/1419/1425/1563); the rest of the sim was verified seconds/ms-based (music scheduler, ambient, beep 720ms, weather, pickup bob, countdown). New `__r3d` hooks: `lastDt()`, `step(dt)`, `haltLoop(bool)`, `resetSteerState(yaw,drive,lean)`. `racing3d_smoke.js` now **18/18 PASS** with a new check proving steering converges identically at dt=1/60 vs 1/120 (dyaw≈1e-16, droll≈7e-4), via the loop-halted virtual-step path. `hub_smoke.js` ALL PASS; docs synced (201) + offline rebuilt (200). NOT committed (user commits/pushes when it feels right).
- 2026-09-22 — **Task 102 CREATED (NEW) + combined action plan saved.** Delivered the merged review plan (`resources/3dracer analysis/combined_action_plan.md`) synthesized from the 4 external reviews + own analysis (verified against real code). User confirmed the 3 open decisions: most-recent-wins on simultaneous steer, clean auto-resume, contact shadows included. Marked IN PROGRESS only when implementation of batch 1 begins.

- 2026-09-22 — **Task 100: polish round 1 — world ambience pack (6 upgrades user-selected via question tool; kart colors 6-8 NOT selected).** `racing3d-config.js` + `racing3d.mjs`: **(1) per-world decor props** — 16 camera-facing `THREE.Sprite` emoji billboards from the shared `world.decor` per world (~2.6–4.2u tall, `decorScale` respected). **(2) sky props** — 260-point starfield for `sky:'stars'` (Свемир + Ноћ), moon disc + crater for Ноћ (`moon:true`), soft sun disc for day worlds, 7 drifting cloud sprites (`sky:'clouds'`) that circle the track at 300-rad; all `fog:false`. **(3) per-world ambient sounds** — ported the 2D racer's `playAmbient` (bird/waves/wind/chime/stars/owl + noise buffer) into `racing3d.mjs`; lean `window.ctx()`-wrapped try/catch; the music scheduler now fires each theme's `m.ambient` at its own rate/vol. **(4) weather particles** — `world.weather`: Снег→snow, Свемир→star-rain; emitter spawns 2/150 ms around the kart (`_pSnow`/`_pStar` geoms, cap `MAX_PARTICLES-30`), runs in every mode. **(5) pickup idle motion** — undriven pickups spin (`rotation.y += 1.2·dt`) + bob (sin ±0.12 around stored `baseY`, per-pickup phase). **(6) live obstacle density + warning beep** — `OBS_COUNT = clamp(6, round(density·1700), 16)` from shared `obstacleDensity` (meadow 7, jungle 14…), obstacles now store `t`, and a soft 330 Hz `window.tone` beeps ~every 720 ms when the nearest obstacle is 12–90 units ahead. Validation: `node --check` clean; `racing3d_smoke.js` **17/17 PASS**; `hub_smoke.js` ALL PASS; throwaway headless probe booted + drove Снег (❄️, snowfall particles), Ноћ (🌙) and Свемир (⭐, star particles) via the REAL picker-click→`location.reload` path (tris 13–17 k; note: direct `Page.reload`/evalv-`location.reload()` is unreliable in the headless harness — the picker click path is the proven one). `tools/sync-docs.sh` (201 files) + offline package rebuilt (200 entries). NOT committed (user play-tests then commits/pushes).

- 2026-09-22 — **Task 100: full-game build — 6-item confirmed scope implemented.** `racing3d-config.js` rewritten to per-world 3D extras keyed to the shared `RACING_CONFIG.worlds`; `racing3d.mjs` is now world-driven (reads `racing3dSave {wins, world, kart}`, merges shared palette + extras, boots to a start picker instead of auto-countdown). Per-world pickup meshes (flower/shell/snowflake/candy/gem/star/moon/carrot), per-world music (2D racer's step-scheduler, `racing3dMusic` toggle), win-flow «Следећи свет» progression, 4-color kart picker, rock obstacle. `racing3d.html` gains `racing-config.js`, start modal, corner 🌍/🔊 buttons, win-modal action stack. Side task: 2D racer hub button hidden (`index.html` `hidden` attr + `.hub-btn[hidden]` CSS; DOM untouched). Validation: `node --check` clean; `racing3d_smoke.js` **17/17 PASS**; `hub_smoke.js` ALL PASS; throwaway headless probe verified night + beach world boots and kart-2 persistence. `docs/` re-synced; offline pack rebuild pending (ask user). NOT committed (user play-tests then commits/pushes).
- 2026-09-22 — **All 10 task-99 racing decisions CONFIRMED as-is by the user** (unlock `[0,2,4,7]`, single 8-card picker, subtle accel stats, MP3 countdown + TTS Крени!, synth engine hum, pickup sparkles, tablet thumb zones, reduced-motion screen-shake gate KEPT — user's OS has reduce on, wins-on-real-finish, ARIA+🎉 unlock toast). No code changes — recorded in PROJECT_TASKS task 99 + this file top section.
- 2026-09-22 — **Task 101 DONE — offline download self-healing.** `game/sw.js`: new `addWithTimeout()` (20 s AbortController timeout + 2 retries) replaces the bare `await cache.add(url)` in `cacheAllAssets`; a file that keeps failing is **skipped and the loop continues**, collected in `skipped[]` sent on `cache-complete`. `game/shared/navigation.js`: completion status shows "Сачувано за ванмрежни рад (N прескочено — покушајте поново)" when files were skipped. Fixes the stuck-percentage symptom (root cause: no per-file timeout in sw.js). Validation: `node --check` clean, `hub_smoke.js` ALL PASS, docs synced (201), offline package rebuilt. NOT committed (user pushes).

## Session log — 2026-09-14

- 2026-09-14 — **Task 100: play-test round 5 ("the whole car is drifting to the side", per user).** `racing3d.mjs`: ROOT CAUSE of the complaint — lateral velocity was driven by `steerYawValue`, so during the slow wheel-unwind (2.8·dt) on release the kart **kept sliding sideways ~1 s**. Fix: split **`steerDrive`** (drives `lateral += steerDrive·LATERAL_GAIN·dt`) from the visible wheel yaw. `steerDrive` only builds while a button is held (8·dt, same as the wheels) and **dies fast on release (10·dt)**, so the car stops drifting almost immediately and holds its position; `steerYawValue` keeps unwinding slowly (2.8·dt) purely as the wheels returning to the middle. Body bank `targetLean` retied to `steerDrive` (straightens up fast with the stop). Smoke strengthened to prove it: samples at +150 ms and +400 ms post-release assert **wheels still visibly turned (`yaw>0.1`, `yaw>0.07`) while the car barely moves (`|Δlat|<0.4`)**; then wheels at the middle (`|yaw|<0.05`), fully settled (`|Δlat|<0.15`), and a left press reverses clearly → `racing3d_smoke.js` **15/15 PASS**; `hub_smoke.js` ALL PASS; `node --check` clean. `docs/` re-synced (202 + zip), offline pack rebuilt. **NOT committed — user commits + pushes tonight.**

- 2026-09-14 — **Task 100: play-test round 4 (half speed + follow-the-wheels steering, per user).** `racing3d.mjs`: **(1) half speed** — `MAX_SPEED` 70→**35** (boost cap still 42, offroad 0.62×). **(2) steering reworked — the front wheels now LEAD the car**: replaced `lateral += steer·STEER_SPEED·dt` with wheel-angle-driven motion `lateral += steerYawValue·LATERAL_GAIN·dt` (`LATERAL_GAIN=26`; `STEER_SPEED` const removed; `STEER_YAW_MAX=0.45`). **Asymmetric easing** — turn-in at 8·dt when held, **slow return to the middle at 2.8·dt on release**; body bank `targetLean = (steerYawValue/STEER_YAW_MAX)·0.34`; nose slip-yaw eased 11·dt. (Superceded by round 5: lateral now driven by a separate fast-decaying `steerDrive` so the car doesn't keep side-drifting while the wheels unwind.)

- 2026-09-14 — **Task 100: play-test round 3 (wheel spin pattern + steering feel, per user).** `racing3d.mjs`: **(1) spin pattern** — each wheel is now a `wg` joint (front two in `steerWheels`) containing tyre + white ring + hub + a `studs` group: **4 gold tread studs** (`BoxGeometry(0.09,0.07,0.07)`, `boostColor` 0xffd23f + emissive) stuck on the tyre circumference at radius 0.55, rolled with `rotation.x` alongside the other `wheelParts` so the wheels clearly appear to spin (chase cam sees the sidewalls). **(2) steering feel** — button now **banks the body into the turn** (`kartLean.rotation.z` target `steer·0.36`, eased 10·dt; was 0.22 / 8·dt), **slip-yaw eased** (11·dt, was snapped) so the nose points into the direction of travel, and the two **front wheels visually steer** per-wheel `wg.rotation.y` (`steer·0.34`, eased 11·dt). On release the kart **stays where it is** (`lateral` freeze — no auto-center) and **straightens upright + forward** (both eased to 0). New `__r3d`: `spokes()` = 16, `steerState()` = `{roll, yaw, steerYaw}`. Smoke: new check "kart banks into the turn + wheels carry a spin pattern, then point forward on release" (`spokes===16`, `roll>0.15`, `steerYaw>0.1` held; all <0.05 after release) → `racing3d_smoke.js` **15/15 PASS**; `hub_smoke.js` ALL PASS; `node --check` clean. `docs/` re-synced (202 + zip), offline pack rebuilt. **NOT committed — user play-tests then commits + pushes.**

- 2026-09-14 — **Task 100: play-test round 2 (5 items, per user).** `racing3d.mjs` + `racing3d.html`: **(1) race-track mini-map** — `#r3d-map` 2D HUD canvas (top-center, 21vmin, border #FFD23F pill): `layoutMap()` fits a 140-sample top-down projection of the loop into the box (640→resize), per-frame `drawMap()` draws the asphalt band, the **driven arc** of the current lap (gold), a start/finish tick, and a **red kart marker** at `progress % 1` (linearly interpolated along the points); `__r3d.mapMarker()` exposes `{t,x,y,n}`. **(2) wider road** — `ROAD_WIDTH` 14→20, rumble/offroad limits scaled to the new half-width (10): `RUMBLE_X` 9.0 (0.6-wide red/white band just inside the white edge line at 9.45), `RUMBLE_ON` 8.4, `OFFROAD_LAT` 9.3, `MAX_LATERAL` 9.6, `BOOST_LAT` 3.0 (lane centres). **(3) wheel rotation fixed** — wheels previously spun around the VERTICAL axis (`rotation.y`, looked broken/wobbling); now the tyre + white rim ring + metallic hub `wheelParts` **roll around their axle** `rotation.x -= (speed/0.5)·dt` (radius 0.5) — `__r3d.wheelPose()` exposes `[[x,y],×4]`. **(4) hills more noticeable** — control-point height profile symmetric + taller (`sin(ang·2)·4.6 + sin(ang·4)·2.1 + jitter·2.6`, no more `max(0,…)` clamp → valleys now dip below y=0; sampled `hillRange` ≈ 12; the earlier clamp was also flattening the visible lows). **(5) "road under the green ground" (fixed)** — ROOT CAUSE: flat `PlaneGeometry` at y≈0 sat ABOVE the road ribbon in valleys → road hidden behind opaque grass. Now a **hilly terrain ribbon** (`TERRAIN_HALF=58`, `TERRAIN_SLOPE=0.10` edge droop `(dist−ROAD_HALF)·0.10`, height = track surface − 0.03) follows the curve so the road always sits ON the grass (`floorClear = minRoadClear = 0.05`), plus a far low plane at y=−16 for the view beyond the ±58 belt; trees/rocks/bushes re-seated with `groundYAt(p, offset)` (tree valley-guard loosened to `gy < −2`). Smoke re-tuned to the wider road (pads bound ≤9.6; rumble seeks 9.4) + 3 new checks (wheel roll, mini-map marker, hills ≥6 + floor clears): `racing3d_smoke.js` **14/14 PASS**; `hub_smoke.js` ALL PASS; `node --check` clean; docs re-synced (202 + zip), offline pack rebuilt. **NOT committed — user play-tests then commits + pushes.**

- 2026-09-14 — **Task 100: 3D look-and-feel pass (meadow-only, per user "apply all that is needed from RACING_VISUAL_STYLE_ANALYSIS.md").** `game/games/racing3d.mjs` + `racing3d-config.js` (added `boostColor`): (1) **cartoon kart upscale** — glossy plastic (shininess 90 + emissive 0x120000), white hot-rim sheen strip, rounded nose bumper, windshield (0x1e3a5f, shininess 140), chunky r0.5 wheels with white sidewall rings + emissive hubs, exhaust pipes (flame anchors). (2) **rich lighting** — warm sun (`world.sunColor||0xfff3d6`) + cool rim 0xbfe0ff (0.45) + fill (0.25); fog kept 90–420. (3) **landmarks readable from far** — 12 rocks + 16 bushes, flower patches (InstancedMesh, 130 instances / 1 draw call), 24 pennant flags, tall start-gate arch (posts + finish-color banner + checkered strip over the finish). (4) **particles** (`spawnP`/`updateParticles`, `MAX_PARTICLES=420`, per-particle cloned MeshBasicMaterial: vel/gravity/grow/spin/fade): exhaust boost flames, drift skid smoke, offroad dust, pickup **petal burst** (8 petals), obstacle puff (6), finish **confetti** (110 flakes). (5) **boost pads** — 6 seeded ⚡ pads (`BOOST_COUNT`, lane ±`BOOST_LAT=2.2`, canvas lightning texture), `BOOST_MULT=1.2` speed clamp + 6° FOV kick + `playWhoosh()` (`window.sweep(350,80,…)`) + flame emitters. (6) **drift** — `driftNow = steer && speed>0.5×MAX && |lat|<RUMBLE_ON`, skid smoke only, no speed penalty (toddler-safe). (7) **edge rumble strips** — flat vertex-colored red/white `ribbonVerts` bands at ±`RUMBLE_X=5.75` (replaces removed curbs), `rumbleShake` + `playRumble()` (`window.sweep(75,38,'square')`) when |lat|>`RUMBLE_ON=5.25`; `MAX_LATERAL` 5→6 so `OFFROAD_LAT=5.8` slow is reachable. (8) **camera juice** — sway `-steer*0.9*ratio`, suspension-follow `+bounce*2.0` (bounce +0.05 while boosting), `shake = max(finishShake, rumbleShake)`, hill-crest look `+ratio*0.4`. Shake/FOV extremes stay `REDUCED_MOTION`-gated; **confetti kept ungated** (celebration feedback — same rationale as the task-83 revert). New `__r3d` handles: `particles()`, `drifting()`, `boosting()`, `rumbleOn()`, `offroadState()`, `boostPads()`, `triggerBoost()`, `seekLateral()`. Debug-race fix: `driftNow` hoisted to main() scope (was shadowed inside update() → `__r3d.drifting()` ReferenceError). Validation: `node --check` clean; `racing3d_smoke.js` **13/13 PASS** (6 new: pads-on-lanes, boost→flames, drift on/off, rumble+offroad, geometry tris≈13k); `hub_smoke.js` ALL PASS; `docs/` re-synced (201 files). **NOT committed — user play-tests (FPS/feel, ideally under Live Server / the real tablet), then commits + pushes.**

## Session log — 2026-09-11

- 2026-09-11 — **2D racer visual-style adjustment — Option A Phases 1–4 (per user approval; NO rival pack — kept as a later option).** `racing.js`/`racing-config.js`: **P1 hills + camera juice** — seeded per-world rolling hills (`initHills`, `HILL_CHUNK=210`, `HILL_PX=170`, + per-world `hillStrength` 0.35–1.2), `roadY(d)` drives road/dashes/pickups/obstacles/decor/finish y (and collision) with per-frame camera follow (`camHill`, `pitch`); car banks into turns (`leanNow`), scene sways opposite the wheel, FOV/scale pulse + speed-line streaks + vignette (pulse+streaks gated by `REDUCED_MOTION`). **P2 textured road + depth** — alternating pavement bands + solid white edge lines + horizon fog veil + distance fade for far props + ellipse ground shadows under car/decor/pickups/obstacles. **P3 particles + boost pads** — wheel dust + skid marks when turning; seeded ⚡ boost pads (~8 per run) → 1.35× speed (cap ~120% of top) + flame sparkles + whoosh synth. **P4 better car** — vertical gradient 3/4 shading, suspension bob, rolling wheel spokes, cast shadow. New `__racing` handles: `roadY`, `hillStrength`, `lean`, `boosts`, `skid`, `bobPhase`. Validation: `node --check` clean; `racing_smoke.js` **39/39 PASS** (10 new checks: lean settle-to-center, hills vs flat baseline, per-world hillStrength, boost spawn + hit/flames, dust+skid, suspension bob); deterministic harnesses now flatten hills (`hillStrength=0`) exactly like curves. **NOT committed — user commits at the end; then play-test → iterated polish rounds.**
- 2026-09-11 — **Task 100 play-test tuning round (3 user feedback iterations, 3D racer).** (1) Too fast → `MAX_SPEED` 132→88→70. (2) Camera flips upside down → ROOT CAUSE `camera.lookAt()` with near-vertical look vectors on hill crests (gimbal flip); replaced with **quaternion-slerp** chase cam (hoisted `_tmpM`/`_tmpQ`, applied `1-exp(-9dt)`, roll via `rotateZ(steer*0.02)`) + hill amplitude 6→3.5→1.6→2 + cam floor guard `max(kart.y+5.0)`. (3) "Still no road" → the road/dashes/edges **ribbon** geometry computed `right = cross(tan, UP)` and wound triangles so their normal faced DOWN → `FrontSide` culling made the road literally invisible (only the ground `PlaneGeometry` and volumetric meshes (kart/flowers/trees) drew); fixed with `side: THREE.DoubleSide` everywhere + a classic **two-lane asphalt** look (dashed center line via `ribbonDash()`, solid white edge lines, flat tarmac, curbs removed) + camera pulled in (12 behind/6 high, look 10 ahead/1.5 up) + fog 90–420 + asphalt lightened 0x6b7176. Steering: removed the `lateral *= 1-0.5dt` auto-centering so the kart **holds its line** when released, added slip-angle body yaw `kartLean.rotation.y = clamp(atan2(lateralVel, speed))` + eased lean (`leanValue`, 8*dt). `window.__r3d` gained `tris()`. Smoke updated to assert hold-line steering + real drawn geometry (`tris()>1000`): `racing3d_smoke.js` **7/7 PASS**; `hub_smoke.js` ALL PASS; `node --check` clean; manifests + docs rebuilt after every change. **User sign-off: "ok good for now" → commit + sync next; next session = full 2D racer visual-style adjustment per the analysis, then 3D continuation.**
- 2026-09-11 — **Task 100 (3D prototype → full three.js 3D racer decision) AUTHENTICATED & NEW GAME BUILT.** Proto in `resources/racing3d-prototype/` (three.js r169, local file — user then decided "lets go towards this full 3D racer with three.js, keep the current 2D racer"). Decisions locked: no AI rivals, meadow-first vertical slice, chunky cartoon kart. Built new hub game `racing3d`: `pages/racing3d.html` + `games/racing3d.mjs` (ES module; `assets/lib/three.module.min.js` vendored) + `games/racing3d-config.js` (meadow: road/grass/fog/sky/curb/kart palette). Features: hilly closed-loop CatmullRom track w/ red-white curbs + checkered start, gradient sky dome + fog, chase cam (lag+look-ahead+FOV kick+tiny roll), suspension bounce + turn-banking kart, countdown 3-2-1-Крени! (shared speech), 🌸 flower pickups ×12 (tone chimes), 💧/🚧 obstacles (slowdown, sr-only announce), 3-lap win modal + `racing3dSave {wins}`, engine-hum synth on first input, reduced-motion-safe (shake/bounce suppressed), landscape hint, tablet thumb-zone + keyboard input. Integration: hub button `game-racing3d`, `navigation.js` route, `main.js` standalone map (`startRacing3D`); `headless.js` MIME map gained `.mjs`; `hub_smoke.js` games-button count 9→10 (expectation only). Manifests rebuilt `tools/build_offline.ps1` (200-entry cache list), `docs/` synced (201 files). Validation: `node --check` clean; new `tools/racing3d_smoke.js` **6/6 PASS** (boot→`window.__r3d`, canvas+HUD Serbian, countdown→drive, steer+auto-forward, page chrome); `tools/hub_smoke.js` ALL PASS. Pending user play-test on tablet (FPS + feel) → tuning + polish round (particles, rim sheen, worlds).

## Session log — 2026-09-09

- 2026-09-09 — **Task 99 (Racing Stage 5: Polish & Accessibility) IMPLEMENTED** in `game/games/racing.js`, `game/pages/racing.html` (validated; commit made locally, push pending — see commit message). **Countdown**: 3600 ms "3-2-1-Крени!" before every race (`beginCountdown`/`stepCountdown`/`countdownDisplay`), big pill text overlay (`drawCountdown`, gold numbers / green Крени!), tone per beat (`countTone` 660Hz tri / 880Hz square), speech "три"/"два"/"један" (existing MP3s) + "Крени!" (TTS). The real rAF `loop()` is gated during the countdown; **manual `update()` calls (smoke harness) are NOT gated** and `skipCountdown()` lets tests skip it; "Крени!" keeps drawing 600 ms after light-through. **Engine hum**: twin sawtooth 70/70.6 Hz → 900 Hz lowpass → gain 0.012→0.018, pitch 70→260 Hz with speed (`startEngine`/`updateEngine`/`stopEngine`); fixed a pre-existing bug — music never resumed after a race (`restart()` now restarts `currentWorld.music`). **Sparkles**: collecting a pickup bursts 5 colored puffs (`addSparkle`, `p.col`, gold/world finishColor). **A11y**: `#racing-announcer` sr-only `role=status aria-live=polite` + `announce()` on car/world pick, countdown beats, finish, unlocks; `REDUCED_MOTION` gates the shake translate; `@media (prefers-reduced-motion: reduce)` CSS. **Tablet-first**: `keys` control via two full screen-half thumb zones (`.racing-zone`, top 26vmin → bottom, z-index 11) wrapping the round buttons; portrait tablets ≥520px see the "Окрени уређај водоравно 🌍" overlay. New `__racing` handles: `particles()`, `pickups()`, `countdown()`, `beginCountdown`, `stepCountdown`, `skipCountdown`, `engine()`, `reducedMotion()`, `announce`. Validation: `node --check` clean; `racing_smoke.js` **32/32 PASS** (countdown sequence ms-exact, ARIA 'три', pickup sparkle ≥5 puffs, engine state type, sr-only region + 2 zones + portrait-only hint; steering checks now single synchronous eval — live rAF loop can't interleave); `hub_smoke.js` ALL PASS; `tools/generate_sw_list.js` + `tools/build_offline.ps1` + `bash tools/sync-docs.sh` run (197 files). Commit `f?` — see git log (push hangs on Git Credential Manager; commit stays local).
- 2026-09-09 — **Task 98 (Racing Stage 4: Full Driver/Car Roster & Progression) IMPLEMENTED** in `game/games/racing-config.js`, `game/games/racing.js`, `game/pages/racing.html` (committed `ffa64db`, pushed pending). **Config**: `short` driver labels (Маца / Девојчица); per-car `accel` (speed-growth multiplier, 0.95–1.15) so the 4 cars per driver differ subtly; `RACING_UNLOCK_WINS = [0,2,4,7]` exported as `unlockWins`. **JS**: save module (`racingSave` localStorage `{char,car,world,wins}`, `loadSave`/`persistSave`/`applySave` applied at boot); `isUnlocked` = `wins >= threshold[carIndex]`; the picker became a **8-card combo grid** (driver sprite + car emoji overlay + names, gold ring on selected, greyed 🔒 + "N победе" on locked, no pick-through); world picker persists `save.world`; `finishRace` increments+persists wins (guarded by `finishRecorded` reset in `restart()`) and shows "🏆 Трке: N" + a "🎉 НОВО: ..." toast on threshold crossing; speed growth = `speedGrowth * accel * mult`. New `__racing` test handles: `selection()`, `wins()`, `isUnlocked(,)`, `unlockWins()`, `saveState()`, `finishRace`. Validation: `node --check` clean; `racing_smoke.js` **31/31 PASS** (5 new Stage-4 checks incl. a real localStorage restore after page reload); `hub_smoke.js` ALL PASS; `bash tools/sync-docs.sh` run. Commit `ffa64db`, push pending (GCM hangs).
- 2026-09-09 — **Task 97 (Racing Stage 3: Obstacles & Difficulty) IMPLEMENTED** in `game/games/racing-config.js` + `game/games/racing.js` (NOT committed; verified headless). **Config**: `OBSTACLE_TYPES` — 💧 puddle (−50% speed, 1.5s, persists), 🪨 rock (stop 0.5s, then crumbles away with particles), 🚧 barricade (×0.3 + progress knocked back 50, 1s); per-world `obstacleDensity` 0.004–0.008 (= obstacles per world-unit → spacing `1/density`, ~7–14 per run, denser in later worlds) + `obstacleTypes` (meadow/candy 2-types lightest, beach rock-no-barricade, space no puddle, snow/jungle/night/farm all 3). **JS**: `initObstacles()` seeded by world, spawns 600→goal−400, min gap 140; near a pickup (<60) flips to the OPPOSITE lane so they never stack (pickup 240-spacing + ±120 forbidden radius tiles the line, so distance-skip is impossible with this seed — lane-flip is the correct splice). `drawObstacles()` emoji at lane offset, perspective-sized ×1.25; `updateObstacles()` collides via the pickup band, per-obstacle `hitCd` 600ms + per-type 800ms cooldown; `triggerObstacle()` = speed modifier + screen shake + gentle `playThud` synth; rock self-removes with a 6-particle puff; effect tracked by game-time `slowdown.remaining` (deterministic under headless), expiry snaps speed back ≥ `cfg.startSpeed`. Exposed on `window.__racing`: obstacles/slowdownState/triggerObstacle/shake. Validation: `node --check` clean on both JS; `tools/racing_smoke.js` **23/23 PASS** (6 new obstacle checks incl. a real steering-into-obstacle collision via update()); `tools/hub_smoke.js` ALL PASS (additive only, zero edits to existing games). `bash tools/sync-docs.sh` pending (run on commit).
- 2026-09-09 — **Task 96 Stage 2.2-2.4 (worlds, decor, music, HUD) IMPLEMENTED** in `game/games/racing-config.js`, `game/games/racing.js`, `game/pages/racing.html` (NOT committed; verified headless). **Config** rewritten to 8 themed worlds (Ливада/createCount/Плажа/Снег/Слаткиш/Џунгла/Свемир/Ноћ/Фарма) — each with sign pickups 🌸🐚❄️🍭💎⭐🌙🥕, its own grass/road/horizon palette, `curveMax`/`curveSeed`/`goal` (all 7200), a decor emoji list, and a `music` key; plus 8 synthesized music themes (`RACING_MUSIC`) and car rosters (4 cars per driver: box/yarn/fish/rocket + bike/scooter/skates/hoverboard with own emoji + maxSpeed/steer/body). **JS**: seeded roadside decorations (`initDecor`/`drawDecor`, mulberry32 from world seed); `playAmbient` extended with waves/wind/chime/stars/owl variants; finish line per-world (`finishLineDist` follows `currentWorld.goal`); `drawCar` draws the selected car's emoji + body color; HUD updates in `draw()` (`#racing-pickup-count` with world `collectibleName`, `#racing-progress-fill` width %); win modal score uses the world's collectibleName; char-picker click and world change route through `restart()`. Fix: `restart()` mutates `keys` in place instead of reassigning so `window.__racing.keys` stays a live reference (smoke steering checks were reading a stale object). Validation: `node --check` clean on both JS files; `tools/racing_smoke.js` **16/16 PASS** (config now asserts 8 worlds; new "world switch: finish line and label follow the chosen world" check); `bash tools/sync-docs.sh` run (docs/ mirrors game/ 197 files).

## Session log — 2026-09-08

- 2026-09-08 — **Task 96 Stage 2.1 (wider road + procedural curves + pickup size) IMPLEMENTED** in `game/games/racing.js` + `racing-config.js` (NOT committed; verified headless). (a) Road widened `W*0.22` → `W*0.34` (clamped 200–640). (b) Seeded procedural curves: `curveAt()` = smoothstep-interpolated curvature knots (chunk 240 world units, seed `curveSeed`, strength `curveMax*0.0005`) → per-frame lateral offset map `offs[d]` (double-integrated heading, normalised to peak `curveMax` capped `MAX_CURVE_OFF=0.45` road-widths) → `roadCenterX(d) = ROAD_CENTER + offs[round(d)]*projectW(d)`. Road quads, grass polygons, center dashes, pickups and finish line all follow the curve; grass switched from `fillRect` to curved polygon fills (fills whole left/right side so no stale-strip gaps). (c) Pickup size now perspective-correct and INVERTED growth: `pickupScreenSize(d) = max(ROAD_MIN_W, pickupSize*(projectW(d)/ROAD_MAX_W)*1.6)` — small far (25px @380) → big near (54px @20); collision uses the same size. `initCurve()` called on boot + restart. Exposed `roadCenterX`, `offs`, `pickupSizeAt` on `__racing`. Validation: `node --check` clean; `tools/racing_smoke.js` now **17/17 PASS** (added "curves: road bends ahead" + "pickups grow as they approach"); `tools/racing_probe.js` shows road ~413px wide at bottom (was 281), mid-rows bend smoothly.
- 2026-09-08 — **Task 95 (Racing Stage 1) COMMITTED** as `4e028b4` ("Add racing game Stage 1: Мала тркачица vertical slice + docs sync + offline pack", 24 files) per user instruction. Racing source now tracked: `game/pages/racing.html`, `game/games/racing.js`, `game/games/racing-config.js` (+ docs/ mirrors), `tools/racing_smoke.js` (15 checks), `tools/racing_probe.js`. Validation before commit: `node --check` on all JS green; `tools/racing_smoke.js` 15/15 PASS; `tools/hub_smoke.js` ALL PASS; `tools/sync-docs.sh` run (197 files); `tools/build_offline.ps1` rebuilt `docs/game-offline.zip`. NOTE: drawRoad reworked this session (camera-fixed segment depths + world-locked scrolling center dashes) — user-reviewed play-test: "ok is a bit better".
- 2026-09-08 — **Task 96 (Racing Stage 2) STARTED** (IN PROGRESS) per user: "commit and sync this so far and start phase 2 execution with this comments added to it as part of refactoring within phase 2". User play-test feedback folded into Stage 2 scope (recorded in PROJECT_TASKS task 96): **road too narrow and not interesting; no cool curves; world boring; pickups must start small and grow larger as they approach the car**. Stage 2 goal (from RACING_GAME_PLAN): 6-8 themed worlds, distinct pickup sprites, procedural track generation (curves/hills), side decorations, per-world music, HUD (world name, pickup count, progress bar).
- 2026-09-08 — Task 94 (PWA offline installer) marked DONE per user confirmation that the offline installer is working correctly on Android. The PWA manifest, service worker, hub UI (download/update/ZIP fallback), build script, and caregiver docs are complete. Package `docs/game-offline.zip` (5,338,546 bytes) built and validated locally. Awaiting user commit/push.
- 2026-09-08 — Task 95 (Racing Game Stage 1) implemented and DONE. New pseudo-3D racing game "Мала тркачица" with: segment-based road renderer (sky gradient, grass, dashed center line), canvas-drawn car at bottom-center with left/right input + speed curve, pickup collection (🌸 flowers → score + pop), finish line with win modal + 🏁 celebrate + "Браво!", character picker (Маца Истраживачица / Истраживачица — reuses explorer kitty sprites), single world (Ливада) with music theme. Hub integration: button in ИГРЕ sub-hub, navigation route, standalone boot. Files: `game/pages/racing.html`, `game/games/racing.js`, `game/games/racing-config.js`, `tools/racing_smoke.js`. `sw-cache-list.json` regenerated (196 entries). Validation: `node --check` passes; `tools/racing_smoke.js` 13/13 PASS; `tools/hub_smoke.js` ALL PASS. No edits to existing game files (additive only). Task 96 (Multi-World) is next, pending user approval.
- 2026-09-08 — Task 95 wrap-up completed (per user decision "finish wrap-up only; do not start Stage 2 yet"): `tools/sync-docs.sh` run (docs/ now mirrors game/ — 197 files incl. racing), README.md updated for the new game (racing.html/racing.js/racing-config.js added to both folder-structure blocks + standalone-pages list; ИГРЕ count 8→9 both in Current Status and the Navigation Model; new 🏎️ Мала тркачица section in "Current Mini Games"; current-focus line now "sixteen games"), and the offline package rebuilt via `tools/build_offline.ps1` (196-entry cache list, offline-manifest.json with racing files, `docs/game-offline.zip` = 5,283,255 bytes — verified it contains racing files + fresh manifest). game/ and docs/ manifests verified byte-identical. Nothing committed. Next per the user: nothing until they approve Stage 2 (Task 96).

## Session log — 2026-08-07

- 2026-08-07 — Per user request, replaced the installed PWA icon artwork with the same Little Explorer kitty sprite used by the hub game button (`explorer_kitty/01_idle_right.png`), while keeping the download-button thumbnail unchanged. Regenerated valid 192×192 and 512×512 PNG icons. Manifest name/short name remain Serbian Cyrillic `Петрин свет`. Package rebuild and Android icon verification follow.
- 2026-08-07 — User-tested Task 94 on Android: the download button showed “Сервис није још активан”, and opening the extracted ZIP only opened `index.html` as a normal browser file. Root cause found: service-worker and cache-list URLs were hard-coded to `/game/...`, which fails under the GitHub Pages repository subpath. Fixed registration, manifest, service-worker scope/cache URLs, relative cache-list entries, cache version (`petrin-v2`), and caregiver ZIP instructions. Validation and a new offline package build are still required; do not commit/push without user approval.
- 2026-08-07 20:12:00 — Ensured standalone boot exports: added window.start* assignments in animals.js, shapes.js, candy.js, coloring.js so shared/main.js reliably calls per-page start functions. node --check OK; targeted headless re-runs recommended on user approval.
- 2026-08-07 — Confirmed Task 92 DONE: the user-provided `jao.mp3` is integrated through the shared Serbian speech map and Little Explorer hurt handling. Task 94 is IN PROGRESS; subtasks 1, 1.5, and 2 are complete. Subtask 2 now has required PNG manifest icons (`icon-192.png`, `icon-512.png`).
- 2026-08-07 — Task 94 subtask 3 started: auditing `game/sw.js` install/activate/fetch, cache-all progress, and manifest-hash update checks before marking it DONE.
- 2026-08-07 — Task 94 subtask 3 DONE: `game/sw.js` is restricted to same-origin `/game/` GET resources, supports install/activate/fetch plus `cacheAll` progress and hash-based update checks, and now reports removed manifest entries too. `game/sw-cache-list.json` regenerated with 193 runtime entries. Validation: `node --check game/sw.js`, `node --check game/shared/navigation.js`, and `node tools/hub_smoke.js` (`0 fail`).
- 2026-08-07 — Task 94 subtasks 4–7 DONE: added manifest link, worker-ready registration, one-tap cache/update UI, progress/ready/update states, update-check button, and `game-offline.zip` fallback link. Fixed `tools/build_offline.ps1` to generate `offline-manifest.json` before packaging so the ZIP contains the current hash manifest. Final package build remains to be run during rollout validation.
- 2026-08-07 — Task 94 documentation/safety work: added `game/docs/OFFLINE_INSTALL.md` with Serbian-Cyrillic caregiver install, update, ZIP fallback, and rollback instructions. Android Chrome/WebView manual testing is still environment-dependent; automated validation is next.
- 2026-08-07 — Task 94 automated validation: Kitty, Puzzle, Memory, Ocean, Space, and Hub smokes pass after updating two stale Ocean/Space standalone-map assertions to include the existing parent-hub field. The package ZIP was built once; final sync/rebuild is still required after the caregiver guide and smoke-tool edits.
- 2026-08-07 — Task 94 rollout completed locally: `tools/sync-docs.sh` mirrored 194 files, then `tools/build_offline.ps1` regenerated the 193-entry cache list, hash manifest, and `docs/game-offline.zip` (5,338,546 bytes). Archive inspection confirmed `index.html`, `manifest.json`, `sw.js`, both PNG icons, and `docs/OFFLINE_INSTALL.md`. Android Chrome/WebView manual confirmation remains a follow-up; no commit/push performed.

> Orientation rule (added 2026-08-07): Do not commit or push without explicit user approval. After any changes to game/, run `tools/sync-docs.sh` and remind the user to commit and push. This file is read at the start of every session.

- Task 74: Completed the full read-only project review. Fixed adventure music toggling and optional Web Audio initialization, made adventure/tracing/driving smoke probes deterministic, and corrected the Phase 5 roadmap plus current validation counts. Full smoke suite and syntax checks pass; `docs/` is synchronized. No commit or push performed.

- Task 91: Fixed Ocean/Space worlds-picker & music toggle (2026-08-07, Radman Milos)

Applied targeted, surgical fixes in `game/games/adventure.js` to make the worlds picker and music toggle deterministic under headless smoke tests and real devices:
- Worlds picker: each world button now stores `data-world-pos` when created and uses that stable index on click (prevents racey index mismatches after DOM rebuilds).
- Music toggle: the music button UI (`textContent` and `.off` class) is updated immediately when `AdventureMusic.setOn()` is called so tests and users see the state change right away.
- initAudio: removed an early use of a possibly-undefined variable to avoid thrown errors when Web Audio is blocked; audio init is now safer.

Validation: ran `node tools/ocean_smoke.js` and `node tools/space_smoke.js` sequentially — both passed fully (all checks green). PROJECT_TASKS.md updated with a DONE entry (Task 91). Docs updated and committed.

- Task 77: Added smoke tests for Animals, Shapes, Counting, Coloring (13/13, 11/11, 11/11, 13/13 PASS). `coloring.js` swatches now carry `data-color`.
- Task 78: Created `CONTRIBUTING.md` (setup, validation, docs mirror one-liner).
- Task 79: Split `adventure.js` into `adventure-music.js` + `adventure-modes.js` + core engine. All 4 adventure pages updated. All smokes green.
- Tasks 80/81/82: asset audit (nothing unused), stale `nul.mp3` doc references cleaned, aria-label audit (zero missing).
- Task 83: shared reduced-motion utility (`window.reducedMotion()` in `shared/utils.js`, loaded on the 5 canvas pages) + JS gates in adventure.js / kitty-standalone.js / candy.js / animal_counting.js; CSS baseline in accessibility.css. **FULLY REVERTED per user decision after play-test feedback** — the user's OS has `prefers-reduced-motion: reduce` ACTIVE (headless Chrome probe: `matchMedia(...reduce).matches === true`), so on their machine the gates stripped the memory card-flip animation, candy combo/hint/level-up animations, and obstacle-hit particles — gameplay-critical feedback for the child. All task-83 feature code removed (utils.js reverted to HEAD, script tags dropped from the 5 pages, JS gates removed, CSS block removed); aria-label work (task 82) untouched.
  - **Test-harness fixes kept (real harness flakes, NOT game bugs):** headless Chrome does NOT fire a native `resize` after `Emulation.setDeviceMetricsOverride` — adventure_smoke drive-resize and kitty_smoke resize checks now `sleep(300)` after the override and (kitty) manually dispatch `window.dispatchEvent(new Event('resize'))`. adventure_smoke drive-coin check made deterministic (reset `c.collected=false` + `coinCount=0`).
  - **Two PRE-EXISTING task-79 split regressions FOUND + FIXED while reverting (live on main since commit `5ed9978`):** (1) `adventure.js:1044` passed the `roadTopY` FUNCTION instead of `roadTopY()` into `AdventureModes.drawRoad` → `canvas.height - top` NaN → road/grass/dashed center line never painted (fix: pass `roadTopY()`; pixel probe confirms the [26,34] dash line renders). (2) `adventure.js:1119` restored the dropped `if (cfg.drawObstacle) { cfg.drawObstacle(ctx, o, theme); return; }` dispatch so ocean/space custom obstacle painters (ocean.js:596 / space.js:556) actually draw — pixel probe: ocean crab 84/84 + space asteroid 120/120 bbox pixels non-background (previously invisible). This is the "ocean/space obstacles invisible" bug the user reported.
  - **Pre-existing, NOT caused by this revert (verified against a git-stash HEAD baseline):** dino_smoke and space_smoke still fail the worlds-picker + music-toggle checks; ocean_smoke fails the same 3 (worlds picker open, jump-to-world, music toggle). Same class as the documented native-resize/music-timing flakes. Left for a later session.
- AGENTS.md updated: added rule to work in small batches + delegate to subagents when helpful; ask before each step.
- Task 87 (DONE): PROJECT_TASKS.md fully reformatted to `### NN. STATUS — Title (date, author)` headings, numeric order in both Active + History sections (verified by script: 0 malformed, 0 out of order).
- Task 88 (DONE): HANDOVER_PROMPT.md trimmed 268 → 167 lines (83KB → 52KB). All session logs before 2026-08-07 (dated 2026-08-03 through 2026-08-05) archived to `resources/handover-archive/handover-sessions-2026-08-03-to-2026-08-05.md`; only the most recent session log (2026-08-07) + the current-state reference sections remain.
- Task 89 (DONE): README.md folder-structure listings updated to match disk — added piano.html/space.html to pages/, kitty-standalone.js/kids_games.js/piano.js/adventure-music.js/adventure-modes.js/space.js to games/, celebration.js to shared/ (both the `## game/` example block and the `# Architecture` block, plus the standalone-pages inline list). Scripted re-check: README ↔ disk fully consistent, code-fence balance unchanged.
- Task 90 (DONE): Little Explorer character picker + new explorer kitty hero. The game now shows a character picker at every start (per user): **Маца Истраживачица (kitty, default)** or **Истраживачица (explorer girl)**. New kitty sprites copied from `resources/little explorer/little_explorer_kitty/explorer_kitty/` → `game/assets/images/explorer_kitty/` (same filenames as the old pack, distinct folder required). Sprite loader reworked from hard-coded dims (237×352, 23px gap) to a per-character `CHARACTERS` config with measured source dims (kitty 273×334 → srcH 312; explorer 237×352 → srcH 329). Boot is gated on the picker: `chooseCharacter(id)` sets the char, loads frames, then `loadWorld()/loop()`. **Death sounds now character-aware** (user addition): kitty plays `game/assets/audio/cat.ogg` via a lazy `Audio`, girl speaks "Јао!" via the shared speech API (now backed by `game/assets/audio/speech/jao.mp3`); `playHurtSound()` branches on `selectedCharacter`. Hub button (`index.html`) now shows the kitty's `01_idle_right.png`. Validated: `node tools/kitty_smoke.js` (30/30 PASS, incl. new task90 checks + 53x60 kitty frames) + `node tools/hub_smoke.js` (ALL PASS). Docs mirror verified synchronized.

- Task 92 (DONE): Little Explorer: add Serbian "Јао!" speech asset and integrate (2026-08-07, Radman Milos)

  - Added `game/assets/audio/speech/jao.mp3` (generated with `resources/tts_generate.js`).
  - Updated `game/shared/speech.js` to map 'Јао'/'Јао!' → `assets/audio/speech/jao.mp3`.
  - Updated `game/games/kitty-standalone.js` to use `window.speech.speak('Јао!')` so the pre-recorded MP3 plays on the girl's hurt event and added fallbacks.
  - Ran `node resources/tts_generate.js` (generated: 1), `node tools/kitty_smoke.js` (ALL PASS), and `tools/sync-docs.sh` to mirror `game/` → `docs/`.

"Јао!" via speechSynthesis `sr-RS`; `playHurtSound()` branches on `selectedCharacter`. Hub button (`index.html`) now shows the kitty's `01_idle_right.png`. Validated: `node tools/kitty_smoke.js` (30/30 PASS, incl. new task90 checks + 53x60 kitty frames) + `node tools/hub_smoke.js` (ALL PASS).

Primary files to read first

- `README.md` — (first) high-level project overview, vision, architecture, and roadmap
- `PROJECT_TASKS.md` — authoritative task list and statuses (NEW / IN PROGRESS / DONE)
- `AGENTS.md` — Ponytail Lazy Dev (YAGNI) persona rules — always active for this project
- `game/index.html` — hub and script load order
- `game/shared/*.js` — navigation, audio, speech, utils, accessibility
- `game/games/*.js` — individual game runtimes (animals, shapes, candy, kitty, puzzle, counting, memory, coloring)
- `tools/README.md` + `tools/headless.js` — shared headless-Chrome test harnesses for the games (tracing smoke/probe live here)

Activate skill

- Ponytail Lazy Dev — enable this project persona and follow its rules (surgical, YAGNI, minimal changes).

Kilo config

- `kilo.jsonc` at the project root sets the default model (Big Pickle), snapshot mode, compaction, and permissions.
- Commands live in `.kilo/command/` (invoked via `/name`): `/smoke`, `/check`, `/sync-docs`, `/review`.
- Agents live in `.kilo/agent/` (e.g. `ponytail.md` for the YAGNI agent).

Working rules (short)

- Be surgical: prefer the smallest correct change. Follow the Ponytail Lazy Dev (YAGNI) rules.
- Use `game/` for final runtime files. `resources/` is reference only. `tools/` holds dev/test tooling (never deployed, never required by `game/`).
- No external runtime dependencies or remote assets without explicit approval. Audio assets are acquired once and stored locally.
- Validate with the smallest targeted check (node --check for JS, `tools/tracing_smoke.js` for tracing changes, `tools/driving_smoke.js` for adventure-engine/Driving changes, `tools/ocean_smoke.js` for Ocean changes, `tools/dino_smoke.js` for Dino/ground-mode changes, `tools/space_smoke.js` for Space changes, `tools/memory_smoke.js` for Memory, `tools/candy_smoke.js` for Candy, `tools/puzzle_smoke.js` for Puzzle, `tools/kitty_smoke.js` for Paper Kitty changes, open HTML manually for visual checks).
- Update `PROJECT_TASKS.md` when starting/completing tasks and leave a short note.
- Never add ads, analytics, tracking, or monetization. Offline-first, tablet-first, toddler-friendly.

Current state (as of 2026-09-09)

- **ALL 5 racing stages (tasks 95–99) are DONE and validated.** Stage 1 committed `4e028b4`, Stages 3–4+5 committed locally (`eea68d8`, `ffa64db`, and the Stage-5 commit just made). **git push hangs on Git Credential Manager** — user pushes manually. `racing_smoke.js` 32/32 PASS, `hub_smoke.js` ALL PASS. Next (per user directive "finish the whole game, I will play-test at the end"): user's play-test round on the complete game, decisions list from this session to review, then polish feedback rounds.

- **Task 99 (Racing Game Stage 5: Polish & Accessibility, DONE 2026-09-09):** countdown 3-2-1-Крени!, engine hum, pickup sparkles, ARIA announcer, reduced-motion-safe, tablet thumb zones + landscape hint — see session log. `racing_smoke.js` 32/32 PASS + `hub_smoke.js` ALL PASS. Docs synced, offline pack regenerated.

- **Task 98 (Racing Game Stage 4: Roster & Progression, DONE 2026-09-09):** combo picker, unlocking, localStorage persistence — see session log. `racing_smoke.js` 31/31 PASS. Next: **Task 99 — Stage 5: Polish & Accessibility** (countdown, engine hum, pickup particles, ARIA live, reduced-motion safe, tablet touch zones, landscape hint), then full regression + offline pack regen + final commit/sync.

- **Task 97 (Racing Game Stage 3: Obstacles & Difficulty, DONE 2026-09-09):** obstacles implemented — check the session log above for the full detail. `racing_smoke.js` 23/23 PASS, `hub_smoke.js` ALL PASS.

- **Task 96 (Racing Game Stage 2, DONE 2026-09-09):** Multi-World & Visual Polish. **Stages 2.1–2.4 done** (road widen + curves + growing pickups; decor; 8 worlds; HUD — see session log); Stages 3–5 (tasks 97–99) now also DONE. Remaining: user play-test round (build-then-polish) → commit/sync (Stage 1 committed as `4e028b4`; Stages 3–5 committed locally, push pending — GCM hangs).

- **Task 95 (Racing Game Stage 1, DONE & COMMITTED as `4e028b4` 2026-09-08):** New pseudo-3D racing game "Мала тркачица" (Little Racer) shipped. Files: `game/pages/racing.html`, `game/games/racing.js`, `game/games/racing-config.js`, `tools/racing_smoke.js`. Segment-based road renderer, canvas-drawn car with left/right input + speed curve, pickup collection (🌸 flowers), finish line with win modal + celebrate, character picker (Kitty/Girl reusing explorer sprites), single world (Ливада) with music. Hub button added (🎮 ИГРЕ sub-hub, 🏎️). `sw-cache-list.json` regenerated (196 entries). Validation: `node --check` passes; `tools/racing_smoke.js` 15/15 PASS; `tools/hub_smoke.js` ALL PASS. Includes drawRoad rework (camera-fixed segments + scrolling dashes) user-reviewed as "ok is a bit better". Zero edits to existing game files. Awaiting user push.

- **Phase 5 REDEFINED (2026-09-08, user decision):** New Phase 5 = build a pseudo-3D racing game **"Мала тркачица" (Little Racer)** in 5 stages. Previous Phase 5 polish backlog moved to Phase 6. УЧЕЊЕ learning apps remain out of scope.

  **Racing Game Plan:** `RACING_GAME_PLAN.md` (full detail). Summary:
  - OutRun-style simulated 3D: road scales to horizon, car fixed bottom-center, left/right input only, auto-forward
  - Cute aesthetic matching Kitty Explorer (Fredoka, cream/plum/pastel)
  - Character picker: Kitty (explorer kitty) + Girl (explorer girl) — reuse existing sprites
  - Car selection: 3-4 cute vehicles per driver
  - Worlds: 6-8 themed (Meadow, Beach, Snow, Candy, Jungle, Space, Night, Farm) with unique pickups
  - Pickups: theme-specific collectibles along road (flowers, shells, snowflakes, candy, gems, stars, moons, carrots)
  - Obstacles (Stage 3): puddles, rocks, barricades — slowdown only, no fail state
  - Music: reuse adventure engine music system (32-step melody + 16-beat bass + ambient per world)
  - Max reuse: shared/navigation.js, audio.js, speech.js, utils.js, celebration.js, accessibility.css, Fredoka fonts, Kitty driver sprites, adventure music system, headless.js harness
  - Zero edits to existing game files; additive only

  **Stages (each a task, user approval per stage):**
  - Task 95 — Stage 1: Foundation & Core Loop (vertical slice: road, car, 1 world, pickups, finish, pickers, hub)
  - Task 96 — Stage 2: Multi-World & Visual Polish (6-8 worlds, procedural tracks, per-world music/pickups/decor)
  - Task 97 — Stage 3: Obstacles & Difficulty (puddles, rocks, barricades with forgiving slowdown)
  - Task 98 — Stage 4: Full Driver/Car Roster & Progression (all combos, localStorage persistence)
  - Task 99 — Stage 5: Polish & Accessibility (countdown, engine sound, particles, ARIA, reduced-motion, tablet-first)

- **Task 90 (Little Explorer character picker, DONE 2026-08-07):** `papper_kitty.html` + `kitty-standalone.js` now show a character picker modal at every launch (per user): **Маца Истраживачица** (new explorer kitty, default) or **Истраживачица** (old explorer girl). New kitty sprites shipped in `game/assets/images/explorer_kitty/` (6 frames, source 273×334, 22px bottom gap → srcH 312; old girl 237×352 → srcH 329); loader switched from hard-coded dims to a per-character `CHARACTERS` map; boot is gated on `chooseCharacter(id)`. **Death sounds are now character-aware** (user addition): kitty → `assets/audio/cat.ogg` (lazy `Audio`), girl → speechSynthesis "Јао!" (`sr-RS`, no shared/speech.js in this page); both go through `playHurtSound()`. Hub button in `index.html` now shows the kitty's `01_idle_right.png`. Validated: `node tools/kitty_smoke.js` (30/30 PASS incl. new task90 checks + 53×60 kitty frames) + `node tools/hub_smoke.js` ALL PASS. Docs mirror verified synchronized. Committed & pushed by Radman Milos on 2026-08-07; on-device checks completed.

- **Task 83 (shared reduced-motion utility) is REVERTED (2026-08-07, per user decision):** the original DONE implementation (added `window.reducedMotion()` to `game/shared/utils.js`, script tags on the 5 canvas pages dino/driving/ocean/space/papper_kitty, JS gates in adventure.js/kitty-standalone.js/candy.js/animal_counting.js, `@media (prefers-reduced-motion: reduce)` collapse in accessibility.css) was fully removed. **Why:** the user's OS reports `prefers-reduced-motion: reduce` = true (headless Chrome probe), so on their machine the gates stripped the memory card-flip animation, candy combo/hint/level-up animations, and obstacle-hit particles — all gameplay-critical feedback. All animations are restored; the a11y CSS block, JS gates, utils.js addition, and the 5 script tags are gone. **Two pre-existing task-79 split regressions FOUND + FIXED during the revert** (both live on `main` since commit `5ed9978`): (1) `adventure.js:1044` passed `roadTopY` (function) instead of `roadTopY()` into `AdventureModes.drawRoad` → NaN geometry → road + grass + dashed divider never painted ("driving has no dashed lines"); now passes `roadTopY()`, pixel probe confirms the [26,34] dash line renders. (2) `adventure.js:1119` restored `if (cfg.drawObstacle) { cfg.drawObstacle(ctx, o, theme); return; }` so ocean/space custom obstacle painters (shark/jelly/rock/mine/asteroid/etc., ocean.js:596 / space.js:556) draw again — pixel probe: ocean crab 84/84 + space asteroid 120/120 bbox pixels non-background ("ocean/space obstacles invisible"). **Validation:** node --check clean; adventure_smoke 25/25 PASS (exit 0); kitty_smoke full PASS (exit 0); candy_smoke + counting_smoke ALL PASSED; ocean_smoke/space_smoke failures (worlds picker, music toggle) verified pre-existing against a git-stash HEAD baseline. **Harness fixes kept:** adventure_smoke drive-resize settle sleep + deterministic drive-coin reset; kitty_smoke manual `resize` dispatch.

- **Мала истраживачица (task 73, DONE 2026-08-06, Ponytail Lazy Dev):** **full character rebuild — the 🐱 emoji hero is gone, replaced by the user's Little Explorer sprite pack** (`resources/little explorer/little_explorer_sprite_pack/`, doc `AI_PROMPT_sequence_explainer.md`) as a real animated 2D platformer hero. `game/pages/papper_kitty.html`: 6 sprite frames deployed to `game/assets/images/explorer/` (idle 01/04/06 + walk 02 + run 03 + jump 05; source is 237×352 with feet bottom-aligned at row 328 in all frames, so one offset works; each pre-rendered to a 35×48 offscreen canvas from source rect (0,0,237,329) — no getImageData, no file:// taint, unlike the dino crop); **new state machine** resolved each `update()` (airborne→jump, else |vx|<1.2 idle cycling 01→04→06 at ~366ms, <2.6 walk, else run) + **walk→run acceleration** (`vx ±= 0.55` toward ±5.2, friction ×0.8 — the run frame finally gets used, no new button) + landing squash (0.8) + grounded bob kept; **drawCostume + all 11 `costume:` world keys deleted** (sprite already wears pith helmet + goggles + satchel); **rename to Мала истраживачица** (hub icon 🐱→🧭, iframe title, page title, favicon) and **cat-meow → synth "thud"** `playHurtSound` (sine 180→70Hz, 0.22s; `playCatSound`/`kittyCatAudio`/cat.ogg removed). IDs untouched (`startKitty`/`game-kitty`/etc. — no id churn). **Validated: extracted script `node --check` clean; kitty smoke 29/29 PASS.** Two smoke-test bugs fixed along the way (test-only; game was correct): (1) the new state-machine check raced the shuffled world's walkers/pipes — test now zeroes `walkers`/`mice` and anchors the player at x=40 on ground 0; (2) the **long-documented pre-existing `resize` flake is now FIXED** — its cause was the check requiring the player to land on `platforms[0]` only, but in some shuffled worlds the spawn sits on a `dy:80` float (feet y=932), which IS solid ground though not the ground platform; the settle check now accepts "top of any platform". **Awaiting user commit/push + on-device visual check** (idle/walk/run/jump feel, feet-on-ground, mirror). Next Phase 5 task = **69 adventure tuning (needs user play-test feedback first).**
- **Мала истраживачица follow-up (task 73b, 2026-08-06, per user play-test feedback "no bobbing, make it larger"):** grounded bob REMOVED (`bobPhase` field/increment/draw-offset deleted; smoke static check now asserts NO `bobPhase`) and hero sprite ENLARGED 35×48 → **43×60** (`EXPLORER_SPRITE_H` 48→60; draw anchored feet-flush via `player.height/2 - EXPLORER_SPRITE_H`, so only the head overhangs the 48×48 hitbox ~12px upward — hitbox untouched, levels stay tuned). **All kitty-smoke flakiness root-caused & fixed (test-only; game was correct):** (a) the `mirror` check used a color-SUM signature that is position-independent — a fully-enclosed hero mirrors its colors to mirrored positions, so the sum is unchanged; the old region only passed because it captured a partial hero → now a position-aware pixel-diff (`> 200` changed bytes). (b) the `stomp` check dropped the player at `walkers[0].x - 10` — OUTSIDE the patrol `[x, x+range]`, so it only stomped when the walker chanced to be near minX at the wrong moment (world-shuffle) → now freezes `walkers[0].vx=0` and drops the player straight onto the walker. (c) the `side` check placed the player 30px left of the walker — possibly a pit (walkers sit at their ground's left edge) → now places the player exactly on the walker, deterministically triggering the side-hit branch (feet at `groundY` fail the stomp's `<= groundY-6`). Validated: extracted script `node --check` clean; **kitty smoke 29/29 PASS × 3 consecutive runs** (was previously flaky under serial runs). docs/ re-synced. Still awaiting user commit/push + on-device visual check.
- **Мала истраживачица follow-up 2 (task 73c, 2026-08-06, per user):** (1) **running now alternates the run (03) and walk (02) frames** — `currentExplorerFrame()`'s run branch returns `floor(animTime/10)%2 === 0 ? run : walk` (~167ms per stride) instead of a frozen run pose; new smoke check "running alternates between the run and walk frames", and the state-machine run assertion relaxed to "run state uses a run-or-walk frame" (frame selection is now owned by the runMix check). (2) **hub kitty button now shows the character** — `game/index.html` `<button class="hub-btn kitty">🧭</button>` → `<img src="assets/images/explorer/03_run_right.png" alt="Мала истраживачица">` with `.hub-btn img{width:78%;height:78%;object-fit:contain}`. Validated: `node --check` clean; **kitty smoke 30/30 PASS + hub smoke 8/8 PASS**. docs/ re-synced. Awaiting user commit/push + on-device look at the run animation and the new hub icon.
- **Play-test follow-ups (task 71, DONE 2026-08-06, Ponytail Lazy Dev):** two fixes in `game/` + one regression in `tools/kitty_smoke.js`, **awaiting user commit/push** (docs/ re-synced). **(1) Adventure bottom-cutoff on tablet/phone** — `game/shared/adventure.css` `#adv-game` now `height:100vh; height:100dvh;` (was bare `100vh`, which extends under the browser address bar on mobile so bottom controls + screen content fell below the viewport). Kitty was unaffected because it runs inside the hub iframe where `100vh` == the iframe's visible height. Layout probe verified at 1100×700 (`gameBottom==700==innerH`, `controlsTop==476`); dvh has no effect on headless smokes (headless viewport == dvh) or the hub iframe. If the user still sees cutoff on-device, next candidate is a `100svh`/`-webkit-fill-available` fallback. **(2) Kitty water world pits → spikes** — `game/pages/papper_kitty.html` `WORLDS[6]` (Подводни свет) `pitHazard: 'water' → 'spikes'` (spring/summer/japan underwater keep `water`; `drawPitHazards()` already drew `'spikes'`). Added kitty-smoke regression ("water world: pits are spikes"), kitty smoke is now **16 checks**. **Pre-existing flake (NOT caused by this change, reproduced at clean HEAD):** kitty_smoke's `resize` check ("ground re-anchored to the new canvas height, kitty lands on it") intermittently fails under serial runs (`onGround:false`, e.g. `y:932` vs expected `gy - player.height`).
- **Adventure bottom-cutoff follow-up (task 71b, FIXED 2026-08-06, Ponytail Lazy Dev):** user reported the task-71 `100dvh` fix STILL cuts the bottom on their tablet (e.g. left buttons show only the top half). Root cause: a browser without `dvh` support (older Android WebView / Samsung Internet) ignores `height:100dvh` and falls back to `100vh`, which extends under the address bar → bottom clipped; a CSS `100svh` fallback can't help because svh ships in the same engine generation as dvh. Fix: `game/games/adventure.js` (shared engine for all 4 adventure pages) now pins `#adv-game` height to `window.innerHeight` (the universally-correct visible-viewport height on every mobile browser) and re-syncs on `resize` + `orientationchange` + `visibilitychange` (defers ~100ms for address-bar settle). Inline style overrides the CSS; desktop unaffected (innerHeight == layout viewport). CSS `100vh/100dvh` remains the pre-JS baseline. Validated: `node --check` clean; **driving 17/17, ocean 20/20, dino 40/40, space 21/21 PASS** (a first-run driving coin FAIL was a pre-existing test-order flake — steering had already collected `coins[0]` — clean on re-run). docs/ re-synced. **Needs user on-device re-test on the tablet.**
- **Dino heroes = user PNGs (task 72, DONE 2026-08-06, Ponytail Lazy Dev):** all four hand-drawn vector dinos replaced with exactly 2 PNG sprites from `resources/dino/` (brontosaurus + tiranosaurus rex, both 2000x2000 transparent PNGs), copied into deployable `game/assets/images/dino/` (filename `tiranosaurus rex.png` sanitized to `tiranosaurus-rex.png`). `game/games/dino.js`: vector block deleted (`DINO_PALETTE`/`dinoEye`/`dinoQuadLegs`/`drawTRex`/`drawTriceratops`/`drawStego`/`drawDiplodocus`); new `DINO_SPRITES` + `loadDinoFrame(type)` decodes each PNG, crops the transparent padding via an alpha scan (step 2, alpha > 32), and pre-scales the content to the 110x100 hitbox with feet flush at the bottom (drawn at local y=+53 to offset the engine's `translate(0,-3)`). Picker = 2 options (Бронтосаурус + Тиранозаур), thumbnails mirrored (`scale(-1,1)` + `translate(55,47)`) to match the in-game look, `redrawPickerPreviews()` refills the canvases when the images finish loading. Config now `heroFlip: true` (pixel-mass analysis: top-35% mass centroid x=585/633 vs image centre ~1000 => both PNGs face LEFT natively, the engine mirrors them to face right). CSS: picker grid 4 -> 2 columns. `tools/dino_smoke.js` updated (2 picker buttons + new names, bronto/t_rex clicks, heroFlip assertion) plus a new check that both cropped frames land 100px tall / 60-110px wide (measured 94 and 95). Validated: `node --check` clean, **dino smoke 40/40 PASS**, ocean/driving/space green. **Awaiting user commit/push + on-device visual check** - facing direction is inferred (if a dino walks backward, flip `heroFlip`); also confirm feet-on-ground and size. **BUG FIXED 2026-08-06 ("dinos not visible at all"):** the pixel-crop in `loadDinoFrame` called `getImageData` on a canvas drawn from the PNG — under `file://` the canvas is tainted and that throws SecurityError, so `onload` aborted before `dinoFrames[type]` was set → invisible dino. Over HTTP it rendered fine (A/B pixel diff 4596px). Fix: `getImageData` wrapped in try/catch with a hardcoded `DINO_CROP_FALLBACK` (exact measured crops: bronto `{x:264,y:106,w:1529,h:1633}`, t_rex `{x:154,y:112,w:1691,h:1775}`) — only the readback is blocked under file://, drawing works. Re-verified headless: file:// now loads both frames, HTTP still renders; dino smoke 41/41. Supported way to play stays Live Server over HTTP (file:// also breaks audio + the kitty iframe).
- **Phase 5 is now GAME polish** (user decision 2026-08-05): focus = the 8 ИГРЕ games only (Paper Kitty, Driving, Ocean, Dino, Space, Candy, Memory, Puzzle); УЧЕЊЕ learning apps (Classroom, Tracing, Animals, Shapes, Counting, Coloring, Piano) are OUT of scope. Task 65 (Memory) is DONE: progress line "Парова: N од 8 · Потези: M", floating "Пар!" popup on matched pairs, moves counter, card back = 🃏 (game hub icon, per user). Validated by `tools/memory_smoke.js` (8 checks, ALL PASS). Task 66 (Candy) is DONE (incl. round-1 play-test feedback): level milestones + cute progress bar (⭐ riding the fill), 🎉 level-up overlay + fresh board, grid grows 4×4→8×8 (tiles shrink, grid always fits), rotating farm/wild animal sets, bar spaced off the grid, **star-spawn logic guaranteed at all levels** (build-time check guarded against the mid-move race; also runs after miss swaps), new 💡 hint button (highlights the 2 tiles + "Погледај жабе 🐸 😉" message). Validated by `tools/candy_smoke.js` (12 checks, ALL PASS). Task 67 (Puzzle) is DONE (per user requirements 2026-08-05): **8 painted scenes** (5 existing redone better + 3 new — Другари на фарми 🐄 / Другари у свемиру 🚀 / Другари на снегу ⛄), **NO text on any image** (painted title banner removed; title only in the page h1), **thin frame drawn on every picture** (white band + plum inner line — corner/edge reference for the child), and **ring placement**: split pieces are arranged AROUND the placement board (never covering it, no piece-piece overlap; greedy first-fit on ring bands + fine fallback), board resized to `min(46vw,420px,60vh)` so pieces physically fit around it at every screen size. Existing painters enhanced (park fountain/bench/butterfly, house curtains/path/fence, savanna watering hole/birds, forest extra tree/berries, sea submarine/coral/starfish). Validated by `tools/puzzle_smoke.js` (16 checks, ALL PASS — pixel-verified frame + no banner, ring checks at 2×2 AND 3×3 with 0 pieces on the board + 0 overlaps, keyboard-completed all 8 puzzles, scene order + grid alternation + final score 8). Remaining tasks: 68 Kitty (level pacing — needs user feedback first), 69 adventure games tuning (needs user feedback first). **User rule: before executing any of these tasks, ask the user what they would add/change first. Tasks 65+66+67 are implemented but NOT yet committed — ask the user before committing.**

- **Paper Kitty (task 68) is DONE (2026-08-05, Ponytail Lazy Dev):** scope changed from "level-pacing re-tune" to a **level-element redo** per user ("worlds are fine"): (1) **ground-walking enemies** — 4 per world, data-driven `walkers:[{x,range}]` patrols on the ground (vx 1.3, flip at patrol ends, gentle sine bob); jumping on one defeats it + gives a coin (particles + sound), touching its side plays the cat sound + respawns; **NEW dedicated themed sprites per world** (user rule: do NOT reuse pipe-pop art) — ladybug/bee/hedgehog/snowman/worm/spider/crab/meerkat/panda/seal/scarab via `drawWalker(type)` with per-type `WALKER_FOOT` feet offsets; all pixel-measured ≤ 48×48 (≤ kitty size). (2) **Water world finish line = submarine** — new `drawGoal('submarine')` branch. (3) **Gentle kitty bob** — `player.bobPhase` → `Math.sin(bobPhase)*2.5` translate while grounded. Walkers freeze after the level is won. Validated: `node --check` clean + new canonical `tools/kitty_smoke.js` **14/14 PASS** (boot/HUD, 11 worlds × 4 walkers + valid types, patrol geometry clear of pipes/stairs/goal/floats/moves/peers, submarine goal, static wiring, patrol inside [minX,maxX], jump-kill +1 coin, side-hit respawn no coin, freeze after win, 11 sprites ≤48×48 via real-canvas pixel readback, all 11 worlds load/update/draw). **Smoke gotchas:** `drawWalker` draws on the page's global `ctx` — sprite-size probes must clearRect/read back from the REAL canvas (an offscreen canvas reads empty); walker patrols that crossed a pipe made the fall-kill test land the kitty on the pipe (fixed the data: spring/summer/savanna/northpole/egypt walker0 moved past their first pipe). Next Phase 5 task = **69 adventure tuning (needs user play-test feedback first).**
- **Adventure polish round (task 70, DONE 2026-08-05, per user play-test feedback) is implemented but NOT yet committed:** (1) **fly-mode spawn clear of the bottom D-pads** (ocean/space) — `x = Math.max(280, canvas.width*0.3)`, `y = canvas.height*0.4`; (2) **full-vertical fly-mode spread** — new engine var `flyDyScale` = `(canvas.height*0.88)/maxDy` (per-world obstacles/coins/goal now span ~450–660px instead of a flat band; `dyScale` stays drive-only); (3) **themed pit hazards** — `pitHazard` per world: `lava` (volcano/cave/underground/cave2) with animated bubbles, `water` (lake/swamp/island/spring/summer/underwater/japan) with waves + 🐟, `spikes` (jungle/valley/ice/desert/night/autumn/winter/savanna/northpole/egypt) as dark spikes; drawn by `drawPitHazards(t)` (adventure.js) + `drawPitHazards()` (kitty) before platforms; (4) **space vortex stargate finish** — `drawSpaceGoal` now draws pylons + a three-arm swirling vortex (rotating `ellipse`s) + event-horizon core with inward spiral sparkles + „ЦИЉ" pill; (5) **dino ground always extends under the finish line** — each of the 10 dino last-grounds extended +260px past its `goalX` (regression check: no `lastRight < goalX+220`); (6) **root-cause "drop through the ground and respawn" fix (dino + kitty)** — `resizeCanvas` re-anchors world geometry bottom-up on resize (`dy = canvas.height - oldH`; grounds, mice/walkers y-shifts in kitty, non-ground obstacle/coin/goal y-shifts in adventure) so the ground stays at `canvas.height - 140`; plus **swept-landing** — platform checks now use `previousY + player.height <= p.y + 24` (with `player.vy >= 0`) instead of the narrow vertical band, so fast falls can't tunnel through the ground on tall canvases. Kitty also got a hoist-safe `worldLoaded` flag (resize ran before `theme`/`groundY` were set). Validated: `node --check` + all five smokes green — **dino 38/38** (incl. pits ≥2 fallable ≥150px + one ≥180px + no >240px gap per world, last-ground ≥ `goalX+220`, resize re-anchor to `gy = height-140` + landing, chosen-hero+behind-decor draw), **kitty 15/15**, **driving 17/17**, **ocean 20/20**, **space 21/21** (incl. vortex-stargate draw). Coin pickups in ocean/space/driving smokes now compare `before` vs `after` (coinCount is a session total — absolute `1` was flaky). Leftover: `C:\Users\RADMAN~1\AppData\Local\Temp\opencode\fly_spread_probe.js` debug probe — delete.
- Tasks 1–34, 38–52, **53 (Писање Tracing game)**, **54 (tools/ tooling)**, **43 (alphabet pronunciation/image audit)**, **55 (Phase 3: Учионица kids tier)**, **57 (GitHub Pages via `docs/` mirror)**, **58/59 (Клавир Piano game)**, **60 (shared adventure engine + Возила Driving game)**, **61 (Океан Ocean game)**, **62 (Дино Dino game)**, and **63 (Свемир Space game)** are DONE. **Task 56 (GitHub Actions auto-deploy) is ABANDONED** (2026-08-04, per user decision — Pages refused to deploy from `game/`; the workflow was removed). Tasks 35/36 (stickers + screen transitions) are **deferred to a later phase**. **Phase 4 is COMPLETE (2026-08-05) — all four adventure games shipped (Возила/Океан/Дино/Свемир). The user is play-testing Свемир (task 63) now; wait for feedback before any follow-up (user rule: stop after each game for feedback).**
- **Клавир (Piano, tasks 58/59):** standalone `game/pages/piano.html` + `game/games/piano.js`. 8-key one-octave keyboard (C4–C5, rainbow color-coded), Web Audio `tone()` (no new audio assets). Free play (tap → note + glow) and "Свирај песму" — follow-the-melody mode with a **3-song chip picker** (`#songChips`, **emoji icons ⭐/🎂/🔔** with the title as `aria-label`): **Трепери, трепери звездице** (42 notes), **Срећан ти рођендан** (25), **Џингл белс** (26) — all transposed/simplified to fit the octave; keys light up one at a time playing their tone, child taps the same key to advance (wrong = shake + "Покушај још једном!", no punishment; counter adapts to song length; finish → celebrate 🎹 + "Свирао си песму!" panel + Играј поново; 🔊 Чуј песму preview plays the chosen melody — stopping it re-lights the current guided note). Hub button 🎹 is **3rd** (after Учионица/Писање, per user request). Canonical validation: `node tools/piano_smoke.js` (14/14 PASS).
- **Возила (Driving, task 60, DONE 2026-08-04):** new **shared adventure engine** `game/games/adventure.js` (~1470 lines) — config-driven port of the Paper Kitty runtime, reusable by all four Phase 4 games. Modes: `ground` (kitty physics), `drive` (road auto-scrolls via `cfg.driveSpeed`, hero steers/clamped), `fly` (free 2D + momentum). Config-driven: 10+ worlds each with `hero`, `speed`, `driveSpeed`, obstacles (7–8, forgiving collision), 13 coins, `goalX` (5800–6650), per-world `decor` (city/palms/gulls/stars/leaves/snow), `bg` gradient, and a unique `music` theme (32-step melody + 16-beat bass + ambient layer: horn/cricket/bird/wind/rustle/rumble/owl/waves/desert/bell). Shared HUD ids `#adv-*` (canvas, score, coin-count, level, world-name, worlds/music buttons + modals), shared page chrome `.adv-back` + `#adv-title` in `game/shared/adventure.css`. Exposes `window.AdventureEngine.create` + `window.__adv` debug handle (player/coins/obstacles/goal/theme/music/levelCompleted/lastHitAt/bumpCount) for headless tests. Game = thin page + config: `game/pages/driving.html` (title "Возила", back btn `#driving-back`) + `game/games/driving.js` (hero 🚗, 10 road worlds: Градски трг ⭐, Поље сунцокрета 🍭, Јесења шума 🍂, Зимски пут ⛄, Планински пут 🎿, Ноћни град 🌙, Пустињска магистрала 🌵, Тропско острво 🌴, Морска обала 🐚, Космичка стаза 🪐). Wired: hub 🚗 button (`data-go="game-driving"`, after kitty button), `navigation.js` route, `main.js` standalone boot `'driving': ['driving-back','startDriving']`. **Engine bugs fixed during smoke:** obstacle-collision check had been OUTSIDE the `obstacles.forEach` loop (ReferenceError every frame) — moved inside; drive `respawnPlayer()` left `player.x` stale for a frame (teleport) — now sets `player.x = cameraX + player.offsetX` immediately; drive `player.y` clamp now `canvas.height*0.24 … -10`. Canonical validation: `node tools/driving_smoke.js` (17/17 PASS).
- **Океан (Ocean, task 61, DONE 2026-08-05):** second Phase 4 adventure game on the shared engine — **fly mode** (free 2D, no gravity). `game/pages/ocean.html` (title "Океан", back btn `#ocean-back`, **4-way fly D-pad** `#adv-controls.adv-fly-controls` — new CSS in adventure.css) + `game/games/ocean.js` (hero 🐟, `heroFontSize` 84). 10 underwater worlds: Корални гребен, Лагуна, Морске траве, Каменита обала, Потопљени брод, Морска пећина, Ледени океан, Морски ров, Пиратско благо, Ноћни океан — 8 obstacles + 13 coins + goal (🏁 flag-arch banner, „ЦИЉ!" on a woven-ribbon cloth) each. 9 obstacle types: patrolling sharks (**vx/minX/maxX** — bounce flips vx when `x+width` crosses the bound), jellyfish, rocks, mines, seaweed, pufferfish, crab, anchor, default rock. 10 music themes (32-step melody + 16-beat bass + root + ambient) + 10 decor types (bubbles/corals/starfish/shells/rocks/kelp/fish schools/cave lights/pyramid/glints). **Engine hooks added to adventure.js:** `cfg.drawObstacle(ctx, o, theme)`, `cfg.drawDecor(ctx, t, cameraX, canvas, theme)` (both return early when defined), `cfg.heroFontSize` (default 48), `cfg.heroFlip` (inverts the facing-right scale — needed because the 🐟 emoji natively faces LEFT), `cfg.heroBob` (slow sine vertical bob on the hero draw). Wired: hub 🐠 button (`data-go="game-ocean"`), `navigation.js` route, `main.js` standalone boot `'ocean': ['ocean-back','startOcean']`. Canonical validation: `node tools/ocean_smoke.js` (20/20 PASS). **Polish round (2026-08-05, per user feedback):** fish faces its movement direction (`heroFlip: true`), fish bobs while swimming (`heroBob: 5`), and the fly D-pad is tablet-friendly — ◀▶ in a horizontal cluster on the LEFT edge, ▲▼ in a vertical cluster on the RIGHT edge (`adv-pad-horizontal`/`adv-pad-vertical`; HTML button order is now `left,right,up,down`). **Smoke gotchas:** `window.__adv` `cameraX`/`lastHitAt` are getter-only (assignments silently no-op — reset state via `a.loadWorld()`, not assignment); obstacle-hit checks must wait ≥1100 ms after `loadWorld()` for the `lastHitAt` cooldown; assert patrol bounce on the `vx` flip, not on `x2 < x1`.
- **Дино (Dino, task 62, DONE 2026-08-05):** third Phase 4 adventure game on the shared engine — **ground mode** (jump/gravity/platforms, kitty physics). `game/pages/dino.html` (title "Дино", back btn `#dino-back`, **ground controls** `#adv-controls.adv-ground-controls` — ◀▶ D-pad cluster on the LEFT edge + one big ⬆ jump button (`data-adv="jump"`) on the RIGHT edge; new CSS in adventure.css) + `game/games/dino.js` (hero 🦕, `heroFontSize` 78, `heroW` 110, `heroH` 100, `heroFlip: true` — 🦕 faces LEFT natively; `speed` 5.2, `jumpPower` −13.5). 10 prehistoric worlds: Прашума, Тропска долина, Језеро, Вулкан, Пећина, Мочвара, Ледено доба, Пустиња, Ноћни свет, Острво диносауруса — solid `grounds`, floating `floats` (dy above ground), moving `moves` (shuttle between minX/maxX), auto step-up `stairs` (`stairColor`), and `pipes` (h) that pop out a mini-raptor every 3s; ~15 coins + ЦИЉ goal gate each. 10 music themes (32-step melody + 16-beat bass + root + ambient) + 10 decor types (jungle/valley/lake/volcano/cave/swamp/ice/desert/night/island). **Engine hooks used:** `cfg.drawEnemy` (mini-raptor), `cfg.drawDecor`, `cfg.drawGoal` (stone temple arch + ЦИЉ banner). **Engine bug found+fixed while shipping (dino is the FIRST ground-mode game on this engine):** ground-mode `loadWorld` called `w.floats.map`/`w.moves.map`/`w.pipes.map` unguarded — any world missing those keys threw and left `goal` null (update then crashed on `goal.x`); all three now default to `(w.x || [])`. Also added `get mice()` to the `window.__adv` debug handle (for the enemy smoke checks). Wired: hub 🦕 button (`data-go="game-dino"`), `navigation.js` route, `main.js` standalone boot `'dino': ['dino-back','startDino']`. Canonical validation: `node tools/dino_smoke.js` (25/25 PASS).
- **Свемир (Space, task 63, DONE 2026-08-05):** fourth and FINAL Phase 4 adventure game on the shared engine — **fly mode** (free 2D, no gravity). `game/pages/space.html` (title "Свемир", back btn `#space-back`, **reuses the existing fly controls** `#adv-controls.adv-fly-controls` — ◀▶ horizontal pad LEFT, ▲▼ vertical pad RIGHT; NO new CSS) + `game/games/space.js` (hero 🚀, `heroFontSize` 84, `heroW` 110, `heroH` 100, `heroBob: 4` — NO `heroFlip`: 🚀 faces RIGHT natively, unlike 🐟/🦕; `offsetStartRatio` 0.16, `speed` 4.5, `obstacleScale` 1.15, `pickupFontSize` 58). 10 space worlds: Звездано небо, Месечева стаза, Црвена планета, Астероидни појас, Сатурнови прстенови, Јупитеров вихор, Ледени месец, Свемирска станица, Галаксија, Дубоки свемир — 8–10 obstacles + 13 coins + goal (`drawSpaceGoal`: two pylons + glowing portal + „ЦИЉ" banner) each. 8 obstacle types: meteors, asteroids, UFOs (**patrol via vx/minX/maxX**, same bounce as ocean sharks), comets, ring bands, planets, satellites, black holes — plus an alien cameo (`cfg.drawObstacle` dispatch). 10 music themes (32-step melody + 16-beat bass + root + ambient: stars/moon/mars/belt/rings/giant/ice/station/galaxy/deep) + 10 decor types (stars/moon/mars/belt/rings/giant/ice/station/galaxy/deep — procedural starfields, parallax cratered planets, shooting stars, Mars dunes, Jupiter bands, ice crystals, space stations, galaxy clouds, black-hole swirls). **No new engine work — all hooks pre-exist from tasks 60–62** (`drawObstacle`, `drawDecor`, `drawGoal`, `heroFlip`, `heroBob`, `obstacleScale`, `speed`). Wired: hub 🚀 button (`data-go="game-space"`, after dino), `navigation.js` route, `main.js` standalone boot `'space': ['space-back','startSpace']`. Canonical validation: `node tools/space_smoke.js` (18/18 PASS); all four adventure smokes re-run green.
- **Писање (Tracing, task 53):** standalone `game/pages/tracing.html` + `game/games/tracing.js`. FREE DRAW on a dashed guide (user feedback #2 on 2026-08-03 — connect-the-dots was not intuitive). The `#tracingGuide` canvas draws a faint dashed outline of the target (letters/numbers in `GUIDE_FONT='900 288px Fredoka…'`, shapes via `drawShapeOutline`), refreshed on every `loadItem`, never counted as ink; a compact ref card sits above. Matching is metrics-based (48×48 grid): pass = `coverage ≥ 0.45` && `near ≥ 0.8` (fraction of ink within 2 cells of the guide — decisive anti-blob/anti-scribble rule) && `ink ≤ 2000`. Correct → celebrate + speech then auto-advance after 2600 ms; wrong → "Покушај још једном!" nudge, canvas kept for redraw. Облици is now only the 4 flat shapes (3D wireframes dropped per user decision). Canonical validation: `node tools/tracing_smoke.js` (22/22 PASS). Screenshots in `resources/tracing/*.png` are STALE (pre-redesign; visual review with MiMo V2.5 Free pending re-capture).
- **Task 47 (visual audit) completed:** captured 19 screenshots, reviewed all, found 7 items (0 blockers, 0 major, 3 minor, 4 cosmetic). 5 fixes implemented: memory button emoji 🧠→🃏, portrait grid gap reduction, kitty back button backdrop, coloring palette 4+4+3 grid, classroom title shifted right. Finding #1 (orphaned hub button) deferred; #3 (letter Ј font) accepted.
- **Учионица (Classroom, tasks 39/41/42/44/43):** standalone game `game/pages/classroom.html` + `game/games/classroom.js` — 4 learn-and-repeat activities: Азбука (30 letters; since task 43 the 25 consonants speak their letter sound, vowels their name, plus a word + emoji), Бројеви (0–10, sentences like "Пет слонова"), Облици (Круг/Квадрат/Троугао/Звезда + 3D SVG shapes Лопта/Коцка/Квадар/Ваљак/Купа/Пирамида), Боје (11 colors). Autoplay ▶/⏸ walks tiles at speech-end + 1.5 s pause; no celebrate icon. Polish: "Учионица" title shows only on the hub (not inside an activity); grids no longer resize on tap (no scale animations, `scrollbar-gutter:stable`, fixed-height display); alphabet/number/shape tiles use a cycling 8-color pastel background.
- **Memory (tasks 33/46):** flip whoosh sound + paw-print card back; animal name + sound now play **only on matched pairs** (not on every flip, not on mismatches).
- **Paper Kitty Adventure (task 49, DONE, self-contained `game/pages/papper_kitty.html`):** 11 worlds (Spring, Summer, Autumn, Winter, Underground, Cave, Water, Savanna, Japan, North Pole, Egypt), 1 level each, shuffled order per run + 🌍 world picker, theme-matched goal buildings/enemies/collectible emojis/costumes, stairs with auto step-up, rocky ceiling in Underground/Cave, longer/varied levels (goalX 5150–6850, 4 layout patterns), collectibles drawn as BARE opaque emoji (no backing circle/disc, no alpha/glow — user reported transparency, then asked for the cream sticker circle removed; 2026-08-03). **Procedural background music** (no audio files, Web Audio only): `MUSIC` config = 11 themes with 32-step melody + 16-beat bass + per-world **ambient layer** (bird/cricket/rustle/wind/rumble/drip/bubble/flute/bell/coo/desert), slow tempos 58–100bpm, lookahead scheduler (`musicTick`: 120ms interval, 0.35s horizon, `musicStepTime`/`musicStep` reset on swap). `loadWorld()` calls `startMusic(w.music)`; theme swaps instantly. **Music on/off button** 🔊/🔇 = `#music-btn` at `right:268px` (LEFT of `#worlds-btn` at `right:200px` — do not move closer or they overlap); persists `musicOn` in localStorage key `pkMusic`; music starts on first user gesture via `initAudio()` (which restarts the current theme). The per-world `music:` keys must stay in sync with `MUSIC` keys.
- **Hub (task 64, 2026-08-05):** `game/index.html` is now a **two-level landing** — the title "🌈 Петрин свет" sits prominently over two big group tiles: 🧠-replaced **УЧЕЊЕ first** (icon = 2×2 emoji combo 🏫📝/🎹🎨 — four `<span>` cells in a CSS grid; user rejected the 🧠 brain as not kid-friendly and picked the combo over a chalkboard SVG, then swapped 🎓→🎹 and ✏️→📝 and switched their places) then 🎮 **ИГРЕ**, each opening a sub-hub screen (`#hub-games` / `#hub-learning`) with the group's round buttons and a back arrow to the landing. User-confirmed split: ИГРЕ = Kitty, Driving, Ocean, Dino, Space, Candy, Memory, Puzzle (8); УЧЕЊЕ = Classroom, Tracing, Animals, Shapes, Counting, Coloring, Piano (7). Kitty's in-app back button targets `hub-games`. Sub-hub screens are `justify-content:flex-start` with the grid at `margin: 24vh` (clears title + back button, scrolls cleanly on short landscape). Canonical check: `node tools/hub_smoke.js` (8/8 PASS). Screenshots: `resources/hub-redesign/*.png` (visual review pending with MiMo V2.5 Free).
- **App name: "Petrin svet" (Петрин свет).** All child-facing text in every game is in Serbian Cyrillic; speech synthesis is Serbian (`speech.js` uses `lang='sr-RS'`). Permanent rule in `AGENTS.md`.
- Games shipped: Animals, Shape Match, Match Game (candy), Paper Kitty Adventure (embedded in-app), Animal Scene Puzzle, Animal Counting, Animal Memory, Coloring, **Учионица (Classroom)**, **Писање (Tracing)**, **Клавир (Piano)**, **Возила (Driving)**, **Океан (Ocean)**, **Дино (Dino)**, **Свемир (Space)**.
- Every game page is a standalone HTML that loads only the shared modules it needs, plus its own `games/<name>.js`. Kitty is the one in-app screen (iframe) launched from the hub.
- Navigation is driven by `shared/navigation.js` `window.goTo(id)`; every `[data-go]` button routes there. Standalone pages' back buttons redirect to the hub (`../index.html` from `pages/`), wired in `shared/main.js`.
- Audio is centralized in `shared/audio.js` (single AudioContext via `ctx()`; helpers: `tone`, `popSound`, `successChime`, `gentleMiss`, `sweep`, `playAnimalSound`).
- Celebration: `shared/celebration.js` defines `window.celebrate(emoji)` — self-contained full-screen overlay (injects its own style), plays `successChime`, speaks "Браво!" (MP3 asset). Used by shapes (round), coloring (scene, keeps animal sound), puzzle (complete, keeps Next), memory (win), counting (final "Готово!" — with two confetti waves; per-answer keeps its inline 🎉 + a full-screen `confetti()` burst, added 2026-08-02). Old per-game `.celebrate` overlays/CSS were removed.
- Speech (`shared/speech.js`) plays pre-generated Serbian MP3 assets (`assets/audio/speech/*.mp3`, **108 files**: 12 animals incl. Кока, 4 flat shapes + 6 3D shapes, numbers 0–10, "Браво!", 30 letter names, 23 alphabet words, 11 colors, 10 number sentences) keyed by the exact Cyrillic word, with OS speechSynthesis as fallback for unknown text. The dev machine has NO Serbian TTS voice, so assets are the only audible path here. `speak()` is called by coloring ("Обој …" — no asset yet, falls back silent here), animals (card tap), shapes (correct placement), counting (number word on correct answer, up to 10), memory (animal name on matched pair only), classroom (letter/word/number/sentence/shape/color on tile tap + autoplay). New assets are generated with `resources/tts_generate.js` (Google Translate TTS tl=sr; idempotent, skips existing files).

Audio / animal sounds (verified complete)

- `playAnimalSound(name)` maps each animal to a local file in `game/assets/audio/`:
  Dog→dog.ogg, Cat→cat.ogg, Cow→cow.ogg, Lion→lion.ogg, Elephant→elephant.ogg,
  Frog→frog.oga, Pig→pig-grunt.ogg, Duck→duck.ogg, Fox→fox.mp3, Sheep→sheep.ogg, Horse→horse.ogg, Chicken→chicken.ogg.
- All 12 animals used across the games (animals.js, candy.js, animal_counting.js, animal_memory.js) resolve to valid, real local audio files — verified by header check.
- Fox sound is a real field recording (Pixabay "fox calling", l3hrja) at `assets/audio/fox.mp3`.
  Sheep (`sheep.ogg`) and Horse (`horse.ogg`) are CC0 recordings from BigSoundBank.com (Joseph SARDIN). No attribution required.
  Chicken (`chicken.ogg`) is Mixkit "Chickens clucking short" (id 1772, free license), trimmed to 2.15s, mono 44.1kHz OGG 25KB.
- `playFoxSynth()` and `playChickenSynth()` remain in audio.js only as fallbacks if a file ever fails to load.
- `playAnimalSound` auto-stops playback after a per-animal max duration (see `maxDuration` map in audio.js).

Folder structure

```
game/
  index.html
  pages/     animals.html shapes.html matching_game.html animal_puzzle.html animal_counting.html animal_memory.html coloring.html classroom.html tracing.html papper_kitty.html driving.html ocean.html dino.html space.html
  games/     animals.js shapes.js candy.js kitty.js animal_puzzle.js animal_counting.js animal_memory.js coloring.js classroom.js tracing.js adventure.js driving.js ocean.js dino.js space.js
  shared/    navigation.js audio.js speech.js utils.js main.js accessibility.css adventure.css
  assets/    fonts/ audio/ images/
tools/
  headless.js  tracing_smoke.js  tracing_probe.js  dilate_test.js  piano_smoke.js  driving_smoke.js  ocean_smoke.js  dino_smoke.js  space_smoke.js  kitty_smoke.js  kids_smoke.js  hub_smoke.js  memory_smoke.js  candy_smoke.js  puzzle_smoke.js  adventure_smoke.js  README.md
resources/
  dev-only assets (tts_generate.js, visual_audit_capture.js, screenshots, audit reports)
```

Notes:
- Only `index.html` lives at the `game/` root. All game pages live in `game/pages/`; the hub loads scripts from `game/shared/` and `game/games/`.
- `tools/` = headless test harnesses (see `tools/README.md`); never deployed, never required at runtime. Start every headless run through `tools/headless.js` (unique temp Chrome profile per run) so stale profile locks can't crash later runs.
- `shared/audio.js` roots animal-sound asset paths relative to the document URL (detects `/pages/` in the URL) so sounds work both from the hub and from pages/.
- `game/assets/audio/` holds exactly the 12 animal sounds used by `playAnimalSound` (the legacy duck.ogv, dog-google.ogg, pig.ogg, animal-growl.ogg, animal-squealing.ogg were removed on 2026-08-02).

Coloring game notes

- Scenes live in `game/games/coloring.js` (`coloringScenes`, 12 animals, 134 regions). Each region has `tag`, `color` (a palette hex), and either `attrs: {…}` or the SVG attributes flat on the region object. `createColoringRegion` accepts both forms (`r.attrs || r`, skipping `tag`/`color`), so both conventions are fine — do not assume `attrs` is always present.
- **Revamp (2026-08-03, task 51):** all 12 scenes were redrawn for recognizability (see task 51 note) and the canvas was enlarged in `pages/coloring.html` — ref thumbnail `min(32vmin,24vh)`, main SVG `min(56vmin,48vh)`. New screenshots of every scene are in `resources/coloring-redraw/*.png` (review with MiMo V2.5 Free).
- Layout: `.coloring-wrap` is a flex column; `.coloring-stage` contains name+progress text and a `.coloring-stage-row` (ref thumbnail + main SVG side by side); the palette is pinned at the bottom with `flex-shrink:0`. The wrapper uses `overflow:auto` so content scrolls if it overflows.
- A ➡ next-skip button (top-right, wired in `startColoring()`) advances to the next animal without waiting for the auto-advance.
- Palette is a 6×2 CSS grid (`repeat(6,9vmin)`, 11 swatches + 1 empty cell).
- The success handler calls `playAnimalSound(sceneName)` + `successChime()` and auto-advances after 1500 ms; scene names must match the keys in `shared/audio.js` `animalSoundFiles`.
- Scene-integrity check (colors ∈ palette, region shape valid) was verified; re-run after editing scenes.

Tracing game notes (task 53 — free-draw redesign, 2026-08-03)

- Game lives in `game/pages/tracing.html` + `game/games/tracing.js`. Hub (Слова 🔤 / Бројеви 🔢 / Облици 🔷) shows first on open.
- **Free draw on a dashed guide, not dots.** `#tracingGuide` canvas (400×400, absolute `inset:0`, `pointer-events:none`, no border-radius from the parent) holds the dashed guide — `drawGuide()`: `rgba(155,109,255,0.35)`, lineWidth 12, `setLineDash([16,14])`, `GUIDE_FONT='900 288px Fredoka…'` for letters/numbers (companion `item.shape` check → `drawShapeOutline(g, item.shape, CW)` for shapes). `loadItem()` regenerates the guide AND clears the ink canvas. The guide is never ink.
- Layout: `#tracingRef` (compact ref card) `min(18vmin,14vh)`; `#tracingBoard` wrapper `min(50vmin,34vh)`, `aspect-ratio:1/1`, holds canvas + guide; caption `4.6vmin` below; activity container `overflow-y:auto` — no overlap.
- Matching (`matchResult`) on a 48×48 grid: `refGrid()` = ink of the guide canvas, `inkGrid()` = user ink. Pass = `coverage ≥ MIN_COVER(0.45)` && `near ≥ MIN_NEAR(0.8)` && `ink ≤ MAX_INK(2000)`; `MIN_INK=40` filters accidental taps. `near` = fraction of ink cells within `dilate(ref,2)` (integral-image dilation). Measured (tools/tracing_probe.js): good traces near 0.98–1.0, blobs 0.69, scribbles 0.62, single line 0.7 — big margin at 0.8. Do NOT raise thresholds without re-running the probe.
- Feedback: correct → `window.celebrate('✏️')` + speak name (+ word for letters) then auto-advance after 2600 ms (`glyphDone()` → `loadItem(index+1)`, which clears ink + redraws guide); wrong → "Покушај још једном!" nudge, canvas kept so the child draws over.
- Облици = only the 4 flat shapes (Круг/Квадрат/Троугао/Звезда); 3D wireframes dropped per user decision.
- `window.__traceDebug` (`matchResult`/`refGrid`/`inkGrid`) is used by `tools/tracing_probe.js` — keep it.
- Data: `LETTERS` (30, `label` capital / `name` speech key / `word` + `emoji`), `NUMBERS` (0–10), `SHAPES` (4). Caption "Нацртај А", counter "N од 30/11/4". ➡ `#tracingNext` advances anytime; ↩ `#tracingBack` → hub.
- Wiring: `navigation.js` maps `game-tracing` → `pages/tracing.html`; `main.js` standalone boot `'tracing': ['tracing-back','startTracing']`; hub ✏️ button in `index.html` (`data-go="game-tracing"`).
- All names/words reuse existing `speech.js` MP3 keys (no new audio). **Validation: `node tools/tracing_smoke.js` (22 checks) — expect ALL PASS.** Screenshots in `resources/tracing/*.png` are STALE (pre-redesign); re-capture before the MiMo V2.5 Free visual pass.

Validation checklist (run after edits)

- Syntax check modified/added JS files: `node --check game/shared/*.js game/games/*.js`
- Tracing changes → run the canonical smoke: `node tools/tracing_smoke.js` (expect ALL PASS). Metric/threshold tuning → `node tools/tracing_probe.js`.
- Hub (`index.html`) changes → run the canonical smoke: `node tools/hub_smoke.js` (expect ALL PASS).
- Adventure engine / Driving / Ocean / Dino / Space changes → run the canonical smoke: `node tools/adventure_smoke.js` (expect ALL PASS).
- New headless checks → build on `tools/headless.js` (see `tools/README.md`); never write one-off temp probes.
- Open `game/index.html` in a browser to validate navigation and visual parity.
- For `papper_kitty.html`, extract inline JS and run node --check on the extracted code.
- For `papper_kitty.html` music: after touching `MUSIC` or the engine, re-run the Node stubbed-WebAudio harness (30 checks; script was staged at `%TEMP%/opencode/pk_music_test.js` reading extracted JS from `pk_check.js` — recreate by extracting the inline `<script>` and stubbing `AudioContext`/`localStorage`/`document`).
- Latest visual screenshot for Kitty coins is `resources/kitty-coins-no-disc.png` (bare opaque emoji collectibles, no backing circle — user feedback #4, 2026-08-03) — captured but NOT yet reviewed (current model can't read images; use MiMo V2.5 Free for visual review).
- Verify animal sound coverage after changing animal lists: cross-check `game/shared/audio.js` map against `name:` entries in each `game/games/*.js`.

Hosting notes (deployed on Cloudflare Workers at `games-for-kids.radman-milos-work.workers.dev`)

- `Unchecked runtime.lastError: The message port closed...` in the console is injected by a browser extension, not the site — ignore it.
- Every page declares an inline SVG favicon so no `favicon.ico` 404 is requested.
- `shared/audio.js` uses `preload='none'` so audio files load on demand instead of 11 downloads on every page load; the fallback in `playAnimalSound` fires when a source is missing, genuinely failed (`networkState === 3` or an `error` event set the `_failed` flag), or `play()` rejects.
- `shared/main.js` boot wires the back button and starts the game independently (typeof guards) so a missing game module can't break the page.
- Cloudflare Workers pretty-URLs redirect `/pages/<name>.html` → `/pages/<name>`, so `location.pathname` has NO `.html`. All standalone detection (`shared/main.js`, `animal_puzzle.js`, `animal_counting.js`, `animal_memory.js`) must strip `.html` before comparing page names. Do not match against `'name.html'`.
- `pages/papper_kitty.html` self-hosts the Fredoka font via two local `@font-face` rules (`game/assets/fonts/fredoka-latin.woff2`, `fredoka-latin-ext.woff2`, variable font weights 100–700) — bundled 2026-08-03 per user approval, no external font requests at runtime.

Contacts and conventions

- Commit messages: include small clear messages. If you want commits authored, include the Co-authored-by trailer as in project conventions.
- Keep changes minimal and validate immediately.

End of handover.

---
### Session note 2026-09-23 (opencode, Ponytail Lazy Dev)
- STOPPED mid-Batch-6 (boost visual identity) at user request to close for the day.
- CURRENT FILE STATE: `game/games/racing3d.mjs` passes `node --check` (exit 0, user fixed a stray `}` at line 1056).
- Batch 6 edits already applied in working tree: (a) longer/hotter boost flames in `flamePuff`;
  (b) `spawnP` tag plumbing. PENDING: boost-tinted dust, soft rear-wheel trail, smoke check #23,
  README 22->23, docs sync, offline rebuild, dated notes.
- CRITICAL NEXT SESSION STEP 1: re-run validation loop BEFORE further edits (syntax is ok but the file was
  edited by hand after a brace mishap): `node --check`, then `node tools/racing3d_smoke.js` (expect 22/22),
  `node tools/hub_smoke.js`, then `bash tools/sync-docs.sh`, then `pwsh tools/build_offline.ps1`.
- THEREFORE: do NOT claim batch 6 done; do NOT bump smoke count; do NOT commit. Confirm with user first.
- Encoding traps unchanged: PowerShell corrupts Cyrillic/CRLF files; use node utf8 + ASCII anchors, or the
  Edit tool with ASCII-only oldString. Prefer probe files (tools/probe_*.mjs) over inline `node -e`.
