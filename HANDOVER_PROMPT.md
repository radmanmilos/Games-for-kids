# Petrin svet Handover Prompt

Location: `E:\GitHub\Games for kids`

Purpose

This file summarizes the current workspace, conventions, and project state so the next session can continue without friction. Read this before making changes. It is refreshed at the end of every session.

## Session log — 2026-08-07

- Task 77: Added smoke tests for Animals, Shapes, Counting, Coloring (13/13, 11/11, 11/11, 13/13 PASS). `coloring.js` swatches now carry `data-color`.
- Task 78: Created `CONTRIBUTING.md` (setup, validation, docs mirror one-liner).
- Task 79: Split `adventure.js` into `adventure-music.js` + `adventure-modes.js` + core engine. All 4 adventure pages updated. All smokes green.
- Tasks 80/81/82: asset audit (nothing unused), stale `nul.mp3` doc references cleaned, aria-label audit (zero missing).
- Task 83: shared reduced-motion utility (`window.reducedMotion()` in `shared/utils.js`, loaded on the 5 canvas pages) + JS gates in adventure.js / kitty-standalone.js / candy.js / animal_counting.js; CSS baseline in accessibility.css. **FULLY REVERTED per user decision after play-test feedback** — the user's OS has `prefers-reduced-motion: reduce` ACTIVE (headless Chrome probe: `matchMedia(...reduce).matches === true`), so on their machine the gates stripped the memory card-flip animation, candy combo/hint/level-up animations, and obstacle-hit particles — gameplay-critical feedback for the child. All task-83 feature code removed (utils.js reverted to HEAD, script tags dropped from the 5 pages, JS gates removed, CSS block removed); aria-label work (task 82) untouched.
  - **Test-harness fixes kept (real harness flakes, NOT game bugs):** headless Chrome does NOT fire a native `resize` after `Emulation.setDeviceMetricsOverride` — adventure_smoke drive-resize and kitty_smoke resize checks now `sleep(300)` after the override and (kitty) manually dispatch `window.dispatchEvent(new Event('resize'))`. adventure_smoke drive-coin check made deterministic (reset `c.collected=false` + `coinCount=0`).
  - **Two PRE-EXISTING task-79 split regressions FOUND + FIXED while reverting (live on main since commit `5ed9978`):** (1) `adventure.js:1044` passed the `roadTopY` FUNCTION instead of `roadTopY()` into `AdventureModes.drawRoad` → `canvas.height - top` NaN → road/grass/dashed center line never painted (fix: pass `roadTopY()`; pixel probe confirms the [26,34] dash line renders). (2) `adventure.js:1119` restored the dropped `if (cfg.drawObstacle) { cfg.drawObstacle(ctx, o, theme); return; }` dispatch so ocean/space custom obstacle painters (ocean.js:596 / space.js:556) actually draw — pixel probe: ocean crab 84/84 + space asteroid 120/120 bbox pixels non-background (previously invisible). This is the "ocean/space obstacles invisible" bug the user reported.
  - **Pre-existing, NOT caused by this revert (verified against a git-stash HEAD baseline):** dino_smoke and space_smoke still fail the worlds-picker + music-toggle checks; ocean_smoke fails the same 3 (worlds picker open, jump-to-world, music toggle). Same class as the documented native-resize/music-timing flakes. Left for a later session.
- AGENTS.md updated: added rule to work in small batches + delegate to subagents when helpful; ask before each step.

Primary files to read first

- `README.md` — (first) high-level project overview, vision, architecture, and roadmap
- `PROJECT_TASKS.md` — authoritative task list and statuses (NEW / IN PROGRESS / DONE)
- `AGENTS.md` — Ponytail Lazy Dev (YAGNI) persona rules — always active for this project
- `game/index.html` — hub and script load order
- `game/shared/*.js` — navigation, audio, speech, utils, accessibility
- `game/games/*.js` — individual game runtimes (animals, shapes, candy, kitty, puzzle, counting, memory, coloring)
- `tools/README.md` + `tools/headless.js` — shared headless-Chrome test harnesses for the games (tracing smoke/probe live here)

Activate skill

- Ponytail Lazy Dev — enable this project persona and follow its rules (surgical, YAGNI, minimal changes).

Kilo config

- `kilo.jsonc` at the project root sets the default model (Big Pickle), snapshot mode, compaction, and permissions.
- Commands live in `.kilo/command/` (invoked via `/name`): `/smoke`, `/check`, `/sync-docs`, `/review`.
- Agents live in `.kilo/agent/` (e.g. `ponytail.md` for the YAGNI agent).

Working rules (short)

- Be surgical: prefer the smallest correct change. Follow the Ponytail Lazy Dev (YAGNI) rules.
- Use `game/` for final runtime files. `resources/` is reference only. `tools/` holds dev/test tooling (never deployed, never required by `game/`).
- No external runtime dependencies or remote assets without explicit approval. Audio assets are acquired once and stored locally.
- Validate with the smallest targeted check (node --check for JS, `tools/tracing_smoke.js` for tracing changes, `tools/driving_smoke.js` for adventure-engine/Driving changes, `tools/ocean_smoke.js` for Ocean changes, `tools/dino_smoke.js` for Dino/ground-mode changes, `tools/space_smoke.js` for Space changes, `tools/memory_smoke.js` for Memory, `tools/candy_smoke.js` for Candy, `tools/puzzle_smoke.js` for Puzzle, `tools/kitty_smoke.js` for Paper Kitty changes, open HTML manually for visual checks).
- Update `PROJECT_TASKS.md` when starting/completing tasks and leave a short note.
- Never add ads, analytics, tracking, or monetization. Offline-first, tablet-first, toddler-friendly.

Current state (as of 2026-08-07)

- **Task 83 (shared reduced-motion utility) is REVERTED (2026-08-07, per user decision):** the original DONE implementation (added `window.reducedMotion()` to `game/shared/utils.js`, script tags on the 5 canvas pages dino/driving/ocean/space/papper_kitty, JS gates in adventure.js/kitty-standalone.js/candy.js/animal_counting.js, `@media (prefers-reduced-motion: reduce)` collapse in accessibility.css) was fully removed. **Why:** the user's OS reports `prefers-reduced-motion: reduce` = true (headless Chrome probe), so on their machine the gates stripped the memory card-flip animation, candy combo/hint/level-up animations, and obstacle-hit particles — all gameplay-critical feedback. All animations are restored; the a11y CSS block, JS gates, utils.js addition, and the 5 script tags are gone. **Two pre-existing task-79 split regressions FOUND + FIXED during the revert** (both live on `main` since commit `5ed9978`): (1) `adventure.js:1044` passed `roadTopY` (function) instead of `roadTopY()` into `AdventureModes.drawRoad` → NaN geometry → road + grass + dashed divider never painted ("driving has no dashed lines"); now passes `roadTopY()`, pixel probe confirms the [26,34] dash line renders. (2) `adventure.js:1119` restored `if (cfg.drawObstacle) { cfg.drawObstacle(ctx, o, theme); return; }` so ocean/space custom obstacle painters (shark/jelly/rock/mine/asteroid/etc., ocean.js:596 / space.js:556) draw again — pixel probe: ocean crab 84/84 + space asteroid 120/120 bbox pixels non-background ("ocean/space obstacles invisible"). **Validation:** node --check clean; adventure_smoke 25/25 PASS (exit 0); kitty_smoke full PASS (exit 0); candy_smoke + counting_smoke ALL PASSED; ocean_smoke/space_smoke failures (worlds picker, music toggle) verified pre-existing against a git-stash HEAD baseline. **Harness fixes kept:** adventure_smoke drive-resize settle sleep + deterministic drive-coin reset; kitty_smoke manual `resize` dispatch.

- **Мала истраживачица (task 73, DONE 2026-08-06, Ponytail Lazy Dev):** **full character rebuild — the 🐱 emoji hero is gone, replaced by the user's Little Explorer sprite pack** (`resources/little explorer/little_explorer_sprite_pack/`, doc `AI_PROMPT_sequence_explainer.md`) as a real animated 2D platformer hero. `game/pages/papper_kitty.html`: 6 sprite frames deployed to `game/assets/images/explorer/` (idle 01/04/06 + walk 02 + run 03 + jump 05; source is 237×352 with feet bottom-aligned at row 328 in all frames, so one offset works; each pre-rendered to a 35×48 offscreen canvas from source rect (0,0,237,329) — no getImageData, no file:// taint, unlike the dino crop); **new state machine** resolved each `update()` (airborne→jump, else |vx|<1.2 idle cycling 01→04→06 at ~366ms, <2.6 walk, else run) + **walk→run acceleration** (`vx ±= 0.55` toward ±5.2, friction ×0.8 — the run frame finally gets used, no new button) + landing squash (0.8) + grounded bob kept; **drawCostume + all 11 `costume:` world keys deleted** (sprite already wears pith helmet + goggles + satchel); **rename to Мала истраживачица** (hub icon 🐱→🧭, iframe title, page title, favicon) and **cat-meow → synth "thud"** `playHurtSound` (sine 180→70Hz, 0.22s; `playCatSound`/`kittyCatAudio`/cat.ogg removed). IDs untouched (`startKitty`/`game-kitty`/etc. — no id churn). **Validated: extracted script `node --check` clean; kitty smoke 29/29 PASS.** Two smoke-test bugs fixed along the way (test-only; game was correct): (1) the new state-machine check raced the shuffled world's walkers/pipes — test now zeroes `walkers`/`mice` and anchors the player at x=40 on ground 0; (2) the **long-documented pre-existing `resize` flake is now FIXED** — its cause was the check requiring the player to land on `platforms[0]` only, but in some shuffled worlds the spawn sits on a `dy:80` float (feet y=932), which IS solid ground though not the ground platform; the settle check now accepts "top of any platform". **Awaiting user commit/push + on-device visual check** (idle/walk/run/jump feel, feet-on-ground, mirror). Next Phase 5 task = **69 adventure tuning (needs user play-test feedback first).**
- **Мала истраживачица follow-up (task 73b, 2026-08-06, per user play-test feedback "no bobbing, make it larger"):** grounded bob REMOVED (`bobPhase` field/increment/draw-offset deleted; smoke static check now asserts NO `bobPhase`) and hero sprite ENLARGED 35×48 → **43×60** (`EXPLORER_SPRITE_H` 48→60; draw anchored feet-flush via `player.height/2 - EXPLORER_SPRITE_H`, so only the head overhangs the 48×48 hitbox ~12px upward — hitbox untouched, levels stay tuned). **All kitty-smoke flakiness root-caused & fixed (test-only; game was correct):** (a) the `mirror` check used a color-SUM signature that is position-independent — a fully-enclosed hero mirrors its colors to mirrored positions, so the sum is unchanged; the old region only passed because it captured a partial hero → now a position-aware pixel-diff (`> 200` changed bytes). (b) the `stomp` check dropped the player at `walkers[0].x - 10` — OUTSIDE the patrol `[x, x+range]`, so it only stomped when the walker chanced to be near minX at the wrong moment (world-shuffle) → now freezes `walkers[0].vx=0` and drops the player straight onto the walker. (c) the `side` check placed the player 30px left of the walker — possibly a pit (walkers sit at their ground's left edge) → now places the player exactly on the walker, deterministically triggering the side-hit branch (feet at `groundY` fail the stomp's `<= groundY-6`). Validated: extracted script `node --check` clean; **kitty smoke 29/29 PASS × 3 consecutive runs** (was previously flaky under serial runs). docs/ re-synced. Still awaiting user commit/push + on-device visual check.
- **Мала истраживачица follow-up 2 (task 73c, 2026-08-06, per user):** (1) **running now alternates the run (03) and walk (02) frames** — `currentExplorerFrame()`'s run branch returns `floor(animTime/10)%2 === 0 ? run : walk` (~167ms per stride) instead of a frozen run pose; new smoke check "running alternates between the run and walk frames", and the state-machine run assertion relaxed to "run state uses a run-or-walk frame" (frame selection is now owned by the runMix check). (2) **hub kitty button now shows the character** — `game/index.html` `<button class="hub-btn kitty">🧭</button>` → `<img src="assets/images/explorer/03_run_right.png" alt="Мала истраживачица">` with `.hub-btn img{width:78%;height:78%;object-fit:contain}`. Validated: `node --check` clean; **kitty smoke 30/30 PASS + hub smoke 8/8 PASS**. docs/ re-synced. Awaiting user commit/push + on-device look at the run animation and the new hub icon.
- **Play-test follow-ups (task 71, DONE 2026-08-06, Ponytail Lazy Dev):** two fixes in `game/` + one regression in `tools/kitty_smoke.js`, **awaiting user commit/push** (docs/ re-synced). **(1) Adventure bottom-cutoff on tablet/phone** — `game/shared/adventure.css` `#adv-game` now `height:100vh; height:100dvh;` (was bare `100vh`, which extends under the browser address bar on mobile so bottom controls + screen content fell below the viewport). Kitty was unaffected because it runs inside the hub iframe where `100vh` == the iframe's visible height. Layout probe verified at 1100×700 (`gameBottom==700==innerH`, `controlsTop==476`); dvh has no effect on headless smokes (headless viewport == dvh) or the hub iframe. If the user still sees cutoff on-device, next candidate is a `100svh`/`-webkit-fill-available` fallback. **(2) Kitty water world pits → spikes** — `game/pages/papper_kitty.html` `WORLDS[6]` (Подводни свет) `pitHazard: 'water' → 'spikes'` (spring/summer/japan underwater keep `water`; `drawPitHazards()` already drew `'spikes'`). Added kitty-smoke regression ("water world: pits are spikes"), kitty smoke is now **16 checks**. **Pre-existing flake (NOT caused by this change, reproduced at clean HEAD):** kitty_smoke's `resize` check ("ground re-anchored to the new canvas height, kitty lands on it") intermittently fails under serial runs (`onGround:false`, e.g. `y:932` vs expected `gy - player.height`).
- **Adventure bottom-cutoff follow-up (task 71b, FIXED 2026-08-06, Ponytail Lazy Dev):** user reported the task-71 `100dvh` fix STILL cuts the bottom on their tablet (e.g. left buttons show only the top half). Root cause: a browser without `dvh` support (older Android WebView / Samsung Internet) ignores `height:100dvh` and falls back to `100vh`, which extends under the address bar → bottom clipped; a CSS `100svh` fallback can't help because svh ships in the same engine generation as dvh. Fix: `game/games/adventure.js` (shared engine for all 4 adventure pages) now pins `#adv-game` height to `window.innerHeight` (the universally-correct visible-viewport height on every mobile browser) and re-syncs on `resize` + `orientationchange` + `visibilitychange` (defers ~100ms for address-bar settle). Inline style overrides the CSS; desktop unaffected (innerHeight == layout viewport). CSS `100vh/100dvh` remains the pre-JS baseline. Validated: `node --check` clean; **driving 17/17, ocean 20/20, dino 40/40, space 21/21 PASS** (a first-run driving coin FAIL was a pre-existing test-order flake — steering had already collected `coins[0]` — clean on re-run). docs/ re-synced. **Needs user on-device re-test on the tablet.**
- **Dino heroes = user PNGs (task 72, DONE 2026-08-06, Ponytail Lazy Dev):** all four hand-drawn vector dinos replaced with exactly 2 PNG sprites from `resources/dino/` (brontosaurus + tiranosaurus rex, both 2000x2000 transparent PNGs), copied into deployable `game/assets/images/dino/` (filename `tiranosaurus rex.png` sanitized to `tiranosaurus-rex.png`). `game/games/dino.js`: vector block deleted (`DINO_PALETTE`/`dinoEye`/`dinoQuadLegs`/`drawTRex`/`drawTriceratops`/`drawStego`/`drawDiplodocus`); new `DINO_SPRITES` + `loadDinoFrame(type)` decodes each PNG, crops the transparent padding via an alpha scan (step 2, alpha > 32), and pre-scales the content to the 110x100 hitbox with feet flush at the bottom (drawn at local y=+53 to offset the engine's `translate(0,-3)`). Picker = 2 options (Бронтосаурус + Тиранозаур), thumbnails mirrored (`scale(-1,1)` + `translate(55,47)`) to match the in-game look, `redrawPickerPreviews()` refills the canvases when the images finish loading. Config now `heroFlip: true` (pixel-mass analysis: top-35% mass centroid x=585/633 vs image centre ~1000 => both PNGs face LEFT natively, the engine mirrors them to face right). CSS: picker grid 4 -> 2 columns. `tools/dino_smoke.js` updated (2 picker buttons + new names, bronto/t_rex clicks, heroFlip assertion) plus a new check that both cropped frames land 100px tall / 60-110px wide (measured 94 and 95). Validated: `node --check` clean, **dino smoke 40/40 PASS**, ocean/driving/space green. **Awaiting user commit/push + on-device visual check** - facing direction is inferred (if a dino walks backward, flip `heroFlip`); also confirm feet-on-ground and size. **BUG FIXED 2026-08-06 ("dinos not visible at all"):** the pixel-crop in `loadDinoFrame` called `getImageData` on a canvas drawn from the PNG — under `file://` the canvas is tainted and that throws SecurityError, so `onload` aborted before `dinoFrames[type]` was set → invisible dino. Over HTTP it rendered fine (A/B pixel diff 4596px). Fix: `getImageData` wrapped in try/catch with a hardcoded `DINO_CROP_FALLBACK` (exact measured crops: bronto `{x:264,y:106,w:1529,h:1633}`, t_rex `{x:154,y:112,w:1691,h:1775}`) — only the readback is blocked under file://, drawing works. Re-verified headless: file:// now loads both frames, HTTP still renders; dino smoke 41/41. Supported way to play stays Live Server over HTTP (file:// also breaks audio + the kitty iframe).
- **Phase 5 is now GAME polish** (user decision 2026-08-05): focus = the 8 ИГРЕ games only (Paper Kitty, Driving, Ocean, Dino, Space, Candy, Memory, Puzzle); УЧЕЊЕ learning apps (Classroom, Tracing, Animals, Shapes, Counting, Coloring, Piano) are OUT of scope. Task 65 (Memory) is DONE: progress line "Парова: N од 8 · Потези: M", floating "Пар!" popup on matched pairs, moves counter, card back = 🃏 (game hub icon, per user). Validated by `tools/memory_smoke.js` (8 checks, ALL PASS). Task 66 (Candy) is DONE (incl. round-1 play-test feedback): level milestones + cute progress bar (⭐ riding the fill), 🎉 level-up overlay + fresh board, grid grows 4×4→8×8 (tiles shrink, grid always fits), rotating farm/wild animal sets, bar spaced off the grid, **star-spawn logic guaranteed at all levels** (build-time check guarded against the mid-move race; also runs after miss swaps), new 💡 hint button (highlights the 2 tiles + "Погледај жабе 🐸 😉" message). Validated by `tools/candy_smoke.js` (12 checks, ALL PASS). Task 67 (Puzzle) is DONE (per user requirements 2026-08-05): **8 painted scenes** (5 existing redone better + 3 new — Другари на фарми 🐄 / Другари у свемиру 🚀 / Другари на снегу ⛄), **NO text on any image** (painted title banner removed; title only in the page h1), **thin frame drawn on every picture** (white band + plum inner line — corner/edge reference for the child), and **ring placement**: split pieces are arranged AROUND the placement board (never covering it, no piece-piece overlap; greedy first-fit on ring bands + fine fallback), board resized to `min(46vw,420px,60vh)` so pieces physically fit around it at every screen size. Existing painters enhanced (park fountain/bench/butterfly, house curtains/path/fence, savanna watering hole/birds, forest extra tree/berries, sea submarine/coral/starfish). Validated by `tools/puzzle_smoke.js` (16 checks, ALL PASS — pixel-verified frame + no banner, ring checks at 2×2 AND 3×3 with 0 pieces on the board + 0 overlaps, keyboard-completed all 8 puzzles, scene order + grid alternation + final score 8). Remaining tasks: 68 Kitty (level pacing — needs user feedback first), 69 adventure games tuning (needs user feedback first). **User rule: before executing any of these tasks, ask the user what they would add/change first. Tasks 65+66+67 are implemented but NOT yet committed — ask the user before committing.**

- **Paper Kitty (task 68) is DONE (2026-08-05, Ponytail Lazy Dev):** scope changed from "level-pacing re-tune" to a **level-element redo** per user ("worlds are fine"): (1) **ground-walking enemies** — 4 per world, data-driven `walkers:[{x,range}]` patrols on the ground (vx 1.3, flip at patrol ends, gentle sine bob); jumping on one defeats it + gives a coin (particles + sound), touching its side plays the cat sound + respawns; **NEW dedicated themed sprites per world** (user rule: do NOT reuse pipe-pop art) — ladybug/bee/hedgehog/snowman/worm/spider/crab/meerkat/panda/seal/scarab via `drawWalker(type)` with per-type `WALKER_FOOT` feet offsets; all pixel-measured ≤ 48×48 (≤ kitty size). (2) **Water world finish line = submarine** — new `drawGoal('submarine')` branch. (3) **Gentle kitty bob** — `player.bobPhase` → `Math.sin(bobPhase)*2.5` translate while grounded. Walkers freeze after the level is won. Validated: `node --check` clean + new canonical `tools/kitty_smoke.js` **14/14 PASS** (boot/HUD, 11 worlds × 4 walkers + valid types, patrol geometry clear of pipes/stairs/goal/floats/moves/peers, submarine goal, static wiring, patrol inside [minX,maxX], jump-kill +1 coin, side-hit respawn no coin, freeze after win, 11 sprites ≤48×48 via real-canvas pixel readback, all 11 worlds load/update/draw). **Smoke gotchas:** `drawWalker` draws on the page's global `ctx` — sprite-size probes must clearRect/read back from the REAL canvas (an offscreen canvas reads empty); walker patrols that crossed a pipe made the fall-kill test land the kitty on the pipe (fixed the data: spring/summer/savanna/northpole/egypt walker0 moved past their first pipe). Next Phase 5 task = **69 adventure tuning (needs user play-test feedback first).**
- **Adventure polish round (task 70, DONE 2026-08-05, per user play-test feedback) is implemented but NOT yet committed:** (1) **fly-mode spawn clear of the bottom D-pads** (ocean/space) — `x = Math.max(280, canvas.width*0.3)`, `y = canvas.height*0.4`; (2) **full-vertical fly-mode spread** — new engine var `flyDyScale` = `(canvas.height*0.88)/maxDy` (per-world obstacles/coins/goal now span ~450–660px instead of a flat band; `dyScale` stays drive-only); (3) **themed pit hazards** — `pitHazard` per world: `lava` (volcano/cave/underground/cave2) with animated bubbles, `water` (lake/swamp/island/spring/summer/underwater/japan) with waves + 🐟, `spikes` (jungle/valley/ice/desert/night/autumn/winter/savanna/northpole/egypt) as dark spikes; drawn by `drawPitHazards(t)` (adventure.js) + `drawPitHazards()` (kitty) before platforms; (4) **space vortex stargate finish** — `drawSpaceGoal` now draws pylons + a three-arm swirling vortex (rotating `ellipse`s) + event-horizon core with inward spiral sparkles + „ЦИЉ" pill; (5) **dino ground always extends under the finish line** — each of the 10 dino last-grounds extended +260px past its `goalX` (regression check: no `lastRight < goalX+220`); (6) **root-cause "drop through the ground and respawn" fix (dino + kitty)** — `resizeCanvas` re-anchors world geometry bottom-up on resize (`dy = canvas.height - oldH`; grounds, mice/walkers y-shifts in kitty, non-ground obstacle/coin/goal y-shifts in adventure) so the ground stays at `canvas.height - 140`; plus **swept-landing** — platform checks now use `previousY + player.height <= p.y + 24` (with `player.vy >= 0`) instead of the narrow vertical band, so fast falls can't tunnel through the ground on tall canvases. Kitty also got a hoist-safe `worldLoaded` flag (resize ran before `theme`/`groundY` were set). Validated: `node --check` + all five smokes green — **dino 38/38** (incl. pits ≥2 fallable ≥150px + one ≥180px + no >240px gap per world, last-ground ≥ `goalX+220`, resize re-anchor to `gy = height-140` + landing, chosen-hero+behind-decor draw), **kitty 15/15**, **driving 17/17**, **ocean 20/20**, **space 21/21** (incl. vortex-stargate draw). Coin pickups in ocean/space/driving smokes now compare `before` vs `after` (coinCount is a session total — absolute `1` was flaky). Leftover: `C:\Users\RADMAN~1\AppData\Local\Temp\opencode\fly_spread_probe.js` debug probe — delete.
- Tasks 1–34, 38–52, **53 (Писање Tracing game)**, **54 (tools/ tooling)**, **43 (alphabet pronunciation/image audit)**, **55 (Phase 3: Учионица kids tier)**, **57 (GitHub Pages via `docs/` mirror)**, **58/59 (Клавир Piano game)**, **60 (shared adventure engine + Возила Driving game)**, **61 (Океан Ocean game)**, **62 (Дино Dino game)**, and **63 (Свемир Space game)** are DONE. **Task 56 (GitHub Actions auto-deploy) is ABANDONED** (2026-08-04, per user decision — Pages refused to deploy from `game/`; the workflow was removed). Tasks 35/36 (stickers + screen transitions) are **deferred to a later phase**. **Phase 4 is COMPLETE (2026-08-05) — all four adventure games shipped (Возила/Океан/Дино/Свемир). The user is play-testing Свемир (task 63) now; wait for feedback before any follow-up (user rule: stop after each game for feedback).**
- **Клавир (Piano, tasks 58/59):** standalone `game/pages/piano.html` + `game/games/piano.js`. 8-key one-octave keyboard (C4–C5, rainbow color-coded), Web Audio `tone()` (no new audio assets). Free play (tap → note + glow) and "Свирај песму" — follow-the-melody mode with a **3-song chip picker** (`#songChips`, **emoji icons ⭐/🎂/🔔** with the title as `aria-label`): **Трепери, трепери звездице** (42 notes), **Срећан ти рођендан** (25), **Џингл белс** (26) — all transposed/simplified to fit the octave; keys light up one at a time playing their tone, child taps the same key to advance (wrong = shake + "Покушај још једном!", no punishment; counter adapts to song length; finish → celebrate 🎹 + "Свирао си песму!" panel + Играј поново; 🔊 Чуј песму preview plays the chosen melody — stopping it re-lights the current guided note). Hub button 🎹 is **3rd** (after Учионица/Писање, per user request). Canonical validation: `node tools/piano_smoke.js` (14/14 PASS).
- **Возила (Driving, task 60, DONE 2026-08-04):** new **shared adventure engine** `game/games/adventure.js` (~1470 lines) — config-driven port of the Paper Kitty runtime, reusable by all four Phase 4 games. Modes: `ground` (kitty physics), `drive` (road auto-scrolls via `cfg.driveSpeed`, hero steers/clamped), `fly` (free 2D + momentum). Config-driven: 10+ worlds each with `hero`, `speed`, `driveSpeed`, obstacles (7–8, forgiving collision), 13 coins, `goalX` (5800–6650), per-world `decor` (city/palms/gulls/stars/leaves/snow), `bg` gradient, and a unique `music` theme (32-step melody + 16-beat bass + ambient layer: horn/cricket/bird/wind/rustle/rumble/owl/waves/desert/bell). Shared HUD ids `#adv-*` (canvas, score, coin-count, level, world-name, worlds/music buttons + modals), shared page chrome `.adv-back` + `#adv-title` in `game/shared/adventure.css`. Exposes `window.AdventureEngine.create` + `window.__adv` debug handle (player/coins/obstacles/goal/theme/music/levelCompleted/lastHitAt/bumpCount) for headless tests. Game = thin page + config: `game/pages/driving.html` (title "Возила", back btn `#driving-back`) + `game/games/driving.js` (hero 🚗, 10 road worlds: Градски трг ⭐, Поље сунцокрета 🍭, Јесења шума 🍂, Зимски пут ⛄, Планински пут 🎿, Ноћни град 🌙, Пустињска магистрала 🌵, Тропско острво 🌴, Морска обала 🐚, Космичка стаза 🪐). Wired: hub 🚗 button (`data-go="game-driving"`, after kitty button), `navigation.js` route, `main.js` standalone boot `'driving': ['driving-back','startDriving']`. **Engine bugs fixed during smoke:** obstacle-collision check had been OUTSIDE the `obstacles.forEach` loop (ReferenceError every frame) — moved inside; drive `respawnPlayer()` left `player.x` stale for a frame (teleport) — now sets `player.x = cameraX + player.offsetX` immediately; drive `player.y` clamp now `canvas.height*0.24 … -10`. Canonical validation: `node tools/driving_smoke.js` (17/17 PASS).
- **Океан (Ocean, task 61, DONE 2026-08-05):** second Phase 4 adventure game on the shared engine — **fly mode** (free 2D, no gravity). `game/pages/ocean.html` (title "Океан", back btn `#ocean-back`, **4-way fly D-pad** `#adv-controls.adv-fly-controls` — new CSS in adventure.css) + `game/games/ocean.js` (hero 🐟, `heroFontSize` 84). 10 underwater worlds: Корални гребен, Лагуна, Морске траве, Каменита обала, Потопљени брод, Морска пећина, Ледени океан, Морски ров, Пиратско благо, Ноћни океан — 8 obstacles + 13 coins + goal (🏁 flag-arch banner, „ЦИЉ!" on a woven-ribbon cloth) each. 9 obstacle types: patrolling sharks (**vx/minX/maxX** — bounce flips vx when `x+width` crosses the bound), jellyfish, rocks, mines, seaweed, pufferfish, crab, anchor, default rock. 10 music themes (32-step melody + 16-beat bass + root + ambient) + 10 decor types (bubbles/corals/starfish/shells/rocks/kelp/fish schools/cave lights/pyramid/glints). **Engine hooks added to adventure.js:** `cfg.drawObstacle(ctx, o, theme)`, `cfg.drawDecor(ctx, t, cameraX, canvas, theme)` (both return early when defined), `cfg.heroFontSize` (default 48), `cfg.heroFlip` (inverts the facing-right scale — needed because the 🐟 emoji natively faces LEFT), `cfg.heroBob` (slow sine vertical bob on the hero draw). Wired: hub 🐠 button (`data-go="game-ocean"`), `navigation.js` route, `main.js` standalone boot `'ocean': ['ocean-back','startOcean']`. Canonical validation: `node tools/ocean_smoke.js` (20/20 PASS). **Polish round (2026-08-05, per user feedback):** fish faces its movement direction (`heroFlip: true`), fish bobs while swimming (`heroBob: 5`), and the fly D-pad is tablet-friendly — ◀▶ in a horizontal cluster on the LEFT edge, ▲▼ in a vertical cluster on the RIGHT edge (`adv-pad-horizontal`/`adv-pad-vertical`; HTML button order is now `left,right,up,down`). **Smoke gotchas:** `window.__adv` `cameraX`/`lastHitAt` are getter-only (assignments silently no-op — reset state via `a.loadWorld()`, not assignment); obstacle-hit checks must wait ≥1100 ms after `loadWorld()` for the `lastHitAt` cooldown; assert patrol bounce on the `vx` flip, not on `x2 < x1`.
- **Дино (Dino, task 62, DONE 2026-08-05):** third Phase 4 adventure game on the shared engine — **ground mode** (jump/gravity/platforms, kitty physics). `game/pages/dino.html` (title "Дино", back btn `#dino-back`, **ground controls** `#adv-controls.adv-ground-controls` — ◀▶ D-pad cluster on the LEFT edge + one big ⬆ jump button (`data-adv="jump"`) on the RIGHT edge; new CSS in adventure.css) + `game/games/dino.js` (hero 🦕, `heroFontSize` 78, `heroW` 110, `heroH` 100, `heroFlip: true` — 🦕 faces LEFT natively; `speed` 5.2, `jumpPower` −13.5). 10 prehistoric worlds: Прашума, Тропска долина, Језеро, Вулкан, Пећина, Мочвара, Ледено доба, Пустиња, Ноћни свет, Острво диносауруса — solid `grounds`, floating `floats` (dy above ground), moving `moves` (shuttle between minX/maxX), auto step-up `stairs` (`stairColor`), and `pipes` (h) that pop out a mini-raptor every 3s; ~15 coins + ЦИЉ goal gate each. 10 music themes (32-step melody + 16-beat bass + root + ambient) + 10 decor types (jungle/valley/lake/volcano/cave/swamp/ice/desert/night/island). **Engine hooks used:** `cfg.drawEnemy` (mini-raptor), `cfg.drawDecor`, `cfg.drawGoal` (stone temple arch + ЦИЉ banner). **Engine bug found+fixed while shipping (dino is the FIRST ground-mode game on this engine):** ground-mode `loadWorld` called `w.floats.map`/`w.moves.map`/`w.pipes.map` unguarded — any world missing those keys threw and left `goal` null (update then crashed on `goal.x`); all three now default to `(w.x || [])`. Also added `get mice()` to the `window.__adv` debug handle (for the enemy smoke checks). Wired: hub 🦕 button (`data-go="game-dino"`), `navigation.js` route, `main.js` standalone boot `'dino': ['dino-back','startDino']`. Canonical validation: `node tools/dino_smoke.js` (25/25 PASS).
- **Свемир (Space, task 63, DONE 2026-08-05):** fourth and FINAL Phase 4 adventure game on the shared engine — **fly mode** (free 2D, no gravity). `game/pages/space.html` (title "Свемир", back btn `#space-back`, **reuses the existing fly controls** `#adv-controls.adv-fly-controls` — ◀▶ horizontal pad LEFT, ▲▼ vertical pad RIGHT; NO new CSS) + `game/games/space.js` (hero 🚀, `heroFontSize` 84, `heroW` 110, `heroH` 100, `heroBob: 4` — NO `heroFlip`: 🚀 faces RIGHT natively, unlike 🐟/🦕; `offsetStartRatio` 0.16, `speed` 4.5, `obstacleScale` 1.15, `pickupFontSize` 58). 10 space worlds: Звездано небо, Месечева стаза, Црвена планета, Астероидни појас, Сатурнови прстенови, Јупитеров вихор, Ледени месец, Свемирска станица, Галаксија, Дубоки свемир — 8–10 obstacles + 13 coins + goal (`drawSpaceGoal`: two pylons + glowing portal + „ЦИЉ" banner) each. 8 obstacle types: meteors, asteroids, UFOs (**patrol via vx/minX/maxX**, same bounce as ocean sharks), comets, ring bands, planets, satellites, black holes — plus an alien cameo (`cfg.drawObstacle` dispatch). 10 music themes (32-step melody + 16-beat bass + root + ambient: stars/moon/mars/belt/rings/giant/ice/station/galaxy/deep) + 10 decor types (stars/moon/mars/belt/rings/giant/ice/station/galaxy/deep — procedural starfields, parallax cratered planets, shooting stars, Mars dunes, Jupiter bands, ice crystals, space stations, galaxy clouds, black-hole swirls). **No new engine work — all hooks pre-exist from tasks 60–62** (`drawObstacle`, `drawDecor`, `drawGoal`, `heroFlip`, `heroBob`, `obstacleScale`, `speed`). Wired: hub 🚀 button (`data-go="game-space"`, after dino), `navigation.js` route, `main.js` standalone boot `'space': ['space-back','startSpace']`. Canonical validation: `node tools/space_smoke.js` (18/18 PASS); all four adventure smokes re-run green.
- **Писање (Tracing, task 53):** standalone `game/pages/tracing.html` + `game/games/tracing.js`. FREE DRAW on a dashed guide (user feedback #2 on 2026-08-03 — connect-the-dots was not intuitive). The `#tracingGuide` canvas draws a faint dashed outline of the target (letters/numbers in `GUIDE_FONT='900 288px Fredoka…'`, shapes via `drawShapeOutline`), refreshed on every `loadItem`, never counted as ink; a compact ref card sits above. Matching is metrics-based (48×48 grid): pass = `coverage ≥ 0.45` && `near ≥ 0.8` (fraction of ink within 2 cells of the guide — decisive anti-blob/anti-scribble rule) && `ink ≤ 2000`. Correct → celebrate + speech then auto-advance after 2600 ms; wrong → "Покушај још једном!" nudge, canvas kept for redraw. Облици is now only the 4 flat shapes (3D wireframes dropped per user decision). Canonical validation: `node tools/tracing_smoke.js` (22/22 PASS). Screenshots in `resources/tracing/*.png` are STALE (pre-redesign; visual review with MiMo V2.5 Free pending re-capture).
- **Task 47 (visual audit) completed:** captured 19 screenshots, reviewed all, found 7 items (0 blockers, 0 major, 3 minor, 4 cosmetic). 5 fixes implemented: memory button emoji 🧠→🃏, portrait grid gap reduction, kitty back button backdrop, coloring palette 4+4+3 grid, classroom title shifted right. Finding #1 (orphaned hub button) deferred; #3 (letter Ј font) accepted.
- **Учионица (Classroom, tasks 39/41/42/44/43):** standalone game `game/pages/classroom.html` + `game/games/classroom.js` — 4 learn-and-repeat activities: Азбука (30 letters; since task 43 the 25 consonants speak their letter sound, vowels their name, plus a word + emoji), Бројеви (0–10, sentences like "Пет слонова"), Облици (Круг/Квадрат/Троугао/Звезда + 3D SVG shapes Лопта/Коцка/Квадар/Ваљак/Купа/Пирамида), Боје (11 colors). Autoplay ▶/⏸ walks tiles at speech-end + 1.5 s pause; no celebrate icon. Polish: "Учионица" title shows only on the hub (not inside an activity); grids no longer resize on tap (no scale animations, `scrollbar-gutter:stable`, fixed-height display); alphabet/number/shape tiles use a cycling 8-color pastel background.
- **Memory (tasks 33/46):** flip whoosh sound + paw-print card back; animal name + sound now play **only on matched pairs** (not on every flip, not on mismatches).
- **Paper Kitty Adventure (task 49, DONE, self-contained `game/pages/papper_kitty.html`):** 11 worlds (Spring, Summer, Autumn, Winter, Underground, Cave, Water, Savanna, Japan, North Pole, Egypt), 1 level each, shuffled order per run + 🌍 world picker, theme-matched goal buildings/enemies/collectible emojis/costumes, stairs with auto step-up, rocky ceiling in Underground/Cave, longer/varied levels (goalX 5150–6850, 4 layout patterns), collectibles drawn as BARE opaque emoji (no backing circle/disc, no alpha/glow — user reported transparency, then asked for the cream sticker circle removed; 2026-08-03). **Procedural background music** (no audio files, Web Audio only): `MUSIC` config = 11 themes with 32-step melody + 16-beat bass + per-world **ambient layer** (bird/cricket/rustle/wind/rumble/drip/bubble/flute/bell/coo/desert), slow tempos 58–100bpm, lookahead scheduler (`musicTick`: 120ms interval, 0.35s horizon, `musicStepTime`/`musicStep` reset on swap). `loadWorld()` calls `startMusic(w.music)`; theme swaps instantly. **Music on/off button** 🔊/🔇 = `#music-btn` at `right:268px` (LEFT of `#worlds-btn` at `right:200px` — do not move closer or they overlap); persists `musicOn` in localStorage key `pkMusic`; music starts on first user gesture via `initAudio()` (which restarts the current theme). The per-world `music:` keys must stay in sync with `MUSIC` keys.
- **Hub (task 64, 2026-08-05):** `game/index.html` is now a **two-level landing** — the title "🌈 Петрин свет" sits prominently over two big group tiles: 🧠-replaced **УЧЕЊЕ first** (icon = 2×2 emoji combo 🏫📝/🎹🎨 — four `<span>` cells in a CSS grid; user rejected the 🧠 brain as not kid-friendly and picked the combo over a chalkboard SVG, then swapped 🎓→🎹 and ✏️→📝 and switched their places) then 🎮 **ИГРЕ**, each opening a sub-hub screen (`#hub-games` / `#hub-learning`) with the group's round buttons and a back arrow to the landing. User-confirmed split: ИГРЕ = Kitty, Driving, Ocean, Dino, Space, Candy, Memory, Puzzle (8); УЧЕЊЕ = Classroom, Tracing, Animals, Shapes, Counting, Coloring, Piano (7). Kitty's in-app back button targets `hub-games`. Sub-hub screens are `justify-content:flex-start` with the grid at `margin: 24vh` (clears title + back button, scrolls cleanly on short landscape). Canonical check: `node tools/hub_smoke.js` (8/8 PASS). Screenshots: `resources/hub-redesign/*.png` (visual review pending with MiMo V2.5 Free).
- **App name: "Petrin svet" (Петрин свет).** All child-facing text in every game is in Serbian Cyrillic; speech synthesis is Serbian (`speech.js` uses `lang='sr-RS'`). Permanent rule in `AGENTS.md`.
- Games shipped: Animals, Shape Match, Match Game (candy), Paper Kitty Adventure (embedded in-app), Animal Scene Puzzle, Animal Counting, Animal Memory, Coloring, **Учионица (Classroom)**, **Писање (Tracing)**, **Клавир (Piano)**, **Возила (Driving)**, **Океан (Ocean)**, **Дино (Dino)**, **Свемир (Space)**.
- Every game page is a standalone HTML that loads only the shared modules it needs, plus its own `games/<name>.js`. Kitty is the one in-app screen (iframe) launched from the hub.
- Navigation is driven by `shared/navigation.js` `window.goTo(id)`; every `[data-go]` button routes there. Standalone pages' back buttons redirect to the hub (`../index.html` from `pages/`), wired in `shared/main.js`.
- Audio is centralized in `shared/audio.js` (single AudioContext via `ctx()`; helpers: `tone`, `popSound`, `successChime`, `gentleMiss`, `sweep`, `playAnimalSound`).
- Celebration: `shared/celebration.js` defines `window.celebrate(emoji)` — self-contained full-screen overlay (injects its own style), plays `successChime`, speaks "Браво!" (MP3 asset). Used by shapes (round), coloring (scene, keeps animal sound), puzzle (complete, keeps Next), memory (win), counting (final "Готово!" — with two confetti waves; per-answer keeps its inline 🎉 + a full-screen `confetti()` burst, added 2026-08-02). Old per-game `.celebrate` overlays/CSS were removed.
- Speech (`shared/speech.js`) plays pre-generated Serbian MP3 assets (`assets/audio/speech/*.mp3`, **108 files**: 12 animals incl. Кока, 4 flat shapes + 6 3D shapes, numbers 0–10, "Браво!", 30 letter names, 23 alphabet words, 11 colors, 10 number sentences) keyed by the exact Cyrillic word, with OS speechSynthesis as fallback for unknown text. The dev machine has NO Serbian TTS voice, so assets are the only audible path here. `speak()` is called by coloring ("Обој …" — no asset yet, falls back silent here), animals (card tap), shapes (correct placement), counting (number word on correct answer, up to 10), memory (animal name on matched pair only), classroom (letter/word/number/sentence/shape/color on tile tap + autoplay). New assets are generated with `resources/tts_generate.js` (Google Translate TTS tl=sr; idempotent, skips existing files).

Audio / animal sounds (verified complete)

- `playAnimalSound(name)` maps each animal to a local file in `game/assets/audio/`:
  Dog→dog.ogg, Cat→cat.ogg, Cow→cow.ogg, Lion→lion.ogg, Elephant→elephant.ogg,
  Frog→frog.oga, Pig→pig-grunt.ogg, Duck→duck.ogg, Fox→fox.mp3, Sheep→sheep.ogg, Horse→horse.ogg, Chicken→chicken.ogg.
- All 12 animals used across the games (animals.js, candy.js, animal_counting.js, animal_memory.js) resolve to valid, real local audio files — verified by header check.
- Fox sound is a real field recording (Pixabay "fox calling", l3hrja) at `assets/audio/fox.mp3`.
  Sheep (`sheep.ogg`) and Horse (`horse.ogg`) are CC0 recordings from BigSoundBank.com (Joseph SARDIN). No attribution required.
  Chicken (`chicken.ogg`) is Mixkit "Chickens clucking short" (id 1772, free license), trimmed to 2.15s, mono 44.1kHz OGG 25KB.
- `playFoxSynth()` and `playChickenSynth()` remain in audio.js only as fallbacks if a file ever fails to load.
- `playAnimalSound` auto-stops playback after a per-animal max duration (see `maxDuration` map in audio.js).

Folder structure

```
game/
  index.html
  pages/     animals.html shapes.html matching_game.html animal_puzzle.html animal_counting.html animal_memory.html coloring.html classroom.html tracing.html papper_kitty.html driving.html ocean.html dino.html space.html
  games/     animals.js shapes.js candy.js kitty.js animal_puzzle.js animal_counting.js animal_memory.js coloring.js classroom.js tracing.js adventure.js driving.js ocean.js dino.js space.js
  shared/    navigation.js audio.js speech.js utils.js main.js accessibility.css adventure.css
  assets/    fonts/ audio/ images/
tools/
  headless.js  tracing_smoke.js  tracing_probe.js  dilate_test.js  piano_smoke.js  driving_smoke.js  ocean_smoke.js  dino_smoke.js  space_smoke.js  kitty_smoke.js  kids_smoke.js  hub_smoke.js  memory_smoke.js  candy_smoke.js  puzzle_smoke.js  adventure_smoke.js  README.md
resources/
  dev-only assets (tts_generate.js, visual_audit_capture.js, screenshots, audit reports)
```

Notes:
- Only `index.html` lives at the `game/` root. All game pages live in `game/pages/`; the hub loads scripts from `game/shared/` and `game/games/`.
- `tools/` = headless test harnesses (see `tools/README.md`); never deployed, never required at runtime. Start every headless run through `tools/headless.js` (unique temp Chrome profile per run) so stale profile locks can't crash later runs.
- `shared/audio.js` roots animal-sound asset paths relative to the document URL (detects `/pages/` in the URL) so sounds work both from the hub and from pages/.
- `game/assets/audio/` holds exactly the 12 animal sounds used by `playAnimalSound` (the legacy duck.ogv, dog-google.ogg, pig.ogg, animal-growl.ogg, animal-squealing.ogg were removed on 2026-08-02).

Coloring game notes

- Scenes live in `game/games/coloring.js` (`coloringScenes`, 12 animals, 134 regions). Each region has `tag`, `color` (a palette hex), and either `attrs: {…}` or the SVG attributes flat on the region object. `createColoringRegion` accepts both forms (`r.attrs || r`, skipping `tag`/`color`), so both conventions are fine — do not assume `attrs` is always present.
- **Revamp (2026-08-03, task 51):** all 12 scenes were redrawn for recognizability (see task 51 note) and the canvas was enlarged in `pages/coloring.html` — ref thumbnail `min(32vmin,24vh)`, main SVG `min(56vmin,48vh)`. New screenshots of every scene are in `resources/coloring-redraw/*.png` (review with MiMo V2.5 Free).
- Layout: `.coloring-wrap` is a flex column; `.coloring-stage` contains name+progress text and a `.coloring-stage-row` (ref thumbnail + main SVG side by side); the palette is pinned at the bottom with `flex-shrink:0`. The wrapper uses `overflow:auto` so content scrolls if it overflows.
- A ➡ next-skip button (top-right, wired in `startColoring()`) advances to the next animal without waiting for the auto-advance.
- Palette is a 6×2 CSS grid (`repeat(6,9vmin)`, 11 swatches + 1 empty cell).
- The success handler calls `playAnimalSound(sceneName)` + `successChime()` and auto-advances after 1500 ms; scene names must match the keys in `shared/audio.js` `animalSoundFiles`.
- Scene-integrity check (colors ∈ palette, region shape valid) was verified; re-run after editing scenes.

Tracing game notes (task 53 — free-draw redesign, 2026-08-03)

- Game lives in `game/pages/tracing.html` + `game/games/tracing.js`. Hub (Слова 🔤 / Бројеви 🔢 / Облици 🔷) shows first on open.
- **Free draw on a dashed guide, not dots.** `#tracingGuide` canvas (400×400, absolute `inset:0`, `pointer-events:none`, no border-radius from the parent) holds the dashed guide — `drawGuide()`: `rgba(155,109,255,0.35)`, lineWidth 12, `setLineDash([16,14])`, `GUIDE_FONT='900 288px Fredoka…'` for letters/numbers (companion `item.shape` check → `drawShapeOutline(g, item.shape, CW)` for shapes). `loadItem()` regenerates the guide AND clears the ink canvas. The guide is never ink.
- Layout: `#tracingRef` (compact ref card) `min(18vmin,14vh)`; `#tracingBoard` wrapper `min(50vmin,34vh)`, `aspect-ratio:1/1`, holds canvas + guide; caption `4.6vmin` below; activity container `overflow-y:auto` — no overlap.
- Matching (`matchResult`) on a 48×48 grid: `refGrid()` = ink of the guide canvas, `inkGrid()` = user ink. Pass = `coverage ≥ MIN_COVER(0.45)` && `near ≥ MIN_NEAR(0.8)` && `ink ≤ MAX_INK(2000)`; `MIN_INK=40` filters accidental taps. `near` = fraction of ink cells within `dilate(ref,2)` (integral-image dilation). Measured (tools/tracing_probe.js): good traces near 0.98–1.0, blobs 0.69, scribbles 0.62, single line 0.7 — big margin at 0.8. Do NOT raise thresholds without re-running the probe.
- Feedback: correct → `window.celebrate('✏️')` + speak name (+ word for letters) then auto-advance after 2600 ms (`glyphDone()` → `loadItem(index+1)`, which clears ink + redraws guide); wrong → "Покушај још једном!" nudge, canvas kept so the child draws over.
- Облици = only the 4 flat shapes (Круг/Квадрат/Троугао/Звезда); 3D wireframes dropped per user decision.
- `window.__traceDebug` (`matchResult`/`refGrid`/`inkGrid`) is used by `tools/tracing_probe.js` — keep it.
- Data: `LETTERS` (30, `label` capital / `name` speech key / `word` + `emoji`), `NUMBERS` (0–10), `SHAPES` (4). Caption "Нацртај А", counter "N од 30/11/4". ➡ `#tracingNext` advances anytime; ↩ `#tracingBack` → hub.
- Wiring: `navigation.js` maps `game-tracing` → `pages/tracing.html`; `main.js` standalone boot `'tracing': ['tracing-back','startTracing']`; hub ✏️ button in `index.html` (`data-go="game-tracing"`).
- All names/words reuse existing `speech.js` MP3 keys (no new audio). **Validation: `node tools/tracing_smoke.js` (22 checks) — expect ALL PASS.** Screenshots in `resources/tracing/*.png` are STALE (pre-redesign); re-capture before the MiMo V2.5 Free visual pass.

Validation checklist (run after edits)

- Syntax check modified/added JS files: `node --check game/shared/*.js game/games/*.js`
- Tracing changes → run the canonical smoke: `node tools/tracing_smoke.js` (expect ALL PASS). Metric/threshold tuning → `node tools/tracing_probe.js`.
- Hub (`index.html`) changes → run the canonical smoke: `node tools/hub_smoke.js` (expect ALL PASS).
- Adventure engine / Driving / Ocean / Dino / Space changes → run the canonical smoke: `node tools/adventure_smoke.js` (expect ALL PASS).
- New headless checks → build on `tools/headless.js` (see `tools/README.md`); never write one-off temp probes.
- Open `game/index.html` in a browser to validate navigation and visual parity.
- For `papper_kitty.html`, extract inline JS and run node --check on the extracted code.
- For `papper_kitty.html` music: after touching `MUSIC` or the engine, re-run the Node stubbed-WebAudio harness (30 checks; script was staged at `%TEMP%/opencode/pk_music_test.js` reading extracted JS from `pk_check.js` — recreate by extracting the inline `<script>` and stubbing `AudioContext`/`localStorage`/`document`).
- Latest visual screenshot for Kitty coins is `resources/kitty-coins-no-disc.png` (bare opaque emoji collectibles, no backing circle — user feedback #4, 2026-08-03) — captured but NOT yet reviewed (current model can't read images; use MiMo V2.5 Free for visual review).
- Verify animal sound coverage after changing animal lists: cross-check `game/shared/audio.js` map against `name:` entries in each `game/games/*.js`.

Hosting notes (deployed on Cloudflare Workers at `games-for-kids.radman-milos-work.workers.dev`)

- `Unchecked runtime.lastError: The message port closed...` in the console is injected by a browser extension, not the site — ignore it.
- Every page declares an inline SVG favicon so no `favicon.ico` 404 is requested.
- `shared/audio.js` uses `preload='none'` so audio files load on demand instead of 11 downloads on every page load; the fallback in `playAnimalSound` fires when a source is missing, genuinely failed (`networkState === 3` or an `error` event set the `_failed` flag), or `play()` rejects.
- `shared/main.js` boot wires the back button and starts the game independently (typeof guards) so a missing game module can't break the page.
- Cloudflare Workers pretty-URLs redirect `/pages/<name>.html` → `/pages/<name>`, so `location.pathname` has NO `.html`. All standalone detection (`shared/main.js`, `animal_puzzle.js`, `animal_counting.js`, `animal_memory.js`) must strip `.html` before comparing page names. Do not match against `'name.html'`.
- `pages/papper_kitty.html` self-hosts the Fredoka font via two local `@font-face` rules (`game/assets/fonts/fredoka-latin.woff2`, `fredoka-latin-ext.woff2`, variable font weights 100–700) — bundled 2026-08-03 per user approval, no external font requests at runtime.

Session log — 2026-08-04 (docs accuracy + nula.mp3 rename)

- Renamed the zero-number MP3 (old name `nul.mp3`, blocked by the Windows reserved `nul` device name) → `nula.mp3` and updated every reference: `speech.js` (`'нула'`/`'Нула'` → `nula.mp3`), `resources/tts_generate.js` generator entry (`nul` → `nula`), and the doc mentions in HANDOVER / PROJECT_TASKS / CodeReview_Findings. `node --check` passes on both JS files; `nula.mp3` is now tracked and the working tree is clean.
- Docs accuracy pass: verified app state vs documents (working tree clean, tasks up to 54 DONE). PROJECT_TASKS / HANDOVER / AGENTS were already accurate; README.md fixed 7 stale spots: (1) Fonts section still claimed Paper Kitty HUD uses Press Start 2P — now Fredoka (bundled locally); (2) Coloring scene list missing Chicken (12 scenes); (3) Coloring polish-backlog example "Животиња 3 од 11" → "од 12"; (4–7) folder-structure/architecture listings in README missing `tracing.html` / `tracing.js` (two places each) + repo tree missing `tools/` and top-level docs.

Session log — 2026-08-03 (Tracing free-draw redesign + tools/ consolidation)

- Free-draw redesign (user feedback #2: dots not intuitive → draw on a dashed guide): `tracing.js` now renders a dashed guide on a `#tracingGuide` canvas (letters/numbers via `GUIDE_FONT`, shapes via `drawShapeOutline`), `tracing.html` re-laid out (`#tracingBoard` 1:1 wrapper, compact ref above, caption below, scrollable column); metrics matching on a 48×48 grid with `near` (guide proximity) as the decisive rule (`MIN_NEAR=0.8`, `MAX_INK=2000`, `MIN_COVER=0.45`, `MIN_INK=40`); success auto-advances after 2600 ms; Облици = 4 flat shapes only; `__traceDebug` kept for the probe. Measured separation near 0.98–1.0 (good) vs ≤0.70 (blobs/scribbles).
- tools/ consolidation (user request for a reusable tooling home): `tools/headless.js` shared harness (unique temp profile per run, kills only its own Chrome), `tools/tracing_smoke.js` (22 checks — ALL PASS), `tools/tracing_probe.js` (metric table), `tools/dilate_test.js`, `tools/README.md`. Old temp probes in `%TEMP%/opencode/` superseded.
- Docs: PROJECT_TASKS.md task 53 follow-up + task 54 DONE; AGENTS.md (tools/ + validation command); HANDOVER refreshed; README tracing entry updated (see README notes).

Session log — 2026-08-03 (Писање / Tracing game, task 53)

- Built `game/games/tracing.js` + `game/pages/tracing.html`: connect-the-dots tracing, forgiving radius (auto-fill skipped segments, impossible to fail), hub with Слова/Бројеви/Облици + auto-playing pencil intro, ➡ next button, progress counter, celebrate + speech on completion. Reused classroom letter names/words/emoji and all existing `speech.js` MP3s (no new audio assets).
- Wired `navigation.js` (`game-tracing`), `main.js` (`'tracing'` standalone boot), hub ✏️ button in `index.html` (fills task-47 orphaned-row finding; hub now 10 games).
- Validated: `node --check` on tracing/navigation/main; headless Chrome smoke harness `%TEMP%/opencode/tracing_smoke.js` passes 10/10; 5 screenshots in `resources/tracing/*.png` (visual review pending with MiMo V2.5 Free).
- Docs: PROJECT_TASKS.md task 53 DONE; README Current Status + roadmap (Letter/Number Tracing marked ✅ built, hub count updated); HANDOVER refreshed.

Session log — 2026-08-03 (code-review fixes round)

- All actionable findings from `CodeReview_Findings.md` were fixed in one pass; full action log + validation is in that doc ("Fixes performed (2026-08-03, per user request)"). Summary:
  - speech.js: lowercase `'нула'` alias added → classroom zero tile plays nula.mp3.
  - Keyboard accessibility: animals card, candy tiles, shapes pieces/slots, puzzle pieces, coloring regions are now focusable with Enter/Space (and Arrows for candy swaps); Cyrillic aria-labels; shared focus outline in accessibility.css.
  - data-go dedupe: counting/puzzle/memory back buttons now have exactly one handler (blank flash + double popSound gone).
  - audio.js: `_failed` flag via `{once:true}` error listener; playAnimalSound falls back on it.
  - papper_kitty.html: `@media (max-width:329px)` clamps 🔊/🌍 offsets (68px gap preserved) + `musicTick` returns early when paused.
  - animal_puzzle.js: pickup tone added (previously fully silent).
  - index.html stripped dead in-hub game screens + unused game scripts; animals/shapes/matching_game standalone pages slimmed to a single screen + only their own game script (unused `.hub-*` CSS left intentionally).
- Google Fonts (Fredoka) bundled locally per user approval: `game/assets/fonts/fredoka-latin.woff2` + `fredoka-latin-ext.woff2`, two local `@font-face` rules in papper_kitty.html, external `<link>` removed. No external runtime deps remain in `game/`.

Recommended next steps

- **Cleanup tasks 80–89 are in progress (Phase 5 = ИГРЕ polish only; УЧЕЊЕ apps out of scope).** Task 83 is REVERTED (see current state — user play-test showed reduced-motion gates stripped gameplay feedback on their OS; all animations restored, plus two pre-existing task-79 split regressions fixed: driving dashed road + ocean/space obstacles). **Next up: task 84** — document the `heroFlip: true` sprite convention in `game/games/dino.js` (PNGs face left natively; engine mirrors them to face right; add a comment so future devs don't break facing). Then 85 (audio.js synth fallbacks — remove or comment), 86 (CONTRIBUTING.md — ALREADY DONE as task 78; verify), 87 (PROJECT_TASKS formatting), 88 (trim HANDOVER), 89 (README structure consistency), then task 74 (whatever it is — check PROJECT_TASKS). Before task 84, consider the deferred dino_smoke/ocean_smoke/space_smoke pre-existing failures (worlds-picker + music-toggle, verified against a git-stash HEAD baseline — see current state).
- **Task 56 (GitHub Actions deploy) is ABANDONED; task 57 (docs/ mirror) is DONE.** Deployment rule: `game/` is the single source of truth; on EVERY change to `game/`, replace the ENTIRE `docs/` content with the new `game/` content via `tools/sync-docs.sh`, then commit and push to `main`. GitHub Pages serves main → `/docs`, so the push publishes **https://radmanmilos.github.io/Games-for-kids/**.
- **Phase 3 (Учионица kids tier, task 55) is DONE** — wait for the user's play-test feedback on the 4 quiz games ("За децу" set in Учионица) and apply polish rounds as they come.
- **Phase 4 is COMPLETE (tasks 58/59 Клавир/Piano, 60 Возила/Driving, 61 Океан/Ocean, 62 Дино/Dino, 63 Свемир/Space all DONE)** — the user picks the next mini-game from the Future Mini Games list (Musical Instruments, Balloon Pop, Farm, Fruit Matching, Story Time, Birthday Cake Builder …); each chosen game gets its own task. Per the project rule, wait for the user's play-test feedback on Свемир before any follow-up. Phase 4 adventure order (all shipped per user decision): 60 = Возила (drive), 61 = Океан (fly), 62 = Дино (ground), 63 = Свемир (fly, last).
- **Deploy reminder:** every time `game/` changes, run `tools/sync-docs.sh`, commit, and push to `main` — GitHub Pages serves main → `/docs` at https://radmanmilos.github.io/Games-for-kids/.
- Task 43 (alphabet pronunciation + image audit in Учионица) is **DONE (2026-08-04)**: letter tiles now speak the letter sound (25 consonants) instead of the name; words+emoji replaced Е→Екран🖥️, И→Игла🪡, Џ→Џемпер🧥; 28 new MP3s generated and 28 obsolete ones deleted. REMAINING: the user's listening pass on the 25 sound MP3s + 3 words (Google TTS may render a bare consonant like "б" as "бе" — flag any file that needs regenerating via `resources/tts_generate.js`).
- Visual pass pending: **Tracing screenshots are STALE** — `resources/tracing/*.png` predate the free-draw redesign; re-capture (via `tools/headless.js` or Live Server) before reviewing with MiMo V2.5 Free. The 12 coloring scenes `resources/coloring-redraw/*.png` also still await review with MiMo V2.5 Free.

Session log 2026-08-04

- Task 43 executed per user's approved list (all 25 consonants → bare sounds; vowels unchanged; Екран/Игла/Џемпер word swaps). Edited `game/games/classroom.js` + `game/games/tracing.js` (identical ALPHABET/LETTERS data), `game/shared/speech.js` (sound keys → new MP3 filenames; word keys), `resources/tts_generate.js` (generator pairs). Ran generator → `generated: 28, skipped: 53, failed: 0`; verified all 28 MP3s present; removed the 28 obsolete MP3s. `node --check` on all 4 JS files PASS; grep confirmed zero old references; `node tools/tracing_smoke.js` 22/22 PASS. Docs updated (README, HANDOVER, PROJECT_TASKS task 43 → DONE).
- Task 55 (Phase 3: Учионица kids tier) built: classroom hub now has two sets "За малишане"/"За децу"; new `game/games/kids_games.js` shared 8-question multiple-choice engine + 4 configs (Азбука/Бројеви/Боје/Облици за децу); classroom.js exposes `window.classroomData` + kids screen switching; classroom.html added `#kidsGame` screen, kids-tier CSS, and the previously-missing `utils.js` + `celebration.js` script tags. Validated: node --check; `node tools/kids_smoke.js` 12/12 PASS; baby-tier numbers probe 4/4 PASS. Docs updated (PROJECT_TASKS task 55 → DONE, README Phase 3 ACTIVE / Phase 4 queued, tools/README).
- Task 56 (GitHub Actions auto-deploy) tried then ABANDONED: user ran `tools/deploy-game-gh-pages.sh`, PR #2 merged the workflow (`.github/workflows/deploy.yml` deploying `game/` → `gh-pages`), but GitHub Pages refused to serve the site from `game/`, so the user dropped the workflow approach (2026-08-04, per user decision). Workflow + `tools/deploy-game-gh-pages.sh` both removed.
- Task 57 (GitHub Pages via `docs/` mirror) DONE: user pasted the whole `game/` content into `docs/`; Pages serves main → `/docs` at https://radmanmilos.github.io/Games-for-kids/. New `tools/sync-docs.sh` automates "replace the ENTIRE `docs/` content with the new `game/` content on every game change". Verified no root-absolute asset refs (app works under `/Games-for-kids/`). Docs updated (README Deployment section rewritten, PROJECT_TASKS tasks 56/57, HANDOVER).
- Task 58 (Клавир/Piano, Phase 4 first game) DONE: user picked Piano from the Future Mini Games list and approved the scope proposal as-is. Built `game/pages/piano.html` + `game/games/piano.js`: 8-key octave (rainbow-coded), free play + "Свирај песму" follow mode ("Трепери, трепери звездице", 42 notes, Web Audio only), wrong-key nudge, 🏅 finish panel + replay, 🔊 preview with mid-stop re-lighting of the current note. Wired hub button/navigation/main.js boot. Validated: node --check + new `node tools/piano_smoke.js` 12/12 PASS. Docs updated (PROJECT_TASKS task 58 DONE, README Future Mini Games ✅ + Phase 4 entry + Current Status, tools/README, HANDOVER).
- Task 59 (piano polish, per user feedback "looks and sound good"): hub button 🎹 moved to 3rd place; refactored to multi-song engine with a **song chip picker** and 2 new songs the user chose (#1 Срећан ти рођендан, #3 Џингл белс) — Трепери kept as default. Counter/finish/preview adapt to the chosen song. Fixes found by smoke: initial active chip not highlighted. Validated: `node tools/piano_smoke.js` 14/14 PASS. docs/ re-synced; remaining = user commit/push.
- Task 60 (shared adventure engine + Возила/Driving, first of four Phase 4 adventure games) DONE: built `game/games/adventure.js` (config-driven engine, modes ground/drive/fly) + `game/pages/driving.html` + `game/games/driving.js` (drive mode, 10 road worlds, per-world music + ambient, 13 coins, 7–8 obstacles, goal gate); hub 🚗 button + navigation route + main.js standalone boot wired. Fixed two drive-mode engine bugs during smoke (obstacle-collision `o` scope; stale `player.x` after respawn). Validated: node --check + `node tools/driving_smoke.js` 17/17 PASS (run repeatedly, stable). Docs updated (PROJECT_TASKS task 60 DONE, README game section + Current Status + Future list + folder trees, tools/README, HANDOVER). NOT yet run: `tools/sync-docs.sh` + user commit/push. **Pause here for user play-test feedback before task 61 (🐠 Океан).**

Driving play-test follow-up (2026-08-04): only the vertical controls remain on `driving.html` (▼ left, ▲ right); drive mode freezes the car and road once the finish line opens the next-world modal; the road has one dashed center divider; driving car, obstacles, and pickups are larger. Canonical validation is `node tools/driving_smoke.js` after the follow-up smoke assertions.

Driving final follow-up (2026-08-04): the updated transparent PNG car is tinted red, car/obstacle collision bounds are forgiving, and the driving clamp now allows the car 20px above the road edge so the large car can use more of the upper road. Task 60 is DONE; per the project rule, wait for user feedback before starting task 61 (🐠 Океан).

Session log — 2026-08-05 (task 61: Океан/Ocean, second Phase 4 adventure game)

- Built `game/games/ocean.js` + `game/pages/ocean.html` on the shared adventure engine: fly mode (free 2D, no gravity), hero 🐟, 10 underwater worlds with 8 obstacles + 13 coins + 🏁 goal banner each, 9 obstacle types (patrolling sharks via vx/minX/maxX, jellyfish, rocks, mines, seaweed, pufferfish, crab, anchor, default rock), 10 music themes + 10 decor types. Added 3 engine hooks: `cfg.drawObstacle`, `cfg.drawDecor`, `cfg.heroFontSize`. Wired hub 🐠 button, `navigation.js` route, `main.js` standalone boot.
- Canonical validation: new `node tools/ocean_smoke.js` → **18/18 PASS**. Three smoke checks fixed during the session: (1) coin test failed because `a.cameraX = …` silently no-ops on the getter-only debug handle — now resets via `a.loadWorld()`; (2) obstacle-hit check needed `await sleep(1100)` (matches driving — the `lastHitAt` cooldown must expire after `loadWorld()`); (3) patrol test asserted `x2 < x1` which is wrong — the bounce keeps the shark past its earlier position; assert on the `vx` flip at both bounds instead.
- Docs updated: PROJECT_TASKS (task 61 DONE + header), README (new 🐠 game section, Current Status, Future Mini Games + Phase 4 roadmap ✅, folder trees), tools/README (ocean_smoke entry + getter-only `__adv` gotcha), HANDOVER. NOT yet run: `tools/sync-docs.sh` + user commit/push. **Pause here for user play-test feedback before task 62 (🦕 Дино).**
- **Ocean polish round (2026-08-05, per user play-test feedback):** (1) fish faced the wrong way — the 🐟 emoji points LEFT natively, so new engine hook `cfg.heroFlip` inverts the `facingRight` scale (ocean sets `heroFlip: true`); (2) fish now bobs while swimming — new engine hook `cfg.heroBob` (ocean sets `heroBob: 5`) adds a gentle sine bob to the hero draw; (3) tablet-friendly controls — fly D-pad split into two thumb clusters: ◀▶ horizontal pad on the LEFT edge, ▲▼ vertical pad on the RIGHT edge (`adv-pad-horizontal`/`adv-pad-vertical` in adventure.css; HTML button order changed to `left,right,up,down`). Validated: node --check + `node tools/ocean_smoke.js` → **20/20 PASS** (updated controls-order/grouping checks + `heroFlip`/`heroBob` static check). Screenshots for visual review: `resources/ocean-review/{idle,facing-right,facing-left}.png` (Big Pickle can't read images — use MiMo V2.5 Free). docs/ re-synced; user commit/push still pending.

- **Session log - 2026-08-05 (task 62: 🦕 Дино/Dino, third Phase 4 adventure game, ground mode):**
  - Built `game/games/dino.js` + `game/pages/dino.html` on the shared adventure engine in **ground mode** (jump/gravity/platforms): hero 🦕 (`heroFlip: true`, `heroFontSize` 78, `speed` 5.2, `jumpPower` −13.5), 10 prehistoric worlds (Прашума, Тропска долина, Језеро, Вулкан, Пећина, Мочвара, Ледено доба, Пустиња, Ноћни свет, Острво диносауруса) built from `grounds`/`floats`/`moves`/`stairs`/`pipes` + ~15 coins + 🏁 ЦИЉ goal, 10 music themes + 10 decor types. Engine hooks used: `cfg.drawEnemy` (mini-raptor popping from pipes every 3s), `cfg.drawDecor`, `cfg.drawGoal` (stone temple arch + ЦИЉ banner). Ground controls `#adv-controls.adv-ground-controls`: ◀▶ D-pad cluster on the LEFT edge + one big ⬆ jump button on the RIGHT edge (new CSS in adventure.css).
  - **Engine bug found+fixed (dino is the FIRST ground-mode game):** ground-mode `loadWorld` crashed on worlds missing `floats`/`moves`/`pipes` (`TypeError: Cannot read properties of undefined (reading 'map')`), leaving `goal` null → update crash; all three now default to `(w.x || [])`. Added `get mice()` to `window.__adv` for enemy smoke checks.
  - Wired hub 🦕 button, `navigation.js` route, `main.js` standalone boot `'dino': ['dino-back','startDino']`.
  - Canonical validation: `node --check` + new `node tools/dino_smoke.js` → **25/25 PASS** (boot, ground-mode HUD/theme, 10 world configs, ground-feature variety, 10 music themes, fall-and-land physics, run left/right, jump, coin pickup delta, empty-obstacles ground check, pipe wall-stop, raptor 3s pop cycle, raptor knockback, goal → win modal, next level, worlds picker, music toggle, hub/nav/boot static checks).
  - Docs updated: PROJECT_TASKS (task 62 DONE + follow-up entry), tools/README (dino_smoke 25-check row), README (Дино section, fourteen games, roadmap/status, 🦕 built, task-62 log). `tools/sync-docs.sh` RUN — docs/ now mirrors game/ (162 files). User commit/push still pending. **Pause here for user play-test feedback before task 63 (🚀 Свемир, fly mode).**

- **Session log - 2026-08-05 (task 63: 🚀 Свемир/Space, fourth and FINAL Phase 4 adventure game, fly mode):**
  - Built `game/games/space.js` + `game/pages/space.html` on the shared adventure engine in **fly mode** (free 2D, no gravity): hero 🚀 (`heroBob: 4` — NO `heroFlip`: 🚀 faces RIGHT natively, unlike 🐟/🦕; `heroFontSize` 84, `heroW` 110, `heroH` 100, `offsetStartRatio` 0.16, `speed` 4.5, `obstacleScale` 1.15, `pickupFontSize` 58), 10 space worlds (Звездано небо, Месечева стаза, Црвена планета, Астероидни појас, Сатурнови прстенови, Јупитеров вихор, Ледени месец, Свемирска станица, Галаксија, Дубоки свемир) with 8–10 obstacles + 13 coins + ЦИЉ portal goal (`drawSpaceGoal`: two pylons + glowing portal) each, 8 obstacle types (meteors, asteroids, patrolling UFOs, comets, ring bands, planets, satellites, black holes) + alien cameo, 10 music themes + 10 decor types. **No new engine work** — reused the hooks from tasks 60–62 (`drawObstacle`, `drawDecor`, `drawGoal`, `heroFlip`, `heroBob`, `obstacleScale`, `speed`). Page reuses the existing `.adv-fly-controls` (◀▶ left pad, ▲▼ right pad) — no new CSS.
  - Wired hub 🚀 button (`data-go="game-space"`, after dino), `navigation.js` route, `main.js` standalone boot `'space': ['space-back','startSpace']`.
  - Canonical validation: `node --check` on all changed JS + new `node tools/space_smoke.js` → **18/18 PASS** (boot, fly-mode HUD/theme, 10 world configs, 10 music themes, 4-way steering, clamps, coin pickup + HUD, obstacle knockback, UFO patrol vx-flip at both bounds, goal → win modal, next world, worlds picker jump to world 6, music toggle, hub/nav/boot static checks + no-heroFlip/heroBob config). Re-ran driving/ocean/dino smokes — all still PASS (shared wiring untouched).
  - Docs updated: PROJECT_TASKS (task 63 DONE + follow-up entry), tools/README (space_smoke 18-check row), README (Свемир section, fifteen games, roadmap/status, 🚀 built, task-63 log), HANDOVER. `tools/sync-docs.sh` RUN — docs/ mirrors game/. User commit/push still pending (task-62 + task-63 work together). **Pause here for user play-test feedback on Свемир (Phase 4 is complete).**

- **Session log — 2026-08-05 (task 67: Слагалица животиња/Puzzle polish):**
  - Per user requirements: **8 painted scenes** (5 original redone better + 3 new — Другари на фарми 🐄, Другари у свемиру 🚀, Другари на снегу ⛄), **NO text on any image** (painted title banner removed from `paint()`, title only in the page h1), **thin frame on every picture** (white 9px band + plum 2px inner line via `strokeRect` in `paint()`, drawn last so it overlays — edge/corner pieces show it after splitting), and **ring placement** replacing the random scatter: pieces go into the top/bottom/left/right bands around the board (greedy first-fit on shuffled band candidates + fine-grained fallback), so no piece covers the board and no two pieces overlap. Board resized to `width:min(46vw,420px,60vh)` (was `min(58vw,500px)`) so 2×2 pieces physically fit in the surrounding bands at every screen size. Existing painters enhanced: park (fountain, bench, extra flowers, butterfly), house (curtains, path, second bush, fence), savanna (watering hole, rocks, birds), forest (third tree, berry bush), sea (submarine, coral, starfish, bubble moved off the sub).
  - New canonical validation: `tools/puzzle_smoke.js` → **16/16 PASS** (boot; pixel-verified frame edge white / inner plum line / no-banner sky at the old title spot; static checks — no `fillText(scene.title`, farm/space/winter themes, frame strokeRect, board sizing; ring checks at 2×2 AND 3×3 with 0 pieces on the board + 0 overlaps; keyboard-completed all 8 puzzles → scene order correct, grids alternate 2×2/3×3, final score 8). `node --check` clean on `animal_puzzle.js`. Three smoke-test bugs fixed during the run (double-`JSON.stringify` on the ring results, and a 2-char-short `bg` prefix comparison) — the game code was correct.
  - Docs updated: PROJECT_TASKS (task 67 DONE), tools/README (puzzle_smoke 16-check row + command), HANDOVER. `tools/sync-docs.sh` to run; user commit/push still pending (tasks 65+66+67 together). **Remaining Phase 5: 68 Kitty, 69 adventure tuning — both need user play-test feedback first.**

- **Session log — 2026-08-05 (task 68: Папир Маца/Paper Kitty polish, level-element redo):**
  - Scope confirmed with the user: "worlds are fine" → no pacing change. Implemented **ground-walking enemies** (4 per world, data-driven `walkers:[{x,range}]` patrols; jump-on = defeat + coin, side-touch = cat sound + respawn; freeze when the level is won), **submarine finish line** in the water world (new `drawGoal('submarine')` branch), and a **gentle bob** for the kitty (`bobPhase` → `sin(bobPhase)*2.5` while grounded). 11 new **dedicated walker sprites** in `drawWalker(type)` (ladybug/bee/hedgehog/snowman/worm/spider/crab/meerkat/panda/seal/scarab) with per-type `WALKER_FOOT` feet offsets so they stand flush on the ground — all pixel-measured ≤ 48×48 (≤ kitty size, per user rule; worm was 49px wide → shrunk to 44).
  - New canonical validation: `tools/kitty_smoke.js` → **14/14 PASS**. First smoke run caught real data bugs: spring/summer/savanna/northpole/egypt walker0 patrols crossed a pipe (also made the stomp test land the kitty on the pipe) → patrols shifted past the pipes. Probe gotcha: `drawWalker` uses the page's global `ctx`, so the sprite-size check reads back from the real canvas (offscreen canvas came back empty).
  - Docs updated: PROJECT_TASKS (task 68 DONE), tools/README (kitty_smoke 14-check row + command), HANDOVER. `tools/sync-docs.sh` to run; user commit/push still pending. **Remaining Phase 5: 69 adventure tuning — needs user play-test feedback first.**

- **Session log — 2026-08-05 (adventure polish: Дино hero picker + shared floating controls + smoke fixes):**
  - **Дино hero picker** (per user): the child now picks one of 4 dinos at the start of every level — Тиранозаур 🦖 / Трицератопс 🦕 / Стегосаурус / Диплодок. Each is drawn by hand with `drawDinoHero` (`game/games/dino.js`) — 4 sprites, all facing right. New engine hooks in `adventure.js`: `cfg.pickHero` (opens the picker via `cfg.onHeroNeeded(game)`), `cfg.drawHero(ctx, type, facing, w, h)` (replaces the emoji `fillText` when defined), `cfg.onHeroChosen`, `cfg.decorBehind` (draws decor BEHIND the world — flag + `drawIsland` bug fix so island decor no longer draws off-canvas), `cfg.heroBob` moved from a hook into a generic bob (dino bobs while grounded, 0 in air); engine state: `heroType` + `setHeroType(type)` + `get paused`, and `maybePickHero()` runs on every `loadWorld` (level start AND worlds-picker jumps). Picker overlay in `game/pages/dino.html` + `.adv-dino-picker` CSS (4 preview buttons with names + one large confirm). All picker copy in Serbian Cyrillic.
  - **Shared floating controls** (`game/shared/adventure.css`): the 4 adventure games' `#adv-controls` is now a **floating overlay over a 100%-size canvas** (`.adv-canvas-wrap` + canvas `position:absolute;inset:0`) instead of the old stacked-canvas-then-buttons layout — the canvas gets the full viewport and the D-pads float on the edges. Driving/ocean/space/dino all re-verified after the CSS change.
  - **Engine/smoke correctness findings (both false alarms resolved):** (1) the occasionally-flaky dino pipe-collision smoke check is NOT a game bug — my debug harness read `levels[worldPos]` instead of the loaded world's pipes after the worlds-modal `worldOrder` shuffle; a corrected probe proves the pipe collision fires and sticks in **all 10 worlds** (`rightEdge == pipe.x`). (2) `driving_smoke.js` "obstacle hit" check had a **pre-existing unsatisfiable assertion** — `loadWorld()` resets `cameraX = 0` and drive `respawnPlayer` clamps `cameraX = Math.max(0, cameraX-160)`, so `after < before` can never hold (reproduced at clean HEAD). Fixed to assert `bumpCount` increased (same pattern as dino/ocean/space smokes) — collision code was correct all along.
  - Validated: `node --check` on adventure.js/dino.js/driving_smoke.js/dino_smoke.js + canonical smokes **dino 25/25, ocean 20/20, driving 17/17, space 18/18 — ALL PASS**. `tools/sync-docs.sh` to run; user commit/push still pending.

- **Session log — 2026-08-05 (adventure polish round, task 70 — fly spawn / full-height fly spread / pit hazards / vortex stargate / ground-under-finish / drop-through fix):**
  - **Fly-mode spawn clear of the bottom controls (ocean/space):** `x = Math.max(280, canvas.width * 0.3)`, `y = Math.round(canvas.height * 0.4)` — the hero no longer spawns hidden behind the bottom fly D-pad cluster. Same file locations as the ground-mode spawn (~adventure.js 509–513).
  - **Full-vertical fly-mode layout (ocean/space):** new engine var `flyDyScale`, set per fly world to `(canvas.height * 0.88) / maxDy` where `maxDy = Math.max(70, all obstacle/coin dy, goalDy || 210)`. `dyScale` remains drive-only. `loadWorld` uses the fly layout only in `fly` mode, so ground/drive are untouched. Probe (ocean ch=700): obstacles y-span 452, coins 448; space: 462/406/655; spaceship spawn `{x:330, y:280}` — the spread now crosses nearly the full screen height.
  - **Themed pit hazards (dino + kitty):** per-world `pitHazard` — `lava` (volcano, cave, underground, cave2) drawn with rising animated bubbles, `water` (lake, swamp, island, spring, summer, underwater, japan) with waves + a swimming 🐟, `spikes` (jungle, valley, ice, desert, night, autumn, winter, savanna, northpole, egypt) as dark-tunnel spikes at the pit floor. Pit geometry is computed from the theme's sorted grounds; new `drawPitHazards(t)` in adventure.js and `drawPitHazards()` in kitty, both hooked into `draw()` right before platforms. No gameplay change (pits were already deadly).
  - **Space vortex stargate finish (task 63 follow-up):** `drawSpaceGoal` (`game/games/space.js`) now draws true pylons (middle tile), a three-arm swirling vortex (three rotating `ellipse` arms at `t*0.5`), an event-horizon core with sparkles spiraling inward, then the „ЦИЉ" pill. Replaces the old checkered arch.
  - **Dino ground always under the finish line (root cause):** the ground-mode goal fires when `player.x + width > goal.x + 30`, but every dino world's last ground previously ended exactly at `goalX` — the player would fall into the pit under the finish banner to trigger the win. Extended all 10 dino last-grounds (`w` +260 in dino.js) so they underlay the whole `[goalX, goalX+220]` banner. New regression: no `lastRight < goalX + 220` for any world. (Kitty's grounds already covered its finish line.)
  - **Root-cause "drop through the ground and respawns" fix (dino + kitty):** both engines now re-anchor world geometry to the new canvas height on `resize` (`dy = canvas.height - oldH`; platforms `p.y += dy` in ground mode, plus `mice`/`walkers` y-shifts in kitty, and non-ground obstacle/coin/goal y-shifts in adventure). The ground stays at `canvas.height - 140`; previously the old pre-resize `gy` was left in place, so a tall canvas resize put the ground below the visible area and triggered a respawn loop. Plus **swept-landing**: static/moving platform checks now compare `previousY + player.height <= p.y + 24` (with `player.vy >= 0`) instead of a player-only vertical band — this removes the 20/22px vertical-window dependence that let fast falls tunnel through the ground on tall canvases.
  - **Kitty load-order bug found during the fix:** `resizeCanvas` runs at script load before `theme`/`groundY` are initialized → TDZ crash; added a hoist-safe `worldLoaded = false` flag, set `true` at the end of kitty `loadWorld`; the resize guard is now `if (!worldLoaded || …)`.
  - **Smoke updates:** `tools/dino_smoke.js` grew to **38 checks** (pit-geometry check: every world has ≥2 open fallable pits ≥150px, one ≥180px, none >240px; goal-ground check `lastRight ≥ goalX+220`; a `Emulation.setDeviceMetricsOverride` 1100×1200 resize test re-anchoring the dino ground to `gy=1060` (=1200−140) and landing on it; a `draw()` check for chosen-hero + behind-world decor). `tools/space_smoke.js` grew to **21 checks** (new `draw()` check: space decor + vortex stargate goal render without error, appended last to avoid resetting the world between goal/next-level tests). `tools/kitty_smoke.js` is **15 checks**. Ocean/space/driving coin-pickup checks now compare `a.coinCount` before vs after the teleported pickup instead of an absolute `1` — coinCount is a session total, and the full-screen fly spread made the steering-test path collect coins via `loadWorld()`. All five smokes green on final runs: **dino 38/38, kitty 15/15, driving 17/17, ocean 20/20, space 21/21** (0 FAIL; kitty passed 6/6 consecutive runs — it was flaky earlier). Occasional stale Chrome needs `Get-Process chrome | Where-Object CommandLine -match 'remote-debugging-port' | Stop-Process` before serial runs. `node --check` clean on adventure.js/dino.js/space.js/ocean.js/driving.js.
  - Docs: HANDOVER/PROJECT_TASKS/tools-README updated this session; `tools/sync-docs.sh` to run (docs/games/space.js is stale); user commit/push pending.

Contacts and conventions

- Commit messages: include small clear messages. If you want commits authored, include the Co-authored-by trailer as in project conventions.
- Keep changes minimal and validate immediately.

End of handover.
