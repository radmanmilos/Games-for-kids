---
description: Run node --check on modified JS files
model: kilo-auto/free
---
Run `node --check` on all modified or added JavaScript files in `game/shared/` and `game/games/`. Report any syntax errors found. If no JS files were modified, run `node --check game/shared/*.js game/games/*.js` to verify the full codebase.