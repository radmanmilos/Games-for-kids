---
description: Sync game/ to docs/ and commit
model: kilo-auto/free
---
Run `tools/sync-docs.sh` to replace the entire `docs/` folder with the current `game/` content. Then commit and push to main. Verify the working tree is clean afterward.